<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>HBase启停流程</title>
    <link href="/2021/03/18/HBase%E5%90%AF%E5%81%9C%E6%B5%81%E7%A8%8B/"/>
    <url>/2021/03/18/HBase%E5%90%AF%E5%81%9C%E6%B5%81%E7%A8%8B/</url>
    
    <content type="html"><![CDATA[<h1 id="整体流程分析"><a href="#整体流程分析" class="headerlink" title="整体流程分析"></a>整体流程分析</h1><p>版本：hbase-2.2.4<br>说明：分析展现的源码和脚本中会省略一部分，只保留与分析相关联的，感兴趣的可自行查阅。</p><h2 id="启动"><a href="#启动" class="headerlink" title="启动"></a>启动</h2><h3 id="start-hbase-sh"><a href="#start-hbase-sh" class="headerlink" title="start-hbase.sh"></a>start-hbase.sh</h3><p>启动 HBase 的入口，有两种模式：单机模式和集群模式，何种模式取决于用户的配置，下文会详细说明。</p><pre><code class="hljs shell"><span class="hljs-meta">#</span><span class="bash"> 获取当前路径，即 &#123;HBASE_HOME&#125;/bin</span>bin=`dirname &quot;$&#123;BASH_SOURCE-$0&#125;&quot;`bin=`cd &quot;$bin&quot;&gt;/dev/null; pwd`<span class="hljs-meta">#</span><span class="bash"> 加载 bin 目录下的 hbase-config.sh 文件</span>. &quot;$bin&quot;/hbase-config.sh<span class="hljs-meta">#</span><span class="bash"> 判断加载 hbase-config.sh 是否成功，失败则退出，通常最后命令的退出状态为 0 表示没有错误</span>errCode=$?if [ $errCode -ne 0 ]then  exit $errCodefi<span class="hljs-meta">#</span><span class="bash"> 此处用户一般不传参，所以默认将 start 赋值给 commandToRun</span>if [ &quot;$1&quot; = &quot;autostart&quot; ]then  commandToRun=&quot;--autostart-window-size $&#123;AUTOSTART_WINDOW_SIZE&#125; --autostart-window-retry-limit $&#123;AUTOSTART_WINDOW_RETRY_LIMIT&#125; autostart&quot;else  commandToRun=&quot;start&quot;fi<span class="hljs-meta">#</span><span class="bash"> 通过 HBase 源码中的 HBaseConfTool 获取 conf/hbase-site.xml 中参数 hbase.cluster.distributed 的配置值，表示是否为集群模式，接下文附 1</span>distMode=`$bin/hbase --config &quot;$HBASE_CONF_DIR&quot; org.apache.hadoop.hbase.util.HBaseConfTool hbase.cluster.distributed | head -n 1`<span class="hljs-meta">#</span><span class="bash"> 当 distMode 为 <span class="hljs-literal">false</span> 时，启动单机测试版，此时 HMaster 和 HRegionServer 以及内嵌的 MiniZooKeeperCluster 均在同一个 JVM 里启动，接下文附 2</span>if [ &quot;$distMode&quot; == &#x27;false&#x27; ]then  &quot;$bin&quot;/hbase-daemon.sh --config &quot;$&#123;HBASE_CONF_DIR&#125;&quot; $commandToRun master<span class="hljs-meta">#</span><span class="bash"> 当该值为 <span class="hljs-literal">true</span> 时，启动 HBase 集群，接下文附 2。分别启动 Zookeeper、HMaster 和 HRegionServer，其中 Zookeeper 的启动情况分两种，一种是 HBase 管理的，一种是独立部署的，取决于是否在 hbase-env.sh 中配置 HBASE_MANAGES_ZK 参数，为 <span class="hljs-literal">true</span> 时由 HBase 管理。</span>else  &quot;$bin&quot;/hbase-daemons.sh --config &quot;$&#123;HBASE_CONF_DIR&#125;&quot; $commandToRun zookeeper  &quot;$bin&quot;/hbase-daemon.sh --config &quot;$&#123;HBASE_CONF_DIR&#125;&quot; $commandToRun master  &quot;$bin&quot;/hbase-daemons.sh --config &quot;$&#123;HBASE_CONF_DIR&#125;&quot; \    --hosts &quot;$&#123;HBASE_REGIONSERVERS&#125;&quot; $commandToRun regionserver  &quot;$bin&quot;/hbase-daemons.sh --config &quot;$&#123;HBASE_CONF_DIR&#125;&quot; \    --hosts &quot;$&#123;HBASE_BACKUP_MASTERS&#125;&quot; $commandToRun master-backupfi</code></pre><h4 id="附-1"><a href="#附-1" class="headerlink" title="附 1"></a>附 1</h4><p>调用 HBaseConfTool 及相关的 HBaseConfiguration 源码部分，可以清楚地看到读取了配置文件 hbase-default.xml 和 hbase-site.xml，通过脚本传入的 key 来获取相应的 value</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">HBaseConfTool</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String args[])</span> </span>&#123;        <span class="hljs-keyword">if</span> (args.length &lt; <span class="hljs-number">1</span>) &#123;          System.err.println(<span class="hljs-string">&quot;Usage: HBaseConfTool &lt;CONFIGURATION_KEY&gt;&quot;</span>);          System.exit(<span class="hljs-number">1</span>);          <span class="hljs-keyword">return</span>;        &#125;            Configuration conf = HBaseConfiguration.create();        System.out.println(conf.get(args[<span class="hljs-number">0</span>]));      &#125;&#125;---<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">HBaseConfiguration</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Configuration</span> </span>&#123;      <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Configuration <span class="hljs-title">create</span><span class="hljs-params">()</span> </span>&#123;        conf.setClassLoader(HBaseConfiguration.class.getClassLoader());        <span class="hljs-keyword">return</span> addHbaseResources(conf);      &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Configuration <span class="hljs-title">addHbaseResources</span><span class="hljs-params">(Configuration conf)</span> </span>&#123;        conf.addResource(<span class="hljs-string">&quot;hbase-default.xml&quot;</span>);        conf.addResource(<span class="hljs-string">&quot;hbase-site.xml&quot;</span>);            checkDefaultsVersion(conf);        <span class="hljs-keyword">return</span> conf;      &#125;&#125;</code></pre><h4 id="附-2"><a href="#附-2" class="headerlink" title="附 2"></a>附 2</h4><p>由 HMaster 接受脚本传入的参数，调用 ServerCommandLine 中的 doMain 方法解析后再通过 HMasterCommandLine 进行启动，单机版和集群版仅仅是在 HMasterCommandLine 中的 run 方法中判断后走了不同的逻辑。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">HMaster</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">HRegionServer</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">MasterServices</span> </span>&#123;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String [] args)</span> </span>&#123;        LOG.info(<span class="hljs-string">&quot;STARTING service &quot;</span> + HMaster.class.getSimpleName());        VersionInfo.logVersion();        <span class="hljs-keyword">new</span> HMasterCommandLine(HMaster.class).doMain(args);      &#125;&#125;---<span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ServerCommandLine</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Configured</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Tool</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">doMain</span><span class="hljs-params">(String args[])</span> </span>&#123;        <span class="hljs-keyword">try</span> &#123;          <span class="hljs-comment">// 加载 HBase 的配置文件，并调用 ToolRunner 类</span>          <span class="hljs-keyword">int</span> ret = ToolRunner.run(HBaseConfiguration.create(), <span class="hljs-keyword">this</span>, args);          <span class="hljs-keyword">if</span> (ret != <span class="hljs-number">0</span>) &#123;            System.exit(ret);          &#125;        &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;          LOG.error(<span class="hljs-string">&quot;Failed to run&quot;</span>, e);          System.exit(-<span class="hljs-number">1</span>);        &#125;      &#125;&#125;---<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ToolRunner</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">int</span> <span class="hljs-title">run</span><span class="hljs-params">(Configuration conf, Tool tool, String[] args)</span> <span class="hljs-keyword">throws</span> Exception </span>&#123;        ……                <span class="hljs-comment">// 调用 HMasterCommandLine 的 run 方法</span>        <span class="hljs-keyword">return</span> tool.run(toolArgs);    &#125;&#125;---<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">HMasterCommandLine</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">ServerCommandLine</span> </span>&#123;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">run</span><span class="hljs-params">(String args[])</span> <span class="hljs-keyword">throws</span> Exception </span>&#123;        <span class="hljs-comment">// 添加默认参数</span>        ……            CommandLine cmd;        <span class="hljs-keyword">try</span> &#123;          <span class="hljs-comment">// 解析参数，失败则最终会调用 HMasterCommandLine 的 getUsage 方法返回操作指示，此处将 start 作为 args 加入到 cmd 中</span>          cmd = <span class="hljs-keyword">new</span> GnuParser().parse(opt, args);        &#125; <span class="hljs-keyword">catch</span> (ParseException e) &#123;          LOG.error(<span class="hljs-string">&quot;Could not parse: &quot;</span>, e);          usage(<span class="hljs-keyword">null</span>);          <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;        &#125;            <span class="hljs-comment">// 配置参数</span>        ……                        <span class="hljs-comment">// 最终解析完成的剩下的参数，此处为 start</span>        <span class="hljs-meta">@SuppressWarnings(&quot;unchecked&quot;)</span>        List&lt;String&gt; remainingArgs = cmd.getArgList();        <span class="hljs-keyword">if</span> (remainingArgs.size() != <span class="hljs-number">1</span>) &#123;          usage(<span class="hljs-keyword">null</span>);          <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;        &#125;            String command = remainingArgs.get(<span class="hljs-number">0</span>);                <span class="hljs-comment">// 根据接收到的 command 调用相应方法</span>        <span class="hljs-keyword">if</span> (<span class="hljs-string">&quot;start&quot;</span>.equals(command)) &#123;          <span class="hljs-keyword">return</span> startMaster();        &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (<span class="hljs-string">&quot;stop&quot;</span>.equals(command)) &#123;          <span class="hljs-keyword">return</span> stopMaster();        &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (<span class="hljs-string">&quot;clear&quot;</span>.equals(command)) &#123;          <span class="hljs-keyword">return</span> (ZNodeClearer.clear(getConf()) ? <span class="hljs-number">0</span> : <span class="hljs-number">1</span>);        &#125; <span class="hljs-keyword">else</span> &#123;          usage(<span class="hljs-string">&quot;Invalid command: &quot;</span> + command);          <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;        &#125;      &#125;    <span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">int</span> <span class="hljs-title">startMaster</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-comment">// 获取配置参数</span>        Configuration conf = getConf();        <span class="hljs-comment">// TraceUtil 是个包装类，以一种简化的方式提供了访问 htrace 4+ 的函数，Apache HTrace 是 Cloudera 开源出来的一个分布式系统跟踪框架，支持HDFS和HBase等系统，为应用提供请求跟踪和性能分析</span>        TraceUtil.initTracer(conf);            <span class="hljs-keyword">try</span> &#123;          <span class="hljs-comment">// 这里从配置文件中识别出当前是单机模式还是集群模式，单机模式下指的是 LocalHBaseCluster 实例，会在同一个 JVM 里启动 Master 和 RegionServer</span>          <span class="hljs-keyword">if</span> (LocalHBaseCluster.isLocal(conf)) &#123;            DefaultMetricsSystem.setMiniClusterMode(<span class="hljs-keyword">true</span>);            <span class="hljs-comment">// 单机模式下启动 MiniZooKeeperCluster 作为 Zookeeper 服务，该类中的许多代码都是从 Zookeeper 的测试代码中剥离出来的</span>            <span class="hljs-keyword">final</span> MiniZooKeeperCluster zooKeeperCluster = <span class="hljs-keyword">new</span> MiniZooKeeperCluster(conf);            <span class="hljs-comment">// 从配置文件获取 hbase.zookeeper.property.dataDir 配置的参数作为 Zookeeper 数据目录</span>            File zkDataPath = <span class="hljs-keyword">new</span> File(conf.get(HConstants.ZOOKEEPER_DATA_DIR));                <span class="hljs-comment">// find out the default client port</span>            <span class="hljs-keyword">int</span> zkClientPort = <span class="hljs-number">0</span>;                <span class="hljs-comment">// 从 hbase.zookeeper.quorum 参数解析并获取 Zookeeper 配置的端口号</span>            String zkserver = conf.get(HConstants.ZOOKEEPER_QUORUM);            <span class="hljs-keyword">if</span> (zkserver != <span class="hljs-keyword">null</span>) &#123;              String[] zkservers = zkserver.split(<span class="hljs-string">&quot;,&quot;</span>);              <span class="hljs-comment">// 单机模式仅支持一个 Zookeeper 服务</span>              <span class="hljs-keyword">if</span> (zkservers.length &gt; <span class="hljs-number">1</span>) &#123;                <span class="hljs-comment">// In local mode deployment, we have the master + a region server and zookeeper server</span>                <span class="hljs-comment">// started in the same process. Therefore, we only support one zookeeper server.</span>                String errorMsg = <span class="hljs-string">&quot;Could not start ZK with &quot;</span> + zkservers.length +                    <span class="hljs-string">&quot; ZK servers in local mode deployment. Aborting as clients (e.g. shell) will not &quot;</span>                    + <span class="hljs-string">&quot;be able to find this ZK quorum.&quot;</span>;                  System.err.println(errorMsg);                  <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IOException(errorMsg);              &#125;                  String[] parts = zkservers[<span class="hljs-number">0</span>].split(<span class="hljs-string">&quot;:&quot;</span>);                  <span class="hljs-keyword">if</span> (parts.length == <span class="hljs-number">2</span>) &#123;                <span class="hljs-comment">// the second part is the client port</span>                zkClientPort = Integer.parseInt(parts [<span class="hljs-number">1</span>]);              &#125;            &#125;            <span class="hljs-comment">// If the client port could not be find in server quorum conf, try another conf</span>            <span class="hljs-keyword">if</span> (zkClientPort == <span class="hljs-number">0</span>) &#123;              zkClientPort = conf.getInt(HConstants.ZOOKEEPER_CLIENT_PORT, <span class="hljs-number">0</span>);              <span class="hljs-comment">// The client port has to be set by now; if not, throw exception.</span>              <span class="hljs-keyword">if</span> (zkClientPort == <span class="hljs-number">0</span>) &#123;                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IOException(<span class="hljs-string">&quot;No config value for &quot;</span> + HConstants.ZOOKEEPER_CLIENT_PORT);              &#125;            &#125;            zooKeeperCluster.setDefaultClientPort(zkClientPort);            <span class="hljs-comment">// set the ZK tick time if specified</span>            <span class="hljs-keyword">int</span> zkTickTime = conf.getInt(HConstants.ZOOKEEPER_TICK_TIME, <span class="hljs-number">0</span>);            <span class="hljs-keyword">if</span> (zkTickTime &gt; <span class="hljs-number">0</span>) &#123;              zooKeeperCluster.setTickTime(zkTickTime);            &#125;                <span class="hljs-comment">// 如果启用了安全认证，需要配置 Zookeeper 的 keytab 文件和 principal 等</span>            <span class="hljs-comment">// login the zookeeper server principal (if using security)</span>            ZKUtil.loginServer(conf, HConstants.ZK_SERVER_KEYTAB_FILE,              HConstants.ZK_SERVER_KERBEROS_PRINCIPAL, <span class="hljs-keyword">null</span>);            <span class="hljs-keyword">int</span> localZKClusterSessionTimeout =              conf.getInt(HConstants.ZK_SESSION_TIMEOUT + <span class="hljs-string">&quot;.localHBaseCluster&quot;</span>, <span class="hljs-number">10</span>*<span class="hljs-number">1000</span>);            conf.setInt(HConstants.ZK_SESSION_TIMEOUT, localZKClusterSessionTimeout);            LOG.info(<span class="hljs-string">&quot;Starting a zookeeper cluster&quot;</span>);                        <span class="hljs-comment">// 启动 Zookeeper 服务</span>            <span class="hljs-keyword">int</span> clientPort = zooKeeperCluster.startup(zkDataPath);            <span class="hljs-comment">// Zookeeper 启动失败会输出相应信息</span>            <span class="hljs-keyword">if</span> (clientPort != zkClientPort) &#123;              String errorMsg = <span class="hljs-string">&quot;Could not start ZK at requested port of &quot;</span> +                zkClientPort + <span class="hljs-string">&quot;.  ZK was started at port: &quot;</span> + clientPort +                <span class="hljs-string">&quot;.  Aborting as clients (e.g. shell) will not be able to find &quot;</span> +                <span class="hljs-string">&quot;this ZK quorum.&quot;</span>;              System.err.println(errorMsg);              <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IOException(errorMsg);            &#125;            <span class="hljs-comment">// 启动成功则设置 HBase 有关 Zookeeper 的参数</span>            conf.set(HConstants.ZOOKEEPER_CLIENT_PORT, Integer.toString(clientPort));                <span class="hljs-comment">// Need to have the zk cluster shutdown when master is shutdown.</span>            <span class="hljs-comment">// Run a subclass that does the zk cluster shutdown on its way out.</span>            <span class="hljs-keyword">int</span> mastersCount = conf.getInt(<span class="hljs-string">&quot;hbase.masters&quot;</span>, <span class="hljs-number">1</span>);            <span class="hljs-keyword">int</span> regionServersCount = conf.getInt(<span class="hljs-string">&quot;hbase.regionservers&quot;</span>, <span class="hljs-number">1</span>);            <span class="hljs-comment">// Set start timeout to 5 minutes for cmd line start operations</span>            conf.setIfUnset(<span class="hljs-string">&quot;hbase.master.start.timeout.localHBaseCluster&quot;</span>, <span class="hljs-string">&quot;300000&quot;</span>);            LOG.info(<span class="hljs-string">&quot;Starting up instance of localHBaseCluster; master=&quot;</span> + mastersCount +              <span class="hljs-string">&quot;, regionserversCount=&quot;</span> + regionServersCount);                        <span class="hljs-comment">// LocalHMaster 继承自 HMaster，和 HRegionServer 同时启动，在停止的同时也停止 Zookeeper 服务</span>            LocalHBaseCluster cluster = <span class="hljs-keyword">new</span> LocalHBaseCluster(conf, mastersCount, regionServersCount,              LocalHMaster.class, HRegionServer.class);                        <span class="hljs-comment">// 将运行的 zooKeeperCluster 置于 LocalHMaster 中，以便在 LocalHMaster 停止的时候停止 Zookeeper 服务</span>            ((LocalHMaster)cluster.getMaster(<span class="hljs-number">0</span>)).setZKCluster(zooKeeperCluster);            <span class="hljs-comment">// 调用 LocalHBaseCluster 的 startup 方法启动</span>            cluster.startup();            waitOnMasterThreads(cluster);          &#125; <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">// 启动集群模式</span>            <span class="hljs-comment">// 记录有关当前正在运行的JVM进程的信息，包括环境变量，可以通过配置 hbase.envvars.logging.disabled 为 true 禁用</span>            logProcessInfo(getConf());            <span class="hljs-comment">// 通过反射 HMaster 的构造方法对其进行实例化</span>            HMaster master = HMaster.constructMaster(masterClass, conf);            <span class="hljs-comment">// 如果此时请求关闭 HMaster 则不会启动</span>            <span class="hljs-keyword">if</span> (master.isStopped()) &#123;              LOG.info(<span class="hljs-string">&quot;Won&#x27;t bring the Master up as a shutdown is requested&quot;</span>);              <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;            &#125;            <span class="hljs-comment">// 启动 HMaster，调用 HMaster 的 run 方法进行处理</span>            master.start();            <span class="hljs-comment">// 等待 HMaster 启动成功</span>            master.join();            <span class="hljs-comment">// 异常信息则输出错误信息</span>            <span class="hljs-keyword">if</span>(master.isAborted())              <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> RuntimeException(<span class="hljs-string">&quot;HMaster Aborted&quot;</span>);          &#125;        &#125; <span class="hljs-keyword">catch</span> (Throwable t) &#123;          LOG.error(<span class="hljs-string">&quot;Master exiting&quot;</span>, t);          <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;        &#125;        <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;      &#125;          <span class="hljs-comment">// 由 HMasterCommandLine 在 startMaster 方法中启动单机模式的 HBase 时调用  </span>    <span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">waitOnMasterThreads</span><span class="hljs-params">(LocalHBaseCluster cluster)</span> <span class="hljs-keyword">throws</span> InterruptedException</span>&#123;        List&lt;JVMClusterUtil.MasterThread&gt; masters = cluster.getMasters();        List&lt;JVMClusterUtil.RegionServerThread&gt; regionservers = cluster.getRegionServers();            <span class="hljs-keyword">if</span> (masters != <span class="hljs-keyword">null</span>) &#123;          <span class="hljs-keyword">for</span> (JVMClusterUtil.MasterThread t : masters) &#123;            <span class="hljs-comment">// 先等待 MasterThread 启动完成再启动 RegionServerThread，如果出现异常则关闭 RegionServer 并输出错误信息</span>            t.join();            <span class="hljs-keyword">if</span>(t.getMaster().isAborted()) &#123;              closeAllRegionServerThreads(regionservers);              <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> RuntimeException(<span class="hljs-string">&quot;HMaster Aborted&quot;</span>);            &#125;          &#125;        &#125;      &#125;&#125;---<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">LocalHBaseCluster</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">LocalHBaseCluster</span><span class="hljs-params">(<span class="hljs-keyword">final</span> Configuration conf, <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> noMasters,</span></span><span class="hljs-function"><span class="hljs-params">        <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> noRegionServers, <span class="hljs-keyword">final</span> Class&lt;? extends HMaster&gt; masterClass,</span></span><span class="hljs-function"><span class="hljs-params">        <span class="hljs-keyword">final</span> Class&lt;? extends HRegionServer&gt; regionServerClass)</span></span><span class="hljs-function">      <span class="hljs-keyword">throws</span> IOException </span>&#123;        <span class="hljs-keyword">this</span>.conf = conf;            <span class="hljs-comment">// 获取及配置 HBase 相关参数</span>        ……                <span class="hljs-comment">// 设置 masterClass，此处即为传入的 LocalHMaster</span>        <span class="hljs-keyword">this</span>.masterClass = (Class&lt;? extends HMaster&gt;)          conf.getClass(HConstants.MASTER_IMPL, masterClass);                  <span class="hljs-comment">// 最终调用至 JVMClusterUtil 工具类的 createMasterThread 方法，通过反射调用继承自 HMaster 的子类构造方法进行实例化，得到 MasterThread</span>        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; noMasters; i++) &#123;          addMaster(<span class="hljs-keyword">new</span> Configuration(conf), i);        &#125;                <span class="hljs-comment">// 设置 regionServerClass，此处即为传入的 HRegionServer</span>        <span class="hljs-keyword">this</span>.regionServerClass =          (Class&lt;? extends HRegionServer&gt;)conf.getClass(HConstants.REGION_SERVER_IMPL,           regionServerClass);            <span class="hljs-comment">// 最终调用至 JVMClusterUtil 工具类的 createRegionServerThread 方法，通过反射调用继承自 HRegionServer 的子类构造方法进行实例化，得到 RegionServerThread</span>        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; noRegionServers; i++) &#123;          addRegionServer(<span class="hljs-keyword">new</span> Configuration(conf), i);        &#125;      &#125;          <span class="hljs-comment">// 启动前面实例化好的 masterThreads 和 regionThreads，等待启动完成，至此单机模式下的 HMaster 和 HRegionServer 均已启动，可以正常提供服务</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">startup</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> IOException </span>&#123;        JVMClusterUtil.startup(<span class="hljs-keyword">this</span>.masterThreads, <span class="hljs-keyword">this</span>.regionThreads);    &#125;&#125;---<span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">LocalHMaster</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">HMaster</span> </span>&#123;    <span class="hljs-keyword">private</span> MiniZooKeeperCluster zkcluster = <span class="hljs-keyword">null</span>;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">LocalHMaster</span><span class="hljs-params">(Configuration conf)</span></span><span class="hljs-function">    <span class="hljs-keyword">throws</span> IOException, KeeperException, InterruptedException </span>&#123;      <span class="hljs-keyword">super</span>(conf);    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">run</span><span class="hljs-params">()</span> </span>&#123;      <span class="hljs-comment">// 调用父类 HMaster 的 run 方法</span>      <span class="hljs-keyword">super</span>.run();      <span class="hljs-comment">// 调用 MiniZooKeeperCluster 的 shutdown 方法，停止单机模式下的 Zookeeper 服务</span>      <span class="hljs-keyword">if</span> (<span class="hljs-keyword">this</span>.zkcluster != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">try</span> &#123;          <span class="hljs-keyword">this</span>.zkcluster.shutdown();        &#125; <span class="hljs-keyword">catch</span> (IOException e) &#123;          e.printStackTrace();        &#125;      &#125;    &#125;    <span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">setZKCluster</span><span class="hljs-params">(<span class="hljs-keyword">final</span> MiniZooKeeperCluster zkcluster)</span> </span>&#123;      <span class="hljs-keyword">this</span>.zkcluster = zkcluster;    &#125;  &#125;</code></pre><h3 id="hbase-config-sh"><a href="#hbase-config-sh" class="headerlink" title="hbase-config.sh"></a>hbase-config.sh</h3><p>用于获取配置参数的脚本，会去加载 hbase-env.sh 中设置的环境变量。</p><pre><code class="hljs shell"><span class="hljs-meta">#</span><span class="bash"> 获取当前路径</span>this=&quot;$&#123;BASH_SOURCE-$0&#125;&quot;<span class="hljs-meta">#</span><span class="bash"> 解析 <span class="hljs-variable">$&#123;BASH_SOURCE-$0&#125;</span> 有可能是 softlink 的问题</span>while [ -h &quot;$this&quot; ]; do  ls=`ls -ld &quot;$this&quot;`  link=`expr &quot;$ls&quot; : &#x27;.*-&gt; \(.*\)$&#x27;`  if expr &quot;$link&quot; : &#x27;.*/.*&#x27; &gt; /dev/null; then    this=&quot;$link&quot;  else    this=`dirname &quot;$this&quot;`/&quot;$link&quot;  fidone<span class="hljs-meta">#</span><span class="bash"> convert relative path to absolute path</span>bin=`dirname &quot;$this&quot;`script=`basename &quot;$this&quot;`bin=`cd &quot;$bin&quot;&gt;/dev/null; pwd`this=&quot;$bin/$script&quot;<span class="hljs-meta">#</span><span class="bash"> 将 HBASE_HOME 设置为 HBase 安装的根目录</span>if [ -z &quot;$HBASE_HOME&quot; ]; then  export HBASE_HOME=`dirname &quot;$this&quot;`/..fi<span class="hljs-meta">#</span><span class="bash"> 检查是否有可选参数传入，接受到则进行相应的参数设置</span>……<span class="hljs-meta">#</span><span class="bash"> 以下项为参数设置</span><span class="hljs-meta">#</span><span class="bash"> Allow alternate hbase conf dir location.</span>HBASE_CONF_DIR=&quot;$&#123;HBASE_CONF_DIR:-$HBASE_HOME/conf&#125;&quot;<span class="hljs-meta">#</span><span class="bash"> List of hbase regions servers.</span>HBASE_REGIONSERVERS=&quot;$&#123;HBASE_REGIONSERVERS:-$HBASE_CONF_DIR/regionservers&#125;&quot;<span class="hljs-meta">#</span><span class="bash"> List of hbase secondary masters.</span>HBASE_BACKUP_MASTERS=&quot;$&#123;HBASE_BACKUP_MASTERS:-$HBASE_CONF_DIR/backup-masters&#125;&quot;if [ -n &quot;$HBASE_JMX_BASE&quot; ] &amp;&amp; [ -z &quot;$HBASE_JMX_OPTS&quot; ]; then  HBASE_JMX_OPTS=&quot;$HBASE_JMX_BASE&quot;fi……<span class="hljs-meta">#</span><span class="bash"> 加载 hbase-env.sh</span>if [ -z &quot;$HBASE_ENV_INIT&quot; ] &amp;&amp; [ -f &quot;$&#123;HBASE_CONF_DIR&#125;/hbase-env.sh&quot; ]; then  . &quot;$&#123;HBASE_CONF_DIR&#125;/hbase-env.sh&quot;  export HBASE_ENV_INIT=&quot;true&quot;fi<span class="hljs-meta">#</span><span class="bash"> 检测 HBASE_REGIONSERVER_MLOCK 是否设置为 <span class="hljs-literal">true</span>，主要是判断系统是否使用了 mlock 来锁住内存，防止这段内存被操作系统放到 swap 空间，即使该程序已经有一段时间没有访问这段空间</span>if [ &quot;$HBASE_REGIONSERVER_MLOCK&quot; = &quot;true&quot; ]; then  MLOCK_AGENT=&quot;$HBASE_HOME/lib/native/libmlockall_agent.so&quot;  if [ ! -f &quot;$MLOCK_AGENT&quot; ]; then    cat 1&gt;&amp;2 &lt;&lt;EOFUnable to find mlockall_agent, hbase must be compiled with -PnativeEOF    exit 1  fi  // 配置 HBASE_REGIONSERVER_UID  if [ -z &quot;$HBASE_REGIONSERVER_UID&quot; ] || [ &quot;$HBASE_REGIONSERVER_UID&quot; == &quot;$USER&quot; ]; then      HBASE_REGIONSERVER_OPTS=&quot;$HBASE_REGIONSERVER_OPTS -agentpath:$MLOCK_AGENT&quot;  else      HBASE_REGIONSERVER_OPTS=&quot;$HBASE_REGIONSERVER_OPTS -agentpath:$MLOCK_AGENT=user=$HBASE_REGIONSERVER_UID&quot;  fifi……<span class="hljs-meta">#</span><span class="bash"> 检查是否配置了 jdk，HBase-2.2.4 至少需要 1.8 以上的 JDK 版本，未配置则退出</span><span class="hljs-meta">#</span><span class="bash"> Now having JAVA_HOME defined is required </span>if [ -z &quot;$JAVA_HOME&quot; ]; then    cat 1&gt;&amp;2 &lt;&lt;EOF……fi</code></pre><h3 id="hbase-daemons-sh"><a href="#hbase-daemons-sh" class="headerlink" title="hbase-daemons.sh"></a>hbase-daemons.sh</h3><p>根据要启动的进程，生成好远程执行命令 remote_cmd 并调用其他脚本执行。</p><pre><code class="hljs shell"><span class="hljs-meta">#</span><span class="bash"> 脚本用法</span>usage=&quot;Usage: hbase-daemons.sh [--config &lt;hbase-confdir&gt;] [--autostart-window-size &lt;window size in hours&gt;]\      [--autostart-window-retry-limit &lt;retry count limit for autostart&gt;] \      [--hosts regionserversfile] [autostart|autorestart|restart|start|stop] command args...&quot;<span class="hljs-meta">#</span><span class="bash"> 如果没有指定参数，输出 usage</span>if [ $# -le 1 ]; then  echo $usage  exit 1fi<span class="hljs-meta">#</span><span class="bash"> 获取当前路径</span>bin=`dirname &quot;$&#123;BASH_SOURCE-$0&#125;&quot;`bin=`cd &quot;$bin&quot;&gt;/dev/null; pwd`<span class="hljs-meta">#</span><span class="bash"> 默认的自动启动参数相关配置，一般前面脚本都是传递诸如 start 参数过来，可以忽略</span>AUTOSTART_WINDOW_SIZE=0AUTOSTART_WINDOW_RETRY_LIMIT=0<span class="hljs-meta">#</span><span class="bash"> 加载 hbase-config.sh 脚本</span>. $bin/hbase-config.sh……<span class="hljs-meta">#</span><span class="bash"> 调用 hbase-daemon.sh 并向其传递参数</span>remote_cmd=&quot;$bin/hbase-daemon.sh --config $&#123;HBASE_CONF_DIR&#125; $&#123;autostart_args&#125; $@&quot;<span class="hljs-meta">#</span><span class="bash"> 将 <span class="hljs-variable">$remote_cmd</span> 作为参数继续包装到 args 中</span>args=&quot;--hosts $&#123;HBASE_REGIONSERVERS&#125; --config $&#123;HBASE_CONF_DIR&#125; $remote_cmd&quot;<span class="hljs-meta">#</span><span class="bash"> 接受到的第二个参数的值，例如 start-hbase.sh 集群模式下传递了 zookeeper、regionserver 等，基于该参数分别调用相应的脚本执行，执行后退出</span>command=$2case $command in  (zookeeper)    exec &quot;$bin/zookeepers.sh&quot; $args    ;;  (master-backup)    exec &quot;$bin/master-backup.sh&quot; $args    ;;  (*)    exec &quot;$bin/regionservers.sh&quot; $args    ;;esac</code></pre><h3 id="hbase-daemon-sh"><a href="#hbase-daemon-sh" class="headerlink" title="hbase-daemon.sh"></a>hbase-daemon.sh</h3><p>这个脚本很重要，前面的脚本都是做一些准备工作，它负责启动前的检查清理、日志滚动以及进程的启动等等。支持 7 种方式：start、autostart、autorestart、foreground_start、internal_autostart、stop、restart，其他参数则输出操作用法。</p><pre><code class="hljs shell"><span class="hljs-meta">#</span><span class="bash"> 将 Hadoop hbase 命令作为守护程序执行</span><span class="hljs-meta">#</span><span class="bash"> 环境变量</span><span class="hljs-meta">#</span><span class="bash"> 配置文件目录，默认是 <span class="hljs-variable">$&#123;HBASE_HOME&#125;</span>/conf</span><span class="hljs-meta">#</span><span class="bash"> HBASE_CONF_DIR</span><span class="hljs-meta">#</span><span class="bash"> 日志存储目录，默认情况下为 <span class="hljs-built_in">pwd</span></span><span class="hljs-meta">#</span><span class="bash"> HBASE_LOG_DIR</span><span class="hljs-meta">#</span><span class="bash"> 进程号存放目录，默认是 /tmp</span><span class="hljs-meta">#</span><span class="bash"> HBASE_PID_DIR</span><span class="hljs-meta">#</span><span class="bash"> 代表当前 hadoop 实例的字符串，默认是当前点用户 <span class="hljs-variable">$USER</span></span><span class="hljs-meta">#</span><span class="bash"> HBASE_IDENT_STRING</span><span class="hljs-meta">#</span><span class="bash"> 守护程序的调度优先级，默认是 0</span><span class="hljs-meta">#</span><span class="bash"> HBASE_NICENESS</span><span class="hljs-meta">#</span><span class="bash"> 在停止服务此时间之后，服务还未停止，将对其执行 <span class="hljs-built_in">kill</span> -9 命令，默认 1200（单位是 s）</span><span class="hljs-meta">#</span><span class="bash"> HBASE_STOP_TIMEOUT</span><span class="hljs-meta">#</span><span class="bash"> 仿照了 <span class="hljs-variable">$HADOOP_HOME</span>/bin/hadoop-daemon.sh</span>usage=&quot;Usage: hbase-daemon.sh [--config &lt;conf-dir&gt;]\ [--autostart-window-size &lt;window size in hours&gt;]\ [--autostart-window-retry-limit &lt;retry count limit for autostart&gt;]\ (start|stop|restart|autostart|autorestart|foreground_start) &lt;hbase-command&gt; \ &lt;args...&gt;&quot;<span class="hljs-meta">#</span><span class="bash"> 如果没有指定参数，输出 usage</span>if [ $# -le 1 ]; then  echo $usage  exit 1fi<span class="hljs-meta">#</span><span class="bash"> 默认的自动启动配置参数</span>AUTOSTART_WINDOW_SIZE=0AUTOSTART_WINDOW_RETRY_LIMIT=0<span class="hljs-meta">#</span><span class="bash"> 获取当前路径</span>bin=`dirname &quot;$&#123;BASH_SOURCE-$0&#125;&quot;`bin=`cd &quot;$bin&quot;&gt;/dev/null; pwd`<span class="hljs-meta">#</span><span class="bash"> 加载 hbase-config.sh 以及 hbase-common.sh</span>. &quot;$bin&quot;/hbase-config.sh. &quot;$bin&quot;/hbase-common.sh<span class="hljs-meta">#</span><span class="bash"> 获取传递的参数，即 start 或 stop</span>startStop=$1<span class="hljs-meta">#</span><span class="bash"> 命令左移，<span class="hljs-built_in">shift</span> 命令每执行一次，变量的个数(<span class="hljs-variable">$#</span>)减一，而变量值提前一位</span>shiftcommand=$1shift<span class="hljs-meta">#</span><span class="bash"> 日志滚动</span>hbase_rotate_log ()&#123;    log=$1;    num=5;    if [ -n &quot;$2&quot; ]; then    num=$2    fi    # 检查是否存在日志文件，若存在则进行日志滚动    if [ -f &quot;$log&quot; ]; then # rotate logs    while [ $num -gt 1 ]; do        prev=`expr $num - 1`        [ -f &quot;$log.$prev&quot; ] &amp;&amp; mv -f &quot;$log.$prev&quot; &quot;$log.$num&quot;        num=$prev    done    # 修改日志文件名    mv -f &quot;$log&quot; &quot;$log.$num&quot;;    fi&#125;<span class="hljs-meta">#</span><span class="bash"> 当运行遇到问题时进行清理，在 foreground_start 方式中，接收到 SIGHUP（终端线路挂断） SIGINT（中断进程） SIGTERM（软件终止信号） EXIT（退出）等信号时，</span><span class="hljs-meta">#</span><span class="bash"> 使用 <span class="hljs-built_in">trap</span> 命令对要处理的信号名采取相应的行动，即 <span class="hljs-built_in">kill</span> 掉正在运行的进程，并通知 Zookeeper 删除节点</span>cleanAfterRun() &#123;  if [ -f $&#123;HBASE_PID&#125; ]; then    # If the process is still running time to tear it down.    kill -9 `cat $&#123;HBASE_PID&#125;` &gt; /dev/null 2&gt;&amp;1    rm -f $&#123;HBASE_PID&#125; &gt; /dev/null 2&gt;&amp;1  fi  if [ -f $&#123;HBASE_ZNODE_FILE&#125; ]; then    if [ &quot;$command&quot; = &quot;master&quot; ]; then      HBASE_OPTS=&quot;$HBASE_OPTS $HBASE_MASTER_OPTS&quot; $bin/hbase master clear &gt; /dev/null 2&gt;&amp;1    else      # call ZK to delete the node      ZNODE=`cat $&#123;HBASE_ZNODE_FILE&#125;`      HBASE_OPTS=&quot;$HBASE_OPTS $HBASE_REGIONSERVER_OPTS&quot; $bin/hbase zkcli delete $&#123;ZNODE&#125; &gt; /dev/null 2&gt;&amp;1    fi    rm $&#123;HBASE_ZNODE_FILE&#125;  fi&#125;<span class="hljs-meta">#</span><span class="bash"> 启动之间先检查进程是否存在，存在则输出警告信息</span>check_before_start()&#123;    #ckeck if the process is not running    mkdir -p &quot;$HBASE_PID_DIR&quot;    if [ -f $HBASE_PID ]; then      # kill -0 pid 不发送任何信号，但是系统会进行错误检查，检查一个进程是否存在，存在返回 0；不存在返回 1      if kill -0 `cat $HBASE_PID` &gt; /dev/null 2&gt;&amp;1; then        echo $command running as process `cat $HBASE_PID`.  Stop it first.        exit 1      fi    fi&#125;<span class="hljs-meta">#</span><span class="bash"> 等待命令执行完成，超过 HBASE_SLAVE_TIMEOUT 即 300 之后则调用 <span class="hljs-built_in">kill</span> -9 杀掉服务并输出警告信息</span>wait_until_done ()&#123;    p=$1    cnt=$&#123;HBASE_SLAVE_TIMEOUT:-300&#125;    origcnt=$cnt    # 进程仍在运行，睡眠 1s 后重新判断，直到超过指定次数（时间）调用 kill -9 $pid    while kill -0 $p &gt; /dev/null 2&gt;&amp;1; do      if [ $cnt -gt 1 ]; then        cnt=`expr $cnt - 1`        sleep 1      else        echo &quot;Process did not complete after $origcnt seconds, killing.&quot;        kill -9 $p        exit 1      fi    done    return 0&#125;<span class="hljs-meta">#</span><span class="bash"> 获取日志目录</span>if [ &quot;$HBASE_LOG_DIR&quot; = &quot;&quot; ]; then  export HBASE_LOG_DIR=&quot;$HBASE_HOME/logs&quot;fimkdir -p &quot;$HBASE_LOG_DIR&quot;<span class="hljs-meta">#</span><span class="bash"> 如果没有配置 HBASE_PID_DIR 目录，则默认为 /tmp</span>if [ &quot;$HBASE_PID_DIR&quot; = &quot;&quot; ]; then  HBASE_PID_DIR=/tmpfiif [ &quot;$HBASE_IDENT_STRING&quot; = &quot;&quot; ]; then  export HBASE_IDENT_STRING=&quot;$USER&quot;fi<span class="hljs-meta">#</span><span class="bash"> 配置 JAVA_HOME</span>if [ &quot;$JAVA_HOME&quot; != &quot;&quot; ]; then<span class="hljs-meta">  #</span><span class="bash"><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;run java in <span class="hljs-variable">$JAVA_HOME</span>&quot;</span></span>  JAVA_HOME=$JAVA_HOMEfiif [ &quot;$JAVA_HOME&quot; = &quot;&quot; ]; then  echo &quot;Error: JAVA_HOME is not set.&quot;  exit 1fiJAVA=$JAVA_HOME/bin/java<span class="hljs-meta">#</span><span class="bash"> 日志前缀，如：hbase-root-master-node1</span>export HBASE_LOG_PREFIX=hbase-$HBASE_IDENT_STRING-$command-$HOSTNAMEexport HBASE_LOGFILE=$HBASE_LOG_PREFIX.log<span class="hljs-meta">#</span><span class="bash"> 如果未配置 HBASE_ROOT_LOGGER 参数，则设置默认的日志级别</span>if [ -z &quot;$&#123;HBASE_ROOT_LOGGER&#125;&quot; ]; thenexport HBASE_ROOT_LOGGER=$&#123;HBASE_ROOT_LOGGER:-&quot;INFO,RFA&quot;&#125;fi<span class="hljs-meta">#</span><span class="bash"> 如果未配置 HBASE_SECURITY_LOGGER 参数，则设置默认的安全日志级别</span>if [ -z &quot;$&#123;HBASE_SECURITY_LOGGER&#125;&quot; ]; thenexport HBASE_SECURITY_LOGGER=$&#123;HBASE_SECURITY_LOGGER:-&quot;INFO,RFAS&quot;&#125;fi<span class="hljs-meta">#</span><span class="bash"> out 日志，即 System.out 输出信息</span>HBASE_LOGOUT=$&#123;HBASE_LOGOUT:-&quot;$HBASE_LOG_DIR/$HBASE_LOG_PREFIX.out&quot;&#125;HBASE_LOGGC=$&#123;HBASE_LOGGC:-&quot;$HBASE_LOG_DIR/$HBASE_LOG_PREFIX.gc&quot;&#125;HBASE_LOGLOG=$&#123;HBASE_LOGLOG:-&quot;$&#123;HBASE_LOG_DIR&#125;/$&#123;HBASE_LOGFILE&#125;&quot;&#125;<span class="hljs-meta">#</span><span class="bash"> HBase 相关服务进程</span>HBASE_PID=$HBASE_PID_DIR/hbase-$HBASE_IDENT_STRING-$command.pid<span class="hljs-meta">#</span><span class="bash"> HBase 的 znode 文件</span>export HBASE_ZNODE_FILE=$HBASE_PID_DIR/hbase-$HBASE_IDENT_STRING-$command.znode<span class="hljs-meta">#</span><span class="bash"> HBase 的 autostart 文件</span>export HBASE_AUTOSTART_FILE=$HBASE_PID_DIR/hbase-$HBASE_IDENT_STRING-$command.autostart<span class="hljs-meta">#</span><span class="bash"> 如果配置了 SERVER_GC_OPTS、CLIENT_GC_OPTS 参数，则设置对应变量</span>if [ -n &quot;$SERVER_GC_OPTS&quot; ]; then  export SERVER_GC_OPTS=$&#123;SERVER_GC_OPTS/&quot;-Xloggc:&lt;FILE-PATH&gt;&quot;/&quot;-Xloggc:$&#123;HBASE_LOGGC&#125;&quot;&#125;fiif [ -n &quot;$CLIENT_GC_OPTS&quot; ]; then  export CLIENT_GC_OPTS=$&#123;CLIENT_GC_OPTS/&quot;-Xloggc:&lt;FILE-PATH&gt;&quot;/&quot;-Xloggc:$&#123;HBASE_LOGGC&#125;&quot;&#125;fi<span class="hljs-meta">#</span><span class="bash"> 设置默认的程度调度优先级为 0</span>if [ &quot;$HBASE_NICENESS&quot; = &quot;&quot; ]; then    export HBASE_NICENESS=0fi<span class="hljs-meta">#</span><span class="bash"> 获取当前路径</span>thiscmd=&quot;$bin/$(basename $&#123;BASH_SOURCE-$0&#125;)&quot;args=$@case $startStop in(start)    check_before_start    hbase_rotate_log $HBASE_LOGOUT    hbase_rotate_log $HBASE_LOGGC    # 输出启动的程序，以及日志输出目录，接着调用 foreground_start     echo running $command, logging to $HBASE_LOGOUT    $thiscmd --config &quot;$&#123;HBASE_CONF_DIR&#125;&quot; \        foreground_start $command $args &lt; /dev/null &gt; $&#123;HBASE_LOGOUT&#125; 2&gt;&amp;1  &amp;    # 使正在运行的作业忽略 HUP 信号，避免当用户注销（logout）或者网络断开时，终端会收到 Linux HUP（hangup）信号从而关闭其所有子进程    disown -h -r    sleep 1; head &quot;$&#123;HBASE_LOGOUT&#125;&quot;  ;;(autostart)    check_before_start    hbase_rotate_log $HBASE_LOGOUT    hbase_rotate_log $HBASE_LOGGC    echo running $command, logging to $HBASE_LOGOUT    # 使用 nohup 挂起并执行自动启动程序，调用 internal_autostart 继续执行    nohup $thiscmd --config &quot;$&#123;HBASE_CONF_DIR&#125;&quot; --autostart-window-size $&#123;AUTOSTART_WINDOW_SIZE&#125; --autostart-window-retry-limit $&#123;AUTOSTART_WINDOW_RETRY_LIMIT&#125; \        internal_autostart $command $args &lt; /dev/null &gt; $&#123;HBASE_LOGOUT&#125; 2&gt;&amp;1  &amp;  ;;(autorestart)    echo running $command, logging to $HBASE_LOGOUT    # 先停止当前服务，并等待所有进程都停止    $thiscmd --config &quot;$&#123;HBASE_CONF_DIR&#125;&quot; stop $command $args &amp;    wait_until_done $!    # 等待用户指定的睡眠周期    sp=$&#123;HBASE_RESTART_SLEEP:-3&#125;    if [ $sp -gt 0 ]; then      sleep $sp    fi    check_before_start    hbase_rotate_log $HBASE_LOGOUT    # 使用 nohup 挂起并执行自动启动程序，调用 internal_autostart 继续执行    nohup $thiscmd --config &quot;$&#123;HBASE_CONF_DIR&#125;&quot; --autostart-window-size $&#123;AUTOSTART_WINDOW_SIZE&#125; --autostart-window-retry-limit $&#123;AUTOSTART_WINDOW_RETRY_LIMIT&#125; \        internal_autostart $command $args &lt; /dev/null &gt; $&#123;HBASE_LOGOUT&#125; 2&gt;&amp;1  &amp;  ;;(foreground_start)    trap cleanAfterRun SIGHUP SIGINT SIGTERM EXIT    # 日志不重定向参数，一般都输出到日志中，这部分逻辑主要走 else    if [ &quot;$HBASE_NO_REDIRECT_LOG&quot; != &quot;&quot; ]; then        # NO REDIRECT        echo &quot;`date` Starting $command on `hostname`&quot;        echo &quot;`ulimit -a`&quot;        # in case the parent shell gets the kill make sure to trap signals.        # Only one will get called. Either the trap or the flow will go through.        nice -n $HBASE_NICENESS &quot;$HBASE_HOME&quot;/bin/hbase \            --config &quot;$&#123;HBASE_CONF_DIR&#125;&quot; \            $command &quot;$@&quot; start &amp;    else        echo &quot;`date` Starting $command on `hostname`&quot; &gt;&gt; $&#123;HBASE_LOGLOG&#125;        echo &quot;`ulimit -a`&quot; &gt;&gt; &quot;$HBASE_LOGLOG&quot; 2&gt;&amp;1        # nice 以更改过的优先序来执行程序，调用 $HBASE_HOME/bin/hbase 传递参数继续执行        nice -n $HBASE_NICENESS &quot;$HBASE_HOME&quot;/bin/hbase \            --config &quot;$&#123;HBASE_CONF_DIR&#125;&quot; \            $command &quot;$@&quot; start &gt;&gt; $&#123;HBASE_LOGOUT&#125; 2&gt;&amp;1 &amp;    fi    # 获取最后一个进程号，将其覆盖写入 $HBASE_PID 文件中，暂停当前进程并释放资源等待前面的线程执行    hbase_pid=$!    echo $hbase_pid &gt; $&#123;HBASE_PID&#125;    wait $hbase_pid  ;;(internal_autostart)    ONE_HOUR_IN_SECS=3600    # 自动启动的开始日期    autostartWindowStartDate=`date +%s`    autostartCount=0    # 创建自动启动的文件    touch &quot;$HBASE_AUTOSTART_FILE&quot;    # 除非被要求停止，否则一直保持启动命令的状态，在崩溃时重新进入循环    while true    do      hbase_rotate_log $HBASE_LOGGC      if [ -f $HBASE_PID ] &amp;&amp;  kill -0 &quot;$(cat &quot;$HBASE_PID&quot;)&quot; &gt; /dev/null 2&gt;&amp;1 ; then        wait &quot;$(cat &quot;$HBASE_PID&quot;)&quot;      else        # 如果 $HBASE_AUTOSTART_FILE 不存在，说明服务可能不是通过 stop 命令停止的        if [ ! -f &quot;$HBASE_AUTOSTART_FILE&quot; ]; then          echo &quot;`date` HBase might be stopped removing the autostart file. Exiting Autostart process&quot; &gt;&gt; $&#123;HBASE_LOGOUT&#125;          exit 1        fi        echo &quot;`date` Autostarting hbase $command service. Attempt no: $(( $autostartCount + 1))&quot; &gt;&gt; $&#123;HBASE_LOGLOG&#125;        touch &quot;$HBASE_AUTOSTART_FILE&quot;        $thiscmd --config &quot;$&#123;HBASE_CONF_DIR&#125;&quot; foreground_start $command $args        autostartCount=$(( $autostartCount + 1 ))        # HBASE-6504 - 仅当输出详细gc时，才采用输出的第一行        distMode=`$bin/hbase --config &quot;$HBASE_CONF_DIR&quot; org.apache.hadoop.hbase.util.HBaseConfTool hbase.cluster.distributed | head -n 1`        if [ &quot;$distMode&quot; != &#x27;false&#x27; ]; then          # 如果集群正在被停止，不再重启          zparent=`$bin/hbase org.apache.hadoop.hbase.util.HBaseConfTool zookeeper.znode.parent`          # 创建对应的 znode 并设置服务运行状态          if [ &quot;$zparent&quot; == &quot;null&quot; ]; then zparent=&quot;/hbase&quot;; fi          zkrunning=`$bin/hbase org.apache.hadoop.hbase.util.HBaseConfTool zookeeper.znode.state`          if [ &quot;$zkrunning&quot; == &quot;null&quot; ]; then zkrunning=&quot;running&quot;; fi          zkFullRunning=$zparent/$zkrunning          $bin/hbase zkcli stat $zkFullRunning 2&gt;&amp;1 | grep &quot;Node does not exist&quot;  1&gt;/dev/null 2&gt;&amp;1          # 如果发现上述指令的 grep 匹配到结果，则说明遇到了问题，显示警告信息，处理后退出          if [ $? -eq 0 ]; then            echo &quot;`date` hbase znode does not exist. Exiting Autostart process&quot; &gt;&gt; $&#123;HBASE_LOGOUT&#125;            # 删除 $HBASE_AUTOSTART_FILE 文件            rm -f &quot;$HBASE_AUTOSTART_FILE&quot;            exit 1          fi          # 如果没有找到 Zookeeper 服务，就不重启并显示警告信息          $bin/hbase zkcli stat $zkFullRunning 2&gt;&amp;1 | grep Exception | grep ConnectionLoss  1&gt;/dev/null 2&gt;&amp;1          if [ $? -eq 0 ]; then            echo &quot;`date` zookeeper not found. Exiting Autostart process&quot; &gt;&gt; $&#123;HBASE_LOGOUT&#125;            rm -f &quot;$HBASE_AUTOSTART_FILE&quot;            exit 1          fi        fi      fi      // 当前日期      curDate=`date +%s`      // 是否重新设置自动启动窗口      autostartWindowReset=false      # 假如超过了自动启动的窗口大小，就重新设置一下      if [ $AUTOSTART_WINDOW_SIZE -gt 0 ] &amp;&amp; [ $(( $curDate - $autostartWindowStartDate )) -gt $(( $AUTOSTART_WINDOW_SIZE * $ONE_HOUR_IN_SECS )) ]; then        echo &quot;Resetting Autorestart window size: $autostartWindowStartDate&quot; &gt;&gt; $&#123;HBASE_LOGOUT&#125;        autostartWindowStartDate=$curDate        autostartWindowReset=true        autostartCount=0      fi      # 当重试次数超过了给定的窗口大小限制（窗口大小不是 0），就杀掉程序，处理后退出      if ! $autostartWindowReset &amp;&amp; [ $AUTOSTART_WINDOW_RETRY_LIMIT -gt 0 ] &amp;&amp; [ $autostartCount -gt $AUTOSTART_WINDOW_RETRY_LIMIT ]; then        echo &quot;`date` Autostart window retry limit: $AUTOSTART_WINDOW_RETRY_LIMIT exceeded for given window size: $AUTOSTART_WINDOW_SIZE hours.. Exiting...&quot; &gt;&gt; $&#123;HBASE_LOGLOG&#125;        rm -f &quot;$HBASE_AUTOSTART_FILE&quot;        exit 1      fi      # 等待关闭的钩子完成      sleep 20    done  ;;(stop)    echo running $command, logging to $HBASE_LOGOUT    rm -f &quot;$HBASE_AUTOSTART_FILE&quot;    # 判断是否存在进程号的文件    if [ -f $HBASE_PID ]; then      pidToKill=`cat $HBASE_PID`      # 执行 kill -0 以确认进程是否在运行，如果在运行则传递 kill 信号，调用 hbase-common.sh 的 waitForProcessEnd 函数等待执行      if kill -0 $pidToKill &gt; /dev/null 2&gt;&amp;1; then        echo -n stopping $command        echo &quot;`date` Terminating $command&quot; &gt;&gt; $HBASE_LOGLOG        kill $pidToKill &gt; /dev/null 2&gt;&amp;1        waitForProcessEnd $pidToKill $command      else        retval=$?        echo no $command to stop because kill -0 of pid $pidToKill failed with status $retval      fi    else      echo no $command to stop because no pid file $HBASE_PID    fi    rm -f $HBASE_PID  ;;(restart)    echo running $command, logging to $HBASE_LOGOUT    # 停止服务    $thiscmd --config &quot;$&#123;HBASE_CONF_DIR&#125;&quot; stop $command $args &amp;    wait_until_done $!    # 等待用户指定的睡眠周期    sp=$&#123;HBASE_RESTART_SLEEP:-3&#125;    if [ $sp -gt 0 ]; then      sleep $sp    fi    # 启动服务    $thiscmd --config &quot;$&#123;HBASE_CONF_DIR&#125;&quot; start $command $args &amp;    wait_until_done $!  ;;(*)  echo $usage  exit 1  ;;esac</code></pre><h3 id="hbase-common-sh"><a href="#hbase-common-sh" class="headerlink" title="hbase-common.sh"></a>hbase-common.sh</h3><p>仅有 waitForProcessEnd 方法，是个共享函数，用于等待进程结束，以 pid 和命令名称为参数。</p><pre><code class="hljs shell">waitForProcessEnd() &#123;<span class="hljs-meta">  #</span><span class="bash"> 待停止的进程号</span>  pidKilled=$1<span class="hljs-meta">  #</span><span class="bash"> 服务名</span>  commandName=$2  processedAt=`date +%s`<span class="hljs-meta">  #</span><span class="bash"> 判断进程是否仍在运行</span>  while kill -0 $pidKilled &gt; /dev/null 2&gt;&amp;1;   do     echo -n &quot;.&quot;     sleep 1;     # 如果进程持续的时间超过 $HBASE_STOP_TIMEOUT 即 1200s，不再等待，继续往下执行     if [ $(( `date +%s` - $processedAt )) -gt $&#123;HBASE_STOP_TIMEOUT:-1200&#125; ]; then       break;     fi   done<span class="hljs-meta">  #</span><span class="bash"> 如果进程仍在运行，执行 <span class="hljs-built_in">kill</span> -9</span>  if kill -0 $pidKilled &gt; /dev/null 2&gt;&amp;1; then    echo -n force stopping $commandName with kill -9 $pidKilled    $JAVA_HOME/bin/jstack -l $pidKilled &gt; &quot;$logout&quot; 2&gt;&amp;1    kill -9 $pidKilled &gt; /dev/null 2&gt;&amp;1  fi<span class="hljs-meta">  #</span><span class="bash"> Add a CR after we<span class="hljs-string">&#x27;re done w/ dots.</span></span>  echo&#125;</code></pre><h3 id="zookeepers-sh"><a href="#zookeepers-sh" class="headerlink" title="zookeepers.sh"></a>zookeepers.sh</h3><p>接收 hbase-daemon.sh 中传递的参数，在所有的 Zookeeper 主机上执行命令。</p><pre><code class="hljs shell"><span class="hljs-meta">#</span><span class="bash"> 环境变量</span><span class="hljs-meta">#</span><span class="bash"> 配置文件目录，默认是 <span class="hljs-variable">$&#123;HBASE_HOME&#125;</span>/conf</span><span class="hljs-meta">#</span><span class="bash"> HBASE_CONF_DIR</span><span class="hljs-meta">#</span><span class="bash"> 在生成远程命令的时候睡眠的时间，默认未设置</span><span class="hljs-meta">#</span><span class="bash"> HBASE_SLAVE_SLEEP</span><span class="hljs-meta">#</span><span class="bash"> 执行远程命令时，传递给 ssh 的选项</span><span class="hljs-meta">#</span><span class="bash"> HBASE_SSH_OPTS</span><span class="hljs-meta">#</span><span class="bash"> 仿照 <span class="hljs-variable">$HADOOP_HOME</span>/bin/slaves.sh</span>usage=&quot;Usage: zookeepers [--config &lt;hbase-confdir&gt;] command...&quot;<span class="hljs-meta">#</span><span class="bash"> 如果没有指定参数，输出 usage</span>if [ $# -le 0 ]; then  echo $usage  exit 1fi<span class="hljs-meta">#</span><span class="bash"> 获取当前路径</span>bin=`dirname &quot;$&#123;BASH_SOURCE-$0&#125;&quot;`bin=`cd &quot;$bin&quot;&gt;/dev/null; pwd`<span class="hljs-meta">#</span><span class="bash"> 加载 hbase-config.sh</span>. &quot;$bin&quot;/hbase-config.sh<span class="hljs-meta">#</span><span class="bash"> 如果 <span class="hljs-variable">$HBASE_MANAGES_ZK</span> 参数未配置，即 hbase-env.sh 中的 <span class="hljs-built_in">export</span> HBASE_MANAGES_ZK=<span class="hljs-literal">true</span> 注释没打开，则将此参数设置为 <span class="hljs-literal">true</span></span>if [ &quot;$HBASE_MANAGES_ZK&quot; = &quot;&quot; ]; then  HBASE_MANAGES_ZK=truefi<span class="hljs-meta">#</span><span class="bash"> 调用 <span class="hljs-variable">$bin</span>/hbase 脚本，运行 ZKServerTool 类获取 Zookeeper 所有的主机，通过 grep 和 sed 命令对结果进行处理，见附 3，$<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;@// /\\ &#125;</span>&quot;</span>会将命令中将所有的 \ 替换成为空格</span>if [ &quot;$HBASE_MANAGES_ZK&quot; = &quot;true&quot; ]; then  hosts=`&quot;$bin&quot;/hbase org.apache.hadoop.hbase.zookeeper.ZKServerTool | grep &#x27;^ZK host:&#x27; | sed &#x27;s,^ZK host:,,&#x27;`  cmd=$&quot;$&#123;@// /\\ &#125;&quot;<span class="hljs-meta">  #</span><span class="bash"> 在所有的主机上启动 Zookeeper 服务</span>  for zookeeper in $hosts; do   ssh $HBASE_SSH_OPTS $zookeeper $cmd 2&gt;&amp;1 | sed &quot;s/^/$zookeeper: /&quot; &amp;   if [ &quot;$HBASE_SLAVE_SLEEP&quot; != &quot;&quot; ]; then     sleep $HBASE_SLAVE_SLEEP   fi  donefiwait</code></pre><h4 id="附-3"><a href="#附-3" class="headerlink" title="附 3"></a>附 3</h4><p>通过 $bin/hbase 运行此类。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ZKServerTool</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-title">ZKServerTool</span><span class="hljs-params">()</span> </span>&#123;    &#125;        <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> ServerName[] readZKNodes(Configuration conf) &#123;        List&lt;ServerName&gt; hosts = <span class="hljs-keyword">new</span> LinkedList&lt;&gt;();        <span class="hljs-comment">// 从 conf 中获取 hbase.zookeeper.quorum 参数对应的值，默认为 localhost</span>        String quorum = conf.get(HConstants.ZOOKEEPER_QUORUM, HConstants.LOCALHOST);                String[] values = quorum.split(<span class="hljs-string">&quot;,&quot;</span>);        <span class="hljs-keyword">for</span> (String value : values) &#123;          String[] parts = value.split(<span class="hljs-string">&quot;:&quot;</span>);          String host = parts[<span class="hljs-number">0</span>];          <span class="hljs-comment">// 默认端口 2181</span>          <span class="hljs-keyword">int</span> port = HConstants.DEFAULT_ZOOKEEPER_CLIENT_PORT;          <span class="hljs-keyword">if</span> (parts.length &gt; <span class="hljs-number">1</span>) &#123;            port = Integer.parseInt(parts[<span class="hljs-number">1</span>]);          &#125;          hosts.add(ServerName.valueOf(host, port, -<span class="hljs-number">1</span>));        &#125;        <span class="hljs-comment">// 转换成数组输出</span>        <span class="hljs-keyword">return</span> hosts.toArray(<span class="hljs-keyword">new</span> ServerName[hosts.size()]);    &#125;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String[] args)</span> </span>&#123;        <span class="hljs-keyword">for</span>(ServerName server: readZKNodes(HBaseConfiguration.create())) &#123;          <span class="hljs-comment">// bin/zookeeper.sh 依赖于 &quot;ZK host&quot; 字符串进行 grep 操作，区分大小写</span>          System.out.println(<span class="hljs-string">&quot;ZK host: &quot;</span> + server.getHostname());        &#125;    &#125;&#125;</code></pre><h3 id="master-backup-sh"><a href="#master-backup-sh" class="headerlink" title="master-backup.sh"></a>master-backup.sh</h3><p>接收 hbase-daemon.sh 中传递的参数，在所有的 backup master 主机上执行命令。</p><pre><code class="hljs shell"><span class="hljs-meta">#</span><span class="bash"> 环境变量</span><span class="hljs-meta">#</span><span class="bash"> 远程主机文件命名，默认是 <span class="hljs-variable">$&#123;HBASE_CONF_DIR&#125;</span>/backup-masters</span><span class="hljs-meta">#</span><span class="bash"> HBASE_BACKUP_MASTERS</span><span class="hljs-meta">#</span><span class="bash"> Hadoop 配置文件路径，默认是 <span class="hljs-variable">$&#123;HADOOP_HOME&#125;</span>/conf</span><span class="hljs-meta">#</span><span class="bash"> HADOOP_CONF_DIR</span><span class="hljs-meta">#</span><span class="bash"> HBase 配置文件路径，默认是 <span class="hljs-variable">$&#123;HBASE_HOME&#125;</span>/conf</span><span class="hljs-meta">#</span><span class="bash"> HBASE_CONF_DIR</span><span class="hljs-meta">#</span><span class="bash"> 在生成远程命令的时候睡眠的时间，默认未设置</span><span class="hljs-meta">#</span><span class="bash"> HBASE_SLAVE_SLEEP</span><span class="hljs-meta">#</span><span class="bash"> 执行远程命令时，传递给 ssh 的选项</span><span class="hljs-meta">#</span><span class="bash"> HBASE_SSH_OPTS</span><span class="hljs-meta">#</span><span class="bash"> 仿照 <span class="hljs-variable">$HADOOP_HOME</span>/bin/slaves.sh</span>usage=&quot;Usage: $0 [--config &lt;hbase-confdir&gt;] command...&quot;<span class="hljs-meta">#</span><span class="bash"> 如果没有指定参数，输出 usage</span>if [ $# -le 0 ]; then  echo $usage  exit 1fi<span class="hljs-meta">#</span><span class="bash"> 获取当前路径</span>bin=`dirname &quot;$&#123;BASH_SOURCE-$0&#125;&quot;`bin=`cd &quot;$bin&quot;&gt;/dev/null; pwd`<span class="hljs-meta">#</span><span class="bash"> 加载 hbase-config.sh</span>. &quot;$bin&quot;/hbase-config.sh<span class="hljs-meta">#</span><span class="bash"> 如果在命令行中指定了 master backup 文件，那么优先级高于 hbase-env.sh 中的配置，此处进行保存</span>HOSTLIST=$HBASE_BACKUP_MASTERSif [ &quot;$HOSTLIST&quot; = &quot;&quot; ]; then  if [ &quot;$HBASE_BACKUP_MASTERS&quot; = &quot;&quot; ]; then    export HOSTLIST=&quot;$&#123;HBASE_CONF_DIR&#125;/backup-masters&quot;  else    export HOSTLIST=&quot;$&#123;HBASE_BACKUP_MASTERS&#125;&quot;  fifi<span class="hljs-meta">#</span><span class="bash"> $<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;@// /\\ &#125;</span>&quot;</span>会将命令中将所有的 \ 替换成为空格</span>args=$&#123;@// /\\ &#125;args=$&#123;args/master-backup/master&#125;<span class="hljs-meta">#</span><span class="bash"> 登陆到每个节点上，以 backup 的方式启动 master，启动后 Zookeeper 会自动选取一个 master 作为 active，其他的都是 backup</span>if [ -f $HOSTLIST ]; then  for hmaster in `cat &quot;$HOSTLIST&quot;`; do   ssh $HBASE_SSH_OPTS $hmaster $&quot;$args --backup&quot; \     2&gt;&amp;1 | sed &quot;s/^/$hmaster: /&quot; &amp;   if [ &quot;$HBASE_SLAVE_SLEEP&quot; != &quot;&quot; ]; then     sleep $HBASE_SLAVE_SLEEP   fi  donefi wait</code></pre><h3 id="regionservers-sh"><a href="#regionservers-sh" class="headerlink" title="regionservers.sh"></a>regionservers.sh</h3><p>接收 hbase-daemon.sh 中传递的参数，在所有的 RegionServer 主机上执行命令。</p><pre><code class="hljs shell"><span class="hljs-meta">#</span><span class="bash"> 环境变量</span><span class="hljs-meta">#</span><span class="bash"> 远程主机文件命名，默认是 <span class="hljs-variable">$&#123;HADOOP_CONF_DIR&#125;</span>/regionservers</span><span class="hljs-meta">#</span><span class="bash"> HBASE_REGIONSERVERS</span><span class="hljs-meta">#</span><span class="bash"> Hadoop 配置文件路径，默认是 <span class="hljs-variable">$&#123;HADOOP_HOME&#125;</span>/conf</span><span class="hljs-meta">#</span><span class="bash"> HADOOP_CONF_DIR</span><span class="hljs-meta">#</span><span class="bash"> HBase 配置文件路径，默认是 <span class="hljs-variable">$&#123;HBASE_HOME&#125;</span>/conf</span><span class="hljs-meta">#</span><span class="bash"> HBASE_CONF_DIR</span><span class="hljs-meta">#</span><span class="bash"> 在生成远程命令的时候睡眠的时间，默认未设置</span><span class="hljs-meta">#</span><span class="bash"> HBASE_SLAVE_SLEEP</span><span class="hljs-meta">#</span><span class="bash"> 执行远程命令时，传递给 ssh 的选项</span><span class="hljs-meta">#</span><span class="bash"> HBASE_SSH_OPTS</span><span class="hljs-meta">#</span><span class="bash"> 仿照 <span class="hljs-variable">$HADOOP_HOME</span>/bin/slaves.sh</span>usage=&quot;Usage: regionservers [--config &lt;hbase-confdir&gt;] command...&quot;<span class="hljs-meta">#</span><span class="bash"> 如果没有指定参数，输出 usage</span>if [ $# -le 0 ]; then  echo $usage  exit 1fi<span class="hljs-meta">#</span><span class="bash"> 获取当前路径</span>bin=`dirname &quot;$&#123;BASH_SOURCE-$0&#125;&quot;`bin=`cd &quot;$bin&quot;&gt;/dev/null; pwd`. &quot;$bin&quot;/hbase-config.sh<span class="hljs-meta">#</span><span class="bash"> 如果在命令行中指定了 regionservers 文件，那么优先级高于 hbase-env.sh 中的配置，此处进行保存</span>HOSTLIST=$HBASE_REGIONSERVERSif [ &quot;$HOSTLIST&quot; = &quot;&quot; ]; then  if [ &quot;$HBASE_REGIONSERVERS&quot; = &quot;&quot; ]; then    export HOSTLIST=&quot;$&#123;HBASE_CONF_DIR&#125;/regionservers&quot;  else    export HOSTLIST=&quot;$&#123;HBASE_REGIONSERVERS&#125;&quot;  fifiregionservers=`cat &quot;$HOSTLIST&quot;`<span class="hljs-meta">#</span><span class="bash"> 如果 regionservers 是默认的 localhost，则会在本地启动 regionserver，集群模式则按顺序在各个节点上启动 RegionServer，$<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;@// /\\ &#125;</span>&quot;</span>会将命令中将所有的 \ 替换成为空格</span>if [ &quot;$regionservers&quot; = &quot;localhost&quot; ]; then  HBASE_REGIONSERVER_ARGS=&quot;\    -Dhbase.regionserver.port=16020 \    -Dhbase.regionserver.info.port=16030&quot;<span class="hljs-meta">  $</span><span class="bash"><span class="hljs-string">&quot;<span class="hljs-variable">$&#123;@// /\\ &#125;</span>&quot;</span> <span class="hljs-variable">$&#123;HBASE_REGIONSERVER_ARGS&#125;</span> \</span>        2&gt;&amp;1 | sed &quot;s/^/$regionserver: /&quot; &amp;else  for regionserver in `cat &quot;$HOSTLIST&quot;`; do    if $&#123;HBASE_SLAVE_PARALLEL:-true&#125;; then      ssh $HBASE_SSH_OPTS $regionserver $&quot;$&#123;@// /\\ &#125;&quot; \        2&gt;&amp;1 | sed &quot;s/^/$regionserver: /&quot; &amp;    else # run each command serially      ssh $HBASE_SSH_OPTS $regionserver $&quot;$&#123;@// /\\ &#125;&quot; \        2&gt;&amp;1 | sed &quot;s/^/$regionserver: /&quot;    fi    if [ &quot;$HBASE_SLAVE_SLEEP&quot; != &quot;&quot; ]; then      sleep $HBASE_SLAVE_SLEEP    fi  donefiwait</code></pre><h3 id="bin-hbase"><a href="#bin-hbase" class="headerlink" title="bin/hbase"></a>bin/hbase</h3><p>hbase 命令脚本，基于 hadoop 命令脚本，它在 hadoop 脚本之前完成了相关配置。</p><pre><code class="hljs shell"><span class="hljs-meta">#</span><span class="bash"> 环境变量</span><span class="hljs-meta">#</span><span class="bash"> 要使用的 java 实现，覆盖 JAVA_HOME</span><span class="hljs-meta">#</span><span class="bash"> JAVA_HOME</span><span class="hljs-meta">#</span><span class="bash"> 额外的 Java CLASSPATH</span><span class="hljs-meta">#</span><span class="bash"> HBASE_CLASSPATH</span><span class="hljs-meta">#</span><span class="bash"> 作为 system classpath 的额外 Java CLASSPATH 的前缀</span><span class="hljs-meta">#</span><span class="bash"> HBASE_CLASSPATH_PREFIX</span><span class="hljs-meta">#</span><span class="bash"> 使用的最大堆数量，默认未设置，并使用 JVM 的默认设置，通常是可用内存的 1/4</span><span class="hljs-meta">#</span><span class="bash"> HBASE_HEAPSIZE</span><span class="hljs-meta">#</span><span class="bash"> 对 JAVA_LIBRARY_PATH 的 HBase 添加，用于添加本机库</span><span class="hljs-meta">#</span><span class="bash"> HBASE_LIBRARY_PATH</span><span class="hljs-meta">#</span><span class="bash"> 额外的 Java 运行时选项</span><span class="hljs-meta">#</span><span class="bash"> HBASE_OPTS</span><span class="hljs-meta">#</span><span class="bash"> HBase 配置文件路径，默认是 <span class="hljs-variable">$&#123;HBASE_HOME&#125;</span>/conf</span><span class="hljs-meta">#</span><span class="bash"> HBASE_CONF_DIR</span><span class="hljs-meta">#</span><span class="bash">日志追加器，默认是控制台 INFO 级别</span><span class="hljs-meta">#</span><span class="bash"> HBASE_ROOT_LOGGER</span><span class="hljs-meta">#</span><span class="bash"> JRuby路径：<span class="hljs-variable">$JRUBY_HOME</span>/lib/jruby.jar 应该存在，默认为 HBase 打包的jar</span><span class="hljs-meta">#</span><span class="bash"> JRUBY_HOME</span><span class="hljs-meta">#</span><span class="bash"> 额外的选项（例如<span class="hljs-string">&#x27;--1.9&#x27;</span>）传递给了 hbase，默认为空</span><span class="hljs-meta">#</span><span class="bash"> JRUBY_OPTS</span><span class="hljs-meta">#</span><span class="bash"> 额外的传递给 hbase shell 的选项，默认为空</span><span class="hljs-meta">#</span><span class="bash">   HBASE_SHELL_OPTS</span><span class="hljs-meta">#</span><span class="bash"> 获取当前目录</span>bin=`dirname &quot;$0&quot;`bin=`cd &quot;$bin&quot;&gt;/dev/null; pwd`<span class="hljs-meta">#</span><span class="bash"> 加载 hbase-config.sh 获取配置</span>. &quot;$bin&quot;/hbase-config.sh<span class="hljs-meta">#</span><span class="bash"> 检测系统，是否使用 cygwin</span>cygwin=falsecase &quot;`uname`&quot; inCYGWIN*) cygwin=true;;esac<span class="hljs-meta">#</span><span class="bash"> 检测当前是否在 HBase 的根目录中</span>in_dev_env=falseif [ -d &quot;$&#123;HBASE_HOME&#125;/target&quot; ]; then  in_dev_env=truefi<span class="hljs-meta">#</span><span class="bash"> 检测是否在综合压缩包中</span>in_omnibus_tarball=&quot;false&quot;if [ -f &quot;$&#123;HBASE_HOME&#125;/bin/hbase-daemons.sh&quot; ]; then  in_omnibus_tarball=&quot;true&quot;fi<span class="hljs-meta">#</span><span class="bash"> <span class="hljs-built_in">read</span> 当读到 <span class="hljs-string">&#x27;&#x27;</span> 即结束，此处作为一部分用法进行输出</span>read -d &#x27;&#x27; options_string &lt;&lt; EOFOptions:  --config DIR         Configuration direction to use. Default: ./conf  --hosts HOSTS        Override the list in &#x27;regionservers&#x27; file  --auth-as-server     Authenticate to ZooKeeper using servers configuration  --internal-classpath Skip attempting to use client facing jars (WARNING: unstable results between versions)EOF<span class="hljs-meta">#</span><span class="bash"> 如果没有指定参数，输出 usage，包含上面的内容</span>if [ $# = 0 ]; then  echo &quot;Usage: hbase [&lt;options&gt;] &lt;command&gt; [&lt;args&gt;]&quot;  echo &quot;$options_string&quot;  echo &quot;&quot;  echo &quot;Commands:&quot;  echo &quot;Some commands take arguments. Pass no args or -h for usage.&quot;  echo &quot;  shell            Run the HBase shell&quot;  echo &quot;  hbck             Run the HBase &#x27;fsck&#x27; tool. Defaults read-only hbck1.&quot;  echo &quot;                   Pass &#x27;-j /path/to/HBCK2.jar&#x27; to run hbase-2.x HBCK2.&quot;  echo &quot;  snapshot         Tool for managing snapshots&quot;  if [ &quot;$&#123;in_omnibus_tarball&#125;&quot; = &quot;true&quot; ]; then    echo &quot;  wal              Write-ahead-log analyzer&quot;    echo &quot;  hfile            Store file analyzer&quot;    echo &quot;  zkcli            Run the ZooKeeper shell&quot;    echo &quot;  master           Run an HBase HMaster node&quot;    echo &quot;  regionserver     Run an HBase HRegionServer node&quot;    echo &quot;  zookeeper        Run a ZooKeeper server&quot;    echo &quot;  rest             Run an HBase REST server&quot;    echo &quot;  thrift           Run the HBase Thrift server&quot;    echo &quot;  thrift2          Run the HBase Thrift2 server&quot;    echo &quot;  clean            Run the HBase clean up script&quot;  fi  echo &quot;  classpath        Dump hbase CLASSPATH&quot;  echo &quot;  mapredcp         Dump CLASSPATH entries required by mapreduce&quot;  echo &quot;  pe               Run PerformanceEvaluation&quot;  echo &quot;  ltt              Run LoadTestTool&quot;  echo &quot;  canary           Run the Canary tool&quot;  echo &quot;  version          Print the version&quot;  echo &quot;  completebulkload Run BulkLoadHFiles tool&quot;  echo &quot;  regionsplitter   Run RegionSplitter tool&quot;  echo &quot;  rowcounter       Run RowCounter tool&quot;  echo &quot;  cellcounter      Run CellCounter tool&quot;  echo &quot;  pre-upgrade      Run Pre-Upgrade validator tool&quot;  echo &quot;  hbtop            Run HBTop tool&quot;  echo &quot;  CLASSNAME        Run the class named CLASSNAME&quot;  exit 1fi<span class="hljs-meta">#</span><span class="bash"> 获取传入的第一个参数</span>COMMAND=$1<span class="hljs-meta">#</span><span class="bash"> 命令左移</span>shiftJAVA=$JAVA_HOME/bin/java<span class="hljs-meta">#</span><span class="bash"> 覆盖此命令的默认设置（如果适用）</span>if [ -f &quot;$HBASE_HOME/conf/hbase-env-$COMMAND.sh&quot; ]; then  . &quot;$HBASE_HOME/conf/hbase-env-$COMMAND.sh&quot;fiadd_size_suffix() &#123;    # 如果参数缺少一个，则添加一个“m”后缀    local val=&quot;$1&quot;    local lastchar=$&#123;val: -1&#125;    if [[ &quot;mMgG&quot; == *$lastchar* ]]; then        echo $val    else        echo $&#123;val&#125;m    fi&#125;<span class="hljs-meta">#</span><span class="bash"> 检测 HBASE_HEAPSIZE 是否设置</span>if [[ -n &quot;$HBASE_HEAPSIZE&quot; ]]; then    JAVA_HEAP_MAX=&quot;-Xmx$(add_size_suffix $HBASE_HEAPSIZE)&quot;fi<span class="hljs-meta">#</span><span class="bash"> 检测 HBASE_OFFHEAPSIZE 是否设置</span>if [[ -n &quot;$HBASE_OFFHEAPSIZE&quot; ]]; then    JAVA_OFFHEAP_MAX=&quot;-XX:MaxDirectMemorySize=$(add_size_suffix $HBASE_OFFHEAPSIZE)&quot;fi<span class="hljs-meta">#</span><span class="bash"> 这样在下面的循环中可以正确处理带空格的文件名，设置 IFS</span>ORIG_IFS=$IFSIFS=<span class="hljs-meta">#</span><span class="bash"> CLASSPATH 初始化包含 HBASE_CONF_DIR</span>CLASSPATH=&quot;$&#123;HBASE_CONF_DIR&#125;&quot;CLASSPATH=$&#123;CLASSPATH&#125;:$JAVA_HOME/lib/tools.jar<span class="hljs-meta">#</span><span class="bash"> 如果传入的文件存在，则加入 CLASSPATH</span>add_to_cp_if_exists() &#123;  if [ -d &quot;$@&quot; ]; then    CLASSPATH=$&#123;CLASSPATH&#125;:&quot;$@&quot;  fi&#125;<span class="hljs-meta">#</span><span class="bash"> 对于发行版，将 hbase 和 webapp 添加到 CLASSPATH 中 Webapp 必须首先出现，否则会使 Jetty 混乱</span>if [ -d &quot;$HBASE_HOME/hbase-webapps&quot; ]; then  add_to_cp_if_exists &quot;$&#123;HBASE_HOME&#125;&quot;fi<span class="hljs-meta">#</span><span class="bash"> 如果在开发环境中则添加</span>if [ -d &quot;$HBASE_HOME/hbase-server/target/hbase-webapps&quot; ]; then  if [ &quot;$COMMAND&quot; = &quot;thrift&quot; ] ; then    add_to_cp_if_exists &quot;$&#123;HBASE_HOME&#125;/hbase-thrift/target&quot;  elif [ &quot;$COMMAND&quot; = &quot;thrift2&quot; ] ; then    add_to_cp_if_exists &quot;$&#123;HBASE_HOME&#125;/hbase-thrift/target&quot;  elif [ &quot;$COMMAND&quot; = &quot;rest&quot; ] ; then    add_to_cp_if_exists &quot;$&#123;HBASE_HOME&#125;/hbase-rest/target&quot;  else    add_to_cp_if_exists &quot;$&#123;HBASE_HOME&#125;/hbase-server/target&quot;    # 需要下面的 GetJavaProperty 检查    add_to_cp_if_exists &quot;$&#123;HBASE_HOME&#125;/hbase-server/target/classes&quot;  fifi<span class="hljs-meta">#</span><span class="bash"> 如果可用，将 Hadoop 添加到 CLASSPATH 和 JAVA_LIBRARY_PATH，允许禁用此功能</span>if [ &quot;$HBASE_DISABLE_HADOOP_CLASSPATH_LOOKUP&quot; != &quot;true&quot; ] ; then  HADOOP_IN_PATH=$(PATH=&quot;$&#123;HADOOP_HOME:-$&#123;HADOOP_PREFIX&#125;&#125;/bin:$PATH&quot; which hadoop 2&gt;/dev/null)fi<span class="hljs-meta">#</span><span class="bash"> 声明 shaded_jar，将 libs 添加到 CLASSPATH</span>declare shaded_jarif [ &quot;$&#123;INTERNAL_CLASSPATH&#125;&quot; != &quot;true&quot; ]; then<span class="hljs-meta">  #</span><span class="bash"> find our shaded jars</span>  declare shaded_client  declare shaded_client_byo_hadoop  declare shaded_mapreduce  for f in &quot;$&#123;HBASE_HOME&#125;&quot;/lib/shaded-clients/hbase-shaded-client*.jar; do    if [[ &quot;$&#123;f&#125;&quot; =~ byo-hadoop ]]; then      shaded_client_byo_hadoop=&quot;$&#123;f&#125;&quot;    else      shaded_client=&quot;$&#123;f&#125;&quot;    fi  done  for f in &quot;$&#123;HBASE_HOME&#125;&quot;/lib/shaded-clients/hbase-shaded-mapreduce*.jar; do    shaded_mapreduce=&quot;$&#123;f&#125;&quot;  done<span class="hljs-meta">  #</span><span class="bash"> 如果命令可以使用 shaded client，使用它</span>  declare -a commands_in_client_jar=(&quot;classpath&quot; &quot;version&quot; &quot;hbtop&quot;)  for c in &quot;$&#123;commands_in_client_jar[@]&#125;&quot;; do    if [ &quot;$&#123;COMMAND&#125;&quot; = &quot;$&#123;c&#125;&quot; ]; then      if [ -n &quot;$&#123;HADOOP_IN_PATH&#125;&quot; ] &amp;&amp; [ -f &quot;$&#123;HADOOP_IN_PATH&#125;&quot; ]; then        # 如果上面没有找到一个 jar，它将为空，然后下面的检查将默认返回内部类路径        shaded_jar=&quot;$&#123;shaded_client_byo_hadoop&#125;&quot;      else        # 如果上面没有找到一个jar，它将为空，然后下面的检查将默认返回内部类路径        shaded_jar=&quot;$&#123;shaded_client&#125;&quot;      fi      break    fi  done<span class="hljs-meta">  #</span><span class="bash"> 如果命令需要 shaded mapreduce，使用它</span><span class="hljs-meta">  #</span><span class="bash"> 此处不包含 N.B “mapredcp”，因为在 shaded 情况下，它会跳过我们构建的类路径</span>  declare -a commands_in_mr_jar=(&quot;hbck&quot; &quot;snapshot&quot; &quot;canary&quot; &quot;regionsplitter&quot; &quot;pre-upgrade&quot;)  for c in &quot;$&#123;commands_in_mr_jar[@]&#125;&quot;; do    if [ &quot;$&#123;COMMAND&#125;&quot; = &quot;$&#123;c&#125;&quot; ]; then      # 如果上面没有找到一个jar，它将为空，然后下面的检查将默认返回内部类路径      shaded_jar=&quot;$&#123;shaded_mapreduce&#125;&quot;      break    fi  done<span class="hljs-meta">  #</span><span class="bash"> 当我们在运行时获得完整的 hadoop 类路径时，某些命令专门只能使用 shaded mapreduce</span>  if [ -n &quot;$&#123;HADOOP_IN_PATH&#125;&quot; ] &amp;&amp; [ -f &quot;$&#123;HADOOP_IN_PATH&#125;&quot; ]; then    declare -a commands_in_mr_need_hadoop=(&quot;backup&quot; &quot;restore&quot; &quot;rowcounter&quot; &quot;cellcounter&quot;)    for c in &quot;$&#123;commands_in_mr_need_hadoop[@]&#125;&quot;; do      if [ &quot;$&#123;COMMAND&#125;&quot; = &quot;$&#123;c&#125;&quot; ]; then        # 如果上面没有找到一个jar，它将为空，然后下面的检查将默认返回内部类路径        shaded_jar=&quot;$&#123;shaded_mapreduce&#125;&quot;        break      fi    done  fifi<span class="hljs-meta">#</span><span class="bash"> 加载相关 jar 包</span>if [ -n &quot;$&#123;shaded_jar&#125;&quot; ] &amp;&amp; [ -f &quot;$&#123;shaded_jar&#125;&quot; ]; then  CLASSPATH=&quot;$&#123;CLASSPATH&#125;:$&#123;shaded_jar&#125;&quot;<span class="hljs-meta">#</span><span class="bash"> fall through to grabbing all the lib jars and hope we<span class="hljs-string">&#x27;re in the omnibus tarball</span></span><span class="hljs-meta">#</span><span class="hljs-meta">#</span><span class="bash"> N.B. shell specifically can<span class="hljs-string">&#x27;t rely on the shaded artifacts because RSGroups is only</span></span><span class="hljs-meta">#</span><span class="bash"> available as non-shaded</span><span class="hljs-meta">#</span><span class="hljs-meta">#</span><span class="bash"> N.B. pe and ltt can<span class="hljs-string">&#x27;t easily rely on shaded artifacts because they live in hbase-mapreduce:test-jar</span></span><span class="hljs-meta">#</span><span class="bash"> and need some other jars that haven<span class="hljs-string">&#x27;t been relocated. Currently enumerating that list</span></span><span class="hljs-meta">#</span><span class="bash"> is too hard to be worth it.</span><span class="hljs-meta">#</span>else  for f in $HBASE_HOME/lib/*.jar; do    CLASSPATH=$&#123;CLASSPATH&#125;:$f;  done<span class="hljs-meta">  #</span><span class="bash"> make it easier to check <span class="hljs-keyword">for</span> shaded/not later on.</span>  shaded_jar=&quot;&quot;fifor f in &quot;$&#123;HBASE_HOME&#125;&quot;/lib/client-facing-thirdparty/*.jar; do  if [[ ! &quot;$&#123;f&#125;&quot; =~ ^.*/htrace-core-3.*\.jar$ ]] &amp;&amp; \     [ &quot;$&#123;f&#125;&quot; != &quot;htrace-core.jar$&quot; ] &amp;&amp; \     [[ ! &quot;$&#123;f&#125;&quot; =~ ^.*/slf4j-log4j.*$ ]]; then    CLASSPATH=&quot;$&#123;CLASSPATH&#125;:$&#123;f&#125;&quot;  fidone<span class="hljs-meta">#</span><span class="bash"> 默认的日志文件目录</span>if [ &quot;$HBASE_LOG_DIR&quot; = &quot;&quot; ]; then  HBASE_LOG_DIR=&quot;$HBASE_HOME/logs&quot;fi<span class="hljs-meta">#</span><span class="bash"> 默认的日志名</span>if [ &quot;$HBASE_LOGFILE&quot; = &quot;&quot; ]; then  HBASE_LOGFILE=&#x27;hbase.log&#x27;fi<span class="hljs-meta">#</span><span class="bash"> 组装 jar</span>function append_path() &#123;  if [ -z &quot;$1&quot; ]; then    echo &quot;$2&quot;  else    echo &quot;$1:$2&quot;  fi&#125;JAVA_PLATFORM=&quot;&quot;<span class="hljs-meta">#</span><span class="bash"> 如果定义了 HBASE_LIBRARY_PATH，则将其用作第一个或第二个选项</span>if [ &quot;$HBASE_LIBRARY_PATH&quot; != &quot;&quot; ]; then  JAVA_LIBRARY_PATH=$(append_path &quot;$JAVA_LIBRARY_PATH&quot; &quot;$HBASE_LIBRARY_PATH&quot;)fi<span class="hljs-meta">#</span><span class="bash"> 如果已配置并且可用，则将 Hadoop 添加到 CLASSPATH 和 JAVA_LIBRARY_PATH</span>if [ -n &quot;$&#123;HADOOP_IN_PATH&#125;&quot; ] &amp;&amp; [ -f &quot;$&#123;HADOOP_IN_PATH&#125;&quot; ]; then<span class="hljs-meta">  #</span><span class="bash"> 如果构建了 hbase，则将 hbase-server.jar 临时添加到 GetJavaProperty 的类路径中</span><span class="hljs-meta">  #</span><span class="bash"> 排除 hbase-server*-tests.jar</span>  temporary_cp=  for f in &quot;$&#123;HBASE_HOME&#125;&quot;/lib/hbase-server*.jar; do    if [[ ! &quot;$&#123;f&#125;&quot; =~ ^.*\-tests\.jar$ ]]; then      temporary_cp=&quot;:$f&quot;    fi  done  HADOOP_JAVA_LIBRARY_PATH=$(HADOOP_CLASSPATH=&quot;$CLASSPATH$&#123;temporary_cp&#125;&quot; &quot;$&#123;HADOOP_IN_PATH&#125;&quot; \                             org.apache.hadoop.hbase.util.GetJavaProperty java.library.path)  if [ -n &quot;$HADOOP_JAVA_LIBRARY_PATH&quot; ]; then    JAVA_LIBRARY_PATH=$(append_path &quot;$&#123;JAVA_LIBRARY_PATH&#125;&quot; &quot;$HADOOP_JAVA_LIBRARY_PATH&quot;)  fi  CLASSPATH=$(append_path &quot;$&#123;CLASSPATH&#125;&quot; &quot;$($&#123;HADOOP_IN_PATH&#125; classpath 2&gt;/dev/null)&quot;)else<span class="hljs-meta">  #</span><span class="bash"> 否则，如果我们提供的是 Hadoop，我们还需要使用它的版本构建，则应包括 htrace 3</span>  for f in &quot;$&#123;HBASE_HOME&#125;&quot;/lib/client-facing-thirdparty/htrace-core-3*.jar &quot;$&#123;HBASE_HOME&#125;&quot;/lib/client-facing-thirdparty/htrace-core.jar; do    if [ -f &quot;$&#123;f&#125;&quot; ]; then      CLASSPATH=&quot;$&#123;CLASSPATH&#125;:$&#123;f&#125;&quot;      break    fi  done<span class="hljs-meta">  #</span><span class="bash"> 使用 shaded jars 时，某些命令需要特殊处理。对于这些情况，我们依赖于 hbase-shaded-mapreduce 而不是 hbase-shaded-client*，因为我们利用了一些 IA.Private 类，这些私有类不再后者中。但是我们不使用<span class="hljs-string">&quot;hadoop jar&quot;</span>来调用它们，因此当我们不执行运行时 hadoop 类路径查找时，我们需要确保有一些 Hadoop 类可用。</span><span class="hljs-meta"> #</span><span class="bash"> 我们需要的一组类是打包在 shaded-client 中的那些类</span>  for c in &quot;$&#123;commands_in_mr_jar[@]&#125;&quot;; do    if [ &quot;$&#123;COMMAND&#125;&quot; = &quot;$&#123;c&#125;&quot; ] &amp;&amp; [ -n &quot;$&#123;shaded_jar&#125;&quot; ]; then      CLASSPATH=&quot;$&#123;CLASSPATH&#125;:$&#123;shaded_client:?We couldn\&#x27;t find the shaded client jar even though we did find the shaded MR jar. for command $&#123;COMMAND&#125; we need both. please use --internal-classpath as a workaround.&#125;&quot;      break    fi  donefi<span class="hljs-meta">#</span><span class="bash"> 最后添加用户指定的 CLASSPATH</span>if [ &quot;$HBASE_CLASSPATH&quot; != &quot;&quot; ]; then  CLASSPATH=$&#123;CLASSPATH&#125;:$&#123;HBASE_CLASSPATH&#125;fi<span class="hljs-meta">#</span><span class="bash"> 首先添加用户指定的 CLASSPATH 前缀</span>if [ &quot;$HBASE_CLASSPATH_PREFIX&quot; != &quot;&quot; ]; then  CLASSPATH=$&#123;HBASE_CLASSPATH_PREFIX&#125;:$&#123;CLASSPATH&#125;fi<span class="hljs-meta">#</span><span class="bash"> cygwin 路径转换</span>if $cygwin; then  CLASSPATH=`cygpath -p -w &quot;$CLASSPATH&quot;`  HBASE_HOME=`cygpath -d &quot;$HBASE_HOME&quot;`  HBASE_LOG_DIR=`cygpath -d &quot;$HBASE_LOG_DIR&quot;`fiif [ -d &quot;$&#123;HBASE_HOME&#125;/build/native&quot; -o -d &quot;$&#123;HBASE_HOME&#125;/lib/native&quot; ]; then  if [ -z $JAVA_PLATFORM ]; then    JAVA_PLATFORM=`CLASSPATH=$&#123;CLASSPATH&#125; $&#123;JAVA&#125; org.apache.hadoop.util.PlatformName | sed -e &quot;s/ /_/g&quot;`  fi  if [ -d &quot;$HBASE_HOME/build/native&quot; ]; then    JAVA_LIBRARY_PATH=$(append_path &quot;$JAVA_LIBRARY_PATH&quot; &quot;$&#123;HBASE_HOME&#125;/build/native/$&#123;JAVA_PLATFORM&#125;/lib&quot;)  fi  if [ -d &quot;$&#123;HBASE_HOME&#125;/lib/native&quot; ]; then    JAVA_LIBRARY_PATH=$(append_path &quot;$JAVA_LIBRARY_PATH&quot; &quot;$&#123;HBASE_HOME&#125;/lib/native/$&#123;JAVA_PLATFORM&#125;&quot;)  fifi<span class="hljs-meta">#</span><span class="bash"> cygwin 路径转换</span>if $cygwin; then  JAVA_LIBRARY_PATH=`cygpath -p &quot;$JAVA_LIBRARY_PATH&quot;`fi<span class="hljs-meta">#</span><span class="bash"> 清除循环中的 IFS</span>unset IFS<span class="hljs-meta">#</span><span class="bash"> 根据我们所运行的设置正确的 GC 选项</span>declare -a server_cmds=(&quot;master&quot; &quot;regionserver&quot; &quot;thrift&quot; &quot;thrift2&quot; &quot;rest&quot; &quot;avro&quot; &quot;zookeeper&quot;)for cmd in $&#123;server_cmds[@]&#125;; doif [[ $cmd == $COMMAND ]]; thenserver=truebreakfidoneif [[ $server ]]; thenHBASE_OPTS=&quot;$HBASE_OPTS $SERVER_GC_OPTS&quot;elseHBASE_OPTS=&quot;$HBASE_OPTS $CLIENT_GC_OPTS&quot;fiif [ &quot;$AUTH_AS_SERVER&quot; == &quot;true&quot; ] || [ &quot;$COMMAND&quot; = &quot;hbck&quot; ]; then   if [ -n &quot;$HBASE_SERVER_JAAS_OPTS&quot; ]; then     HBASE_OPTS=&quot;$HBASE_OPTS $HBASE_SERVER_JAAS_OPTS&quot;   else     HBASE_OPTS=&quot;$HBASE_OPTS $HBASE_REGIONSERVER_OPTS&quot;   fifi<span class="hljs-meta">#</span><span class="bash"> 检测命令是否需要 jline</span>declare -a jline_cmds=(&quot;zkcli&quot; &quot;org.apache.hadoop.hbase.zookeeper.ZKMainServer&quot;)for cmd in &quot;$&#123;jline_cmds[@]&#125;&quot;; do  if [[ $cmd == &quot;$COMMAND&quot; ]]; then    jline_needed=true    break  fidone<span class="hljs-meta">#</span><span class="bash"> <span class="hljs-keyword">for</span> jruby</span><span class="hljs-meta">#</span><span class="bash"> (1) <span class="hljs-keyword">for</span> the commands <span class="hljs-built_in">which</span> need jruby (see jruby_cmds defined below)</span><span class="hljs-meta">#</span><span class="bash">     A. when JRUBY_HOME is specified explicitly, eg. <span class="hljs-built_in">export</span> JRUBY_HOME=/usr/<span class="hljs-built_in">local</span>/share/jruby</span><span class="hljs-meta">#</span><span class="bash">        CLASSPATH and HBASE_OPTS are updated according to JRUBY_HOME specified</span><span class="hljs-meta">#</span><span class="bash">     B. when JRUBY_HOME is not specified explicitly</span><span class="hljs-meta">#</span><span class="bash">        add jruby packaged with HBase to CLASSPATH</span><span class="hljs-meta">#</span><span class="bash"> (2) <span class="hljs-keyword">for</span> other commands, <span class="hljs-keyword">do</span> nothing</span><span class="hljs-meta">#</span><span class="bash"> 检测命令是否需要 jruby</span>declare -a jruby_cmds=(&quot;shell&quot; &quot;org.jruby.Main&quot;)for cmd in &quot;$&#123;jruby_cmds[@]&#125;&quot;; do  if [[ $cmd == &quot;$COMMAND&quot; ]]; then    jruby_needed=true    break  fidoneadd_maven_deps_to_classpath() &#123;  f=&quot;$&#123;HBASE_HOME&#125;/hbase-build-configuration/target/$1&quot;  if [ ! -f &quot;$&#123;f&#125;&quot; ]; then      echo &quot;As this is a development environment, we need $&#123;f&#125; to be generated from maven (command: mvn install -DskipTests)&quot;      exit 1  fi  CLASSPATH=$&#123;CLASSPATH&#125;:$(cat &quot;$&#123;f&#125;&quot;)&#125;<span class="hljs-meta">#</span><span class="bash"> 添加开发环境类路径的东西</span>if $in_dev_env; then  add_maven_deps_to_classpath &quot;cached_classpath.txt&quot;  if [[ $jline_needed ]]; then    add_maven_deps_to_classpath &quot;cached_classpath_jline.txt&quot;  elif [[ $jruby_needed ]]; then    add_maven_deps_to_classpath &quot;cached_classpath_jruby.txt&quot;  fifi<span class="hljs-meta">#</span><span class="bash"> 命令需要 jruby</span>if [[ $jruby_needed ]]; then  if [ &quot;$JRUBY_HOME&quot; != &quot;&quot; ]; then  # JRUBY_HOME is specified explicitly, eg. export JRUBY_HOME=/usr/local/share/jruby    # add jruby.jar into CLASSPATH    CLASSPATH=&quot;$JRUBY_HOME/lib/jruby.jar:$CLASSPATH&quot;    # add jruby to HBASE_OPTS    HBASE_OPTS=&quot;$HBASE_OPTS -Djruby.home=$JRUBY_HOME -Djruby.lib=$JRUBY_HOME/lib&quot;  else  # JRUBY_HOME is not specified explicitly    if ! $in_dev_env; then  # not in dev environment      # add jruby packaged with HBase to CLASSPATH      JRUBY_PACKAGED_WITH_HBASE=&quot;$HBASE_HOME/lib/ruby/*.jar&quot;      for jruby_jar in $JRUBY_PACKAGED_WITH_HBASE; do        CLASSPATH=$jruby_jar:$CLASSPATH;      done    fi  fifi<span class="hljs-meta">#</span><span class="bash"> 找出要运行的 class，该脚本可用于直接运行 Java 类</span>if [ &quot;$COMMAND&quot; = &quot;shell&quot; ] ; then<span class="hljs-meta">#</span><span class="bash">find the hbase ruby sources</span>  if [ -d &quot;$HBASE_HOME/lib/ruby&quot; ]; then    HBASE_OPTS=&quot;$HBASE_OPTS -Dhbase.ruby.sources=$HBASE_HOME/lib/ruby&quot;  else    HBASE_OPTS=&quot;$HBASE_OPTS -Dhbase.ruby.sources=$HBASE_HOME/hbase-shell/src/main/ruby&quot;  fi  HBASE_OPTS=&quot;$HBASE_OPTS $HBASE_SHELL_OPTS&quot;  CLASS=&quot;org.jruby.Main -X+O $&#123;JRUBY_OPTS&#125; $&#123;HBASE_HOME&#125;/bin/hirb.rb&quot;elif [ &quot;$COMMAND&quot; = &quot;hbck&quot; ] ; then<span class="hljs-meta">  #</span><span class="bash"> Look <span class="hljs-keyword">for</span> the -j /path/to/HBCK2.jar parameter. Else pass through to hbck.</span>  case &quot;$&#123;1&#125;&quot; in    -j)    # Found -j parameter. Add arg to CLASSPATH and set CLASS to HBCK2.    shift    JAR=&quot;$&#123;1&#125;&quot;    if [ ! -f &quot;$&#123;JAR&#125;&quot; ]; then      echo &quot;$&#123;JAR&#125; file not found!&quot;      echo &quot;Usage: hbase [&lt;options&gt;] hbck -jar /path/to/HBCK2.jar [&lt;args&gt;]&quot;      exit 1    fi    CLASSPATH=&quot;$&#123;JAR&#125;:$&#123;CLASSPATH&#125;&quot;;    CLASS=&quot;org.apache.hbase.HBCK2&quot;    shift # past argument=value    ;;    *)    CLASS=&#x27;org.apache.hadoop.hbase.util.HBaseFsck&#x27;    ;;  esacelif [ &quot;$COMMAND&quot; = &quot;wal&quot; ] ; then  CLASS=&#x27;org.apache.hadoop.hbase.wal.WALPrettyPrinter&#x27;elif [ &quot;$COMMAND&quot; = &quot;hfile&quot; ] ; then  CLASS=&#x27;org.apache.hadoop.hbase.io.hfile.HFilePrettyPrinter&#x27;elif [ &quot;$COMMAND&quot; = &quot;zkcli&quot; ] ; then  CLASS=&quot;org.apache.hadoop.hbase.zookeeper.ZKMainServer&quot;  for f in $HBASE_HOME/lib/zkcli/*.jar; do    CLASSPATH=&quot;$&#123;CLASSPATH&#125;:$f&quot;;  doneelif [ &quot;$COMMAND&quot; = &quot;upgrade&quot; ] ; then  echo &quot;This command was used to upgrade to HBase 0.96, it was removed in HBase 2.0.0.&quot;  echo &quot;Please follow the documentation at http://hbase.apache.org/book.html#upgrading.&quot;  exit 1elif [ &quot;$COMMAND&quot; = &quot;snapshot&quot; ] ; then  SUBCOMMAND=$1  shift  if [ &quot;$SUBCOMMAND&quot; = &quot;create&quot; ] ; then    CLASS=&quot;org.apache.hadoop.hbase.snapshot.CreateSnapshot&quot;  elif [ &quot;$SUBCOMMAND&quot; = &quot;info&quot; ] ; then    CLASS=&quot;org.apache.hadoop.hbase.snapshot.SnapshotInfo&quot;  elif [ &quot;$SUBCOMMAND&quot; = &quot;export&quot; ] ; then    CLASS=&quot;org.apache.hadoop.hbase.snapshot.ExportSnapshot&quot;  else    echo &quot;Usage: hbase [&lt;options&gt;] snapshot &lt;subcommand&gt; [&lt;args&gt;]&quot;    echo &quot;$options_string&quot;    echo &quot;&quot;    echo &quot;Subcommands:&quot;    echo &quot;  create          Create a new snapshot of a table&quot;    echo &quot;  info            Tool for dumping snapshot information&quot;    echo &quot;  export          Export an existing snapshot&quot;    exit 1  fielif [ &quot;$COMMAND&quot; = &quot;master&quot; ] ; then  CLASS=&#x27;org.apache.hadoop.hbase.master.HMaster&#x27;  if [ &quot;$1&quot; != &quot;stop&quot; ] &amp;&amp; [ &quot;$1&quot; != &quot;clear&quot; ] ; then    HBASE_OPTS=&quot;$HBASE_OPTS $HBASE_MASTER_OPTS&quot;  fielif [ &quot;$COMMAND&quot; = &quot;regionserver&quot; ] ; then  CLASS=&#x27;org.apache.hadoop.hbase.regionserver.HRegionServer&#x27;  if [ &quot;$1&quot; != &quot;stop&quot; ] ; then    HBASE_OPTS=&quot;$HBASE_OPTS $HBASE_REGIONSERVER_OPTS&quot;  fielif [ &quot;$COMMAND&quot; = &quot;thrift&quot; ] ; then  CLASS=&#x27;org.apache.hadoop.hbase.thrift.ThriftServer&#x27;  if [ &quot;$1&quot; != &quot;stop&quot; ] ; then    HBASE_OPTS=&quot;$HBASE_OPTS $HBASE_THRIFT_OPTS&quot;  fielif [ &quot;$COMMAND&quot; = &quot;thrift2&quot; ] ; then  CLASS=&#x27;org.apache.hadoop.hbase.thrift2.ThriftServer&#x27;  if [ &quot;$1&quot; != &quot;stop&quot; ] ; then    HBASE_OPTS=&quot;$HBASE_OPTS $HBASE_THRIFT_OPTS&quot;  fielif [ &quot;$COMMAND&quot; = &quot;rest&quot; ] ; then  CLASS=&#x27;org.apache.hadoop.hbase.rest.RESTServer&#x27;  if [ &quot;$1&quot; != &quot;stop&quot; ] ; then    HBASE_OPTS=&quot;$HBASE_OPTS $HBASE_REST_OPTS&quot;  fielif [ &quot;$COMMAND&quot; = &quot;zookeeper&quot; ] ; then  CLASS=&#x27;org.apache.hadoop.hbase.zookeeper.HQuorumPeer&#x27;  if [ &quot;$1&quot; != &quot;stop&quot; ] ; then    HBASE_OPTS=&quot;$HBASE_OPTS $HBASE_ZOOKEEPER_OPTS&quot;  fielif [ &quot;$COMMAND&quot; = &quot;clean&quot; ] ; then  case $1 in    --cleanZk|--cleanHdfs|--cleanAll)      matches=&quot;yes&quot; ;;    *) ;;  esac  if [ $# -ne 1 -o &quot;$matches&quot; = &quot;&quot; ]; then    echo &quot;Usage: hbase clean (--cleanZk|--cleanHdfs|--cleanAll)&quot;    echo &quot;Options: &quot;    echo &quot;        --cleanZk   cleans hbase related data from zookeeper.&quot;    echo &quot;        --cleanHdfs cleans hbase related data from hdfs.&quot;    echo &quot;        --cleanAll  cleans hbase related data from both zookeeper and hdfs.&quot;    exit 1;  fi  &quot;$bin&quot;/hbase-cleanup.sh --config $&#123;HBASE_CONF_DIR&#125; $@  exit $?elif [ &quot;$COMMAND&quot; = &quot;mapredcp&quot; ] ; then<span class="hljs-meta">  #</span><span class="bash"> If we didn<span class="hljs-string">&#x27;t find a jar above, this will just be blank and the</span></span><span class="hljs-meta">  #</span><span class="bash"> check below will <span class="hljs-keyword">then</span> default back to the internal classpath.</span>  shaded_jar=&quot;$&#123;shaded_mapreduce&#125;&quot;  if [ &quot;$&#123;INTERNAL_CLASSPATH&#125;&quot; != &quot;true&quot; ] &amp;&amp; [ -f &quot;$&#123;shaded_jar&#125;&quot; ]; then    echo -n &quot;$&#123;shaded_jar&#125;&quot;    for f in &quot;$&#123;HBASE_HOME&#125;&quot;/lib/client-facing-thirdparty/*.jar; do      if [[ ! &quot;$&#123;f&#125;&quot; =~ ^.*/htrace-core-3.*\.jar$ ]] &amp;&amp; \         [ &quot;$&#123;f&#125;&quot; != &quot;htrace-core.jar$&quot; ] &amp;&amp; \         [[ ! &quot;$&#123;f&#125;&quot; =~ ^.*/slf4j-log4j.*$ ]]; then        echo -n &quot;:$&#123;f&#125;&quot;      fi    done    echo &quot;&quot;    exit 0  fi  CLASS=&#x27;org.apache.hadoop.hbase.util.MapreduceDependencyClasspathTool&#x27;elif [ &quot;$COMMAND&quot; = &quot;classpath&quot; ] ; then  echo &quot;$CLASSPATH&quot;  exit 0elif [ &quot;$COMMAND&quot; = &quot;pe&quot; ] ; then  CLASS=&#x27;org.apache.hadoop.hbase.PerformanceEvaluation&#x27;  HBASE_OPTS=&quot;$HBASE_OPTS $HBASE_PE_OPTS&quot;elif [ &quot;$COMMAND&quot; = &quot;ltt&quot; ] ; then  CLASS=&#x27;org.apache.hadoop.hbase.util.LoadTestTool&#x27;  HBASE_OPTS=&quot;$HBASE_OPTS $HBASE_LTT_OPTS&quot;elif [ &quot;$COMMAND&quot; = &quot;canary&quot; ] ; then  CLASS=&#x27;org.apache.hadoop.hbase.tool.CanaryTool&#x27;  HBASE_OPTS=&quot;$HBASE_OPTS $HBASE_CANARY_OPTS&quot;elif [ &quot;$COMMAND&quot; = &quot;version&quot; ] ; then  CLASS=&#x27;org.apache.hadoop.hbase.util.VersionInfo&#x27;elif [ &quot;$COMMAND&quot; = &quot;regionsplitter&quot; ] ; then  CLASS=&#x27;org.apache.hadoop.hbase.util.RegionSplitter&#x27;elif [ &quot;$COMMAND&quot; = &quot;rowcounter&quot; ] ; then  CLASS=&#x27;org.apache.hadoop.hbase.mapreduce.RowCounter&#x27;elif [ &quot;$COMMAND&quot; = &quot;cellcounter&quot; ] ; then  CLASS=&#x27;org.apache.hadoop.hbase.mapreduce.CellCounter&#x27;elif [ &quot;$COMMAND&quot; = &quot;pre-upgrade&quot; ] ; then  CLASS=&#x27;org.apache.hadoop.hbase.tool.PreUpgradeValidator&#x27;elif [ &quot;$COMMAND&quot; = &quot;completebulkload&quot; ] ; then  CLASS=&#x27;org.apache.hadoop.hbase.tool.BulkLoadHFilesTool&#x27;elif [ &quot;$COMMAND&quot; = &quot;hbtop&quot; ] ; then  CLASS=&#x27;org.apache.hadoop.hbase.hbtop.HBTop&#x27;  if [ -n &quot;$&#123;shaded_jar&#125;&quot; ] ; then    for f in &quot;$&#123;HBASE_HOME&#125;&quot;/lib/hbase-hbtop*.jar; do      if [ -f &quot;$&#123;f&#125;&quot; ]; then        CLASSPATH=&quot;$&#123;CLASSPATH&#125;:$&#123;f&#125;&quot;        break      fi    done    for f in &quot;$&#123;HBASE_HOME&#125;&quot;/lib/commons-lang3*.jar; do      if [ -f &quot;$&#123;f&#125;&quot; ]; then        CLASSPATH=&quot;$&#123;CLASSPATH&#125;:$&#123;f&#125;&quot;        break      fi    done  fi  if [ -f &quot;$&#123;HBASE_HOME&#125;/conf/log4j-hbtop.properties&quot; ] ; then    HBASE_HBTOP_OPTS=&quot;$&#123;HBASE_HBTOP_OPTS&#125; -Dlog4j.configuration=file:$&#123;HBASE_HOME&#125;/conf/log4j-hbtop.properties&quot;  fi  HBASE_OPTS=&quot;$&#123;HBASE_OPTS&#125; $&#123;HBASE_HBTOP_OPTS&#125;&quot;else  CLASS=$COMMANDfi<span class="hljs-meta">#</span><span class="bash"> Have JVM dump heap <span class="hljs-keyword">if</span> we run out of memory.  Files will be <span class="hljs-string">&#x27;launch directory&#x27;</span></span><span class="hljs-meta">#</span><span class="bash"> and are named like the following: java_pid21612.hprof. Apparently it doesn<span class="hljs-string">&#x27;t</span></span><span class="hljs-meta">#</span><span class="bash"> <span class="hljs-string">&#x27;cost&#x27;</span> to have this flag enabled. Its a 1.6 flag only. See:</span><span class="hljs-meta">#</span><span class="bash"> http://blogs.sun.com/alanb/entry/outofmemoryerror_looks_a_bit_better</span>HBASE_OPTS=&quot;$HBASE_OPTS -Dhbase.log.dir=$HBASE_LOG_DIR&quot;HBASE_OPTS=&quot;$HBASE_OPTS -Dhbase.log.file=$HBASE_LOGFILE&quot;HBASE_OPTS=&quot;$HBASE_OPTS -Dhbase.home.dir=$HBASE_HOME&quot;HBASE_OPTS=&quot;$HBASE_OPTS -Dhbase.id.str=$HBASE_IDENT_STRING&quot;HBASE_OPTS=&quot;$HBASE_OPTS -Dhbase.root.logger=$&#123;HBASE_ROOT_LOGGER:-INFO,console&#125;&quot;if [ &quot;x$JAVA_LIBRARY_PATH&quot; != &quot;x&quot; ]; then  HBASE_OPTS=&quot;$HBASE_OPTS -Djava.library.path=$JAVA_LIBRARY_PATH&quot;  export LD_LIBRARY_PATH=&quot;$LD_LIBRARY_PATH:$JAVA_LIBRARY_PATH&quot;fi<span class="hljs-meta">#</span><span class="bash"> 仅在 master 和 regionserver 上启用安全日志记录</span>if [ &quot;$COMMAND&quot; = &quot;master&quot; ] || [ &quot;$COMMAND&quot; = &quot;regionserver&quot; ]; then  HBASE_OPTS=&quot;$HBASE_OPTS -Dhbase.security.logger=$&#123;HBASE_SECURITY_LOGGER:-INFO,RFAS&#125;&quot;else  HBASE_OPTS=&quot;$HBASE_OPTS -Dhbase.security.logger=$&#123;HBASE_SECURITY_LOGGER:-INFO,NullAppender&#125;&quot;fiHEAP_SETTINGS=&quot;$JAVA_HEAP_MAX $JAVA_OFFHEAP_MAX&quot;<span class="hljs-meta">#</span><span class="bash"> 现在，如果我们正在运行命令，则意味着我们需要记录</span>for f in $&#123;HBASE_HOME&#125;/lib/client-facing-thirdparty/slf4j-log4j*.jar; do  if [ -f &quot;$&#123;f&#125;&quot; ]; then    CLASSPATH=&quot;$&#123;CLASSPATH&#125;:$&#123;f&#125;&quot;    break  fidone<span class="hljs-meta">#</span><span class="bash"> 除非设置了 HBASE_NOEXEC，否则执行</span>export CLASSPATHif [ &quot;$&#123;DEBUG&#125;&quot; = &quot;true&quot; ]; then  echo &quot;classpath=$&#123;CLASSPATH&#125;&quot; &gt;&amp;2  HBASE_OPTS=&quot;$&#123;HBASE_OPTS&#125; -Xdiag&quot;fiif [ &quot;$&#123;HBASE_NOEXEC&#125;&quot; != &quot;&quot; ]; then  &quot;$JAVA&quot; -Dproc_$COMMAND -XX:OnOutOfMemoryError=&quot;kill -9 %p&quot; $HEAP_SETTINGS $HBASE_OPTS $CLASS &quot;$@&quot;else  export JVM_PID=&quot;$$&quot;  exec &quot;$JAVA&quot; -Dproc_$COMMAND -XX:OnOutOfMemoryError=&quot;kill -9 %p&quot; $HEAP_SETTINGS $HBASE_OPTS $CLASS &quot;$@&quot;fi</code></pre><hr><h2 id="停止"><a href="#停止" class="headerlink" title="停止"></a>停止</h2><h3 id="stop-hbase-sh"><a href="#stop-hbase-sh" class="headerlink" title="stop-hbase.sh"></a>stop-hbase.sh</h3><p>停止 hadoop hbase 守护程序，在主节点上运行以停止整个 HBase 服务。</p><pre><code class="hljs shell"><span class="hljs-meta">#</span><span class="bash"> 仿照 <span class="hljs-variable">$HADOOP_HOME</span>/bin/stop-hbase.sh.</span>bin=`dirname &quot;$&#123;BASH_SOURCE-$0&#125;&quot;`bin=`cd &quot;$bin&quot;&gt;/dev/null; pwd`<span class="hljs-meta">#</span><span class="bash"> 加载环境变量和参数</span>. &quot;$bin&quot;/hbase-config.sh. &quot;$bin&quot;/hbase-common.sh<span class="hljs-meta">#</span><span class="bash"> 停止命令需要的一些参数</span>if [ &quot;$HBASE_LOG_DIR&quot; = &quot;&quot; ]; then  export HBASE_LOG_DIR=&quot;$HBASE_HOME/logs&quot;fimkdir -p &quot;$HBASE_LOG_DIR&quot;if [ &quot;$HBASE_IDENT_STRING&quot; = &quot;&quot; ]; then  export HBASE_IDENT_STRING=&quot;$USER&quot;fiexport HBASE_LOG_PREFIX=hbase-$HBASE_IDENT_STRING-master-$HOSTNAMEexport HBASE_LOGFILE=$HBASE_LOG_PREFIX.loglogout=$HBASE_LOG_DIR/$HBASE_LOG_PREFIX.out  loglog=&quot;$&#123;HBASE_LOG_DIR&#125;/$&#123;HBASE_LOGFILE&#125;&quot;pid=$&#123;HBASE_PID_DIR:-/tmp&#125;/hbase-$HBASE_IDENT_STRING-master.pid<span class="hljs-meta">#</span><span class="bash"> 如果 HBase 的相关进程号文件存在，则调用 <span class="hljs-string">&quot;<span class="hljs-variable">$HBASE_HOME</span>&quot;</span>/bin/hbase 停止服务，并记录日志，停止后删除进程号文件，见附 4</span>if [[ -e $pid ]]; then  echo -n stopping hbase  echo &quot;`date` Stopping hbase (via master)&quot; &gt;&gt; $loglog  nohup nice -n $&#123;HBASE_NICENESS:-0&#125; &quot;$HBASE_HOME&quot;/bin/hbase \     --config &quot;$&#123;HBASE_CONF_DIR&#125;&quot; \     master stop &quot;$@&quot; &gt; &quot;$logout&quot; 2&gt;&amp;1 &lt; /dev/null &amp;  waitForProcessEnd `cat $pid` &#x27;stop-master-command&#x27;  rm -f $pidelse  echo no hbase master foundfi<span class="hljs-meta">#</span><span class="bash"> 单机模式下停止由 HBase 管理的 Zookeeper 服务，即 HQuorumPeer 进程</span>distMode=`$bin/hbase --config &quot;$HBASE_CONF_DIR&quot; org.apache.hadoop.hbase.util.HBaseConfTool hbase.cluster.distributed | head -n 1`if [ &quot;$distMode&quot; == &#x27;true&#x27; ] then  &quot;$bin&quot;/hbase-daemons.sh --config &quot;$&#123;HBASE_CONF_DIR&#125;&quot; stop zookeeperfi</code></pre><h4 id="附-4"><a href="#附-4" class="headerlink" title="附 4"></a>附 4</h4><p><code>$bin/hbase</code>接收到 master stop 参数，并经过脚本识别后调用 HMaster 类，进行停止。省略了从 HMaster 到 HMasterCommandLine 的传参过程，前文已经描述过，这里直接从 HMasterCommandLine 中的 stopMaster 方法开始分析。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">HMasterCommandLine</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">ServerCommandLine</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">run</span><span class="hljs-params">(String args[])</span> <span class="hljs-keyword">throws</span> Exception </span>&#123;        ……                <span class="hljs-keyword">if</span> (<span class="hljs-string">&quot;start&quot;</span>.equals(command)) &#123;          <span class="hljs-keyword">return</span> startMaster();        &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (<span class="hljs-string">&quot;stop&quot;</span>.equals(command)) &#123;          <span class="hljs-comment">// 匹配到 stop 的指令</span>          <span class="hljs-keyword">return</span> stopMaster();        &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (<span class="hljs-string">&quot;clear&quot;</span>.equals(command)) &#123;          <span class="hljs-keyword">return</span> (ZNodeClearer.clear(getConf()) ? <span class="hljs-number">0</span> : <span class="hljs-number">1</span>);        &#125; <span class="hljs-keyword">else</span> &#123;          usage(<span class="hljs-string">&quot;Invalid command: &quot;</span> + command);          <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;        &#125;    &#125;        <span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">int</span> <span class="hljs-title">stopMaster</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-comment">// 获取配置文件</span>        Configuration conf = getConf();        <span class="hljs-comment">// 客户端请求失败不再重试</span>        conf.setInt(HConstants.HBASE_CLIENT_RETRIES_NUMBER, <span class="hljs-number">0</span>);        <span class="hljs-comment">// 此处 createConnection 方法通过反射获取一个新的 connection 实例</span>        <span class="hljs-keyword">try</span> (Connection connection = ConnectionFactory.createConnection(conf)) &#123;          <span class="hljs-comment">// 再经过 connection 获得 Admin 实例，Admin 是 HBase 用来管理的 API</span>          <span class="hljs-keyword">try</span> (Admin admin = connection.getAdmin()) &#123;            admin.shutdown();          &#125; <span class="hljs-keyword">catch</span> (Throwable t) &#123;            LOG.error(<span class="hljs-string">&quot;Failed to stop master&quot;</span>, t);            <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;          &#125;        &#125; <span class="hljs-keyword">catch</span> (MasterNotRunningException e) &#123;          LOG.error(<span class="hljs-string">&quot;Master not running&quot;</span>);          <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;        &#125; <span class="hljs-keyword">catch</span> (ZooKeeperConnectionException e) &#123;          LOG.error(<span class="hljs-string">&quot;ZooKeeper not available&quot;</span>);          <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;        &#125; <span class="hljs-keyword">catch</span> (IOException e) &#123;          LOG.error(<span class="hljs-string">&quot;Got IOException: &quot;</span> +e.getMessage(), e);          <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;        &#125;        <span class="hljs-comment">// 只有当正确停止后，返回 0</span>        <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;    &#125;&#125;</code></pre><p>源码中看到 shutdown 方法和 ShutdownRequest 类等等都是报红的，这是因为 HBase 的某些类和方法是由 protobuf 之类的工具生成的。变量 master 是接口 MasterKeepAliveConnection 的实例，该接口有两个实现类：在 ConnectionImplementation 类中 getKeepAliveMasterService 方法直接返回的内部类 MasterKeepAliveConnection 以及 ShortCircuitMasterConnection。ShortCircuitMasterConnection 是与本地主机通信时可以绕过RPC层（串行化，反序列化，网络等）的短路连接类。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">HBaseAdmin</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Admin</span> </span>&#123;    <span class="hljs-keyword">protected</span> MasterKeepAliveConnection master;    ……        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">synchronized</span> <span class="hljs-keyword">void</span> <span class="hljs-title">shutdown</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> IOException </span>&#123;        executeCallable(<span class="hljs-keyword">new</span> MasterCallable&lt;Void&gt;(getConnection(), getRpcControllerFactory()) &#123;          <span class="hljs-meta">@Override</span>          <span class="hljs-function"><span class="hljs-keyword">protected</span> Void <span class="hljs-title">rpcCall</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> Exception </span>&#123;            <span class="hljs-comment">// 设置请求的优先级为高优先级</span>            setPriority(HConstants.HIGH_QOS);            master.shutdown(getRpcController(), ShutdownRequest.newBuilder().build());            <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;          &#125;        &#125;);      &#125;&#125;</code></pre><p>这里调用分析的是 getKeepAliveMasterService 方法返回的内部类，ShortCircuitMasterConnection 类中的 shutdown 方法也是类似的，通过 MasterProtos 最终调用至实现了 MasterService.BlockingInterface 接口的 MasterRpcServices 类。</p><pre><code class="hljs java"><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ConnectionImplementation</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">ClusterConnection</span>, <span class="hljs-title">Closeable</span> </span>&#123;    ……        <span class="hljs-function"><span class="hljs-keyword">private</span> MasterKeepAliveConnection <span class="hljs-title">getKeepAliveMasterService</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> IOException </span>&#123;        ……        <span class="hljs-comment">// Ugly delegation just so we can add in a Close method.</span>        <span class="hljs-keyword">final</span> MasterProtos.MasterService.BlockingInterface stub = <span class="hljs-keyword">this</span>.masterServiceState.stub;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> MasterKeepAliveConnection() &#123;          MasterServiceState mss = masterServiceState;          ……                    <span class="hljs-meta">@Override</span>          <span class="hljs-keyword">public</span> MasterProtos.<span class="hljs-function">ShutdownResponse <span class="hljs-title">shutdown</span><span class="hljs-params">(RpcController controller,</span></span><span class="hljs-function"><span class="hljs-params">              MasterProtos.ShutdownRequest request)</span> <span class="hljs-keyword">throws</span> ServiceException </span>&#123;            <span class="hljs-keyword">return</span> stub.shutdown(controller, request);          &#125;        &#125;      &#125;&#125;</code></pre><p>可以看到，在 MasterRpcServices 中，通过实例化的 HMaster 对象，调用的是 shutdown 方法来进行停止。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">MasterRpcServices</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">RSRpcServices</span></span><span class="hljs-class">      <span class="hljs-keyword">implements</span> <span class="hljs-title">MasterService</span>.<span class="hljs-title">BlockingInterface</span>, <span class="hljs-title">RegionServerStatusService</span>.<span class="hljs-title">BlockingInterface</span>,</span><span class="hljs-class">        <span class="hljs-title">LockService</span>.<span class="hljs-title">BlockingInterface</span>, <span class="hljs-title">HbckService</span>.<span class="hljs-title">BlockingInterface</span> </span>&#123;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> HMaster master;    ……        <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> ShutdownResponse <span class="hljs-title">shutdown</span><span class="hljs-params">(RpcController controller,</span></span><span class="hljs-function"><span class="hljs-params">          ShutdownRequest request)</span> <span class="hljs-keyword">throws</span> ServiceException </span>&#123;        LOG.info(master.getClientIdAuditPrefix() + <span class="hljs-string">&quot; shutdown&quot;</span>);        <span class="hljs-keyword">try</span> &#123;          master.shutdown();        &#125; <span class="hljs-keyword">catch</span> (IOException e) &#123;          LOG.error(<span class="hljs-string">&quot;Exception occurred in HMaster.shutdown()&quot;</span>, e);          <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> ServiceException(e);        &#125;        <span class="hljs-keyword">return</span> ShutdownResponse.newBuilder().build();      &#125;&#125;</code></pre><p>HMaster 会先停止所有的 HRegionServer 服务，然后再停止自身。将 ServerManager 的状态设置为关闭后，RegionServer 将注意到状态的变化，并开始自行关闭，等最后一个 RegionServer 退出后，HMaster 即可关闭。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">HMaster</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">HRegionServer</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">MasterServices</span> </span>&#123;    ……        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">shutdown</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> IOException </span>&#123;        <span class="hljs-keyword">if</span> (cpHost != <span class="hljs-keyword">null</span>) &#123;          cpHost.preShutdown();        &#125;            <span class="hljs-comment">// 告知 serverManager 关闭集群，serverManager 是用于管理 RegionServer 的</span>        <span class="hljs-keyword">if</span> (<span class="hljs-keyword">this</span>.serverManager != <span class="hljs-keyword">null</span>) &#123;          <span class="hljs-keyword">this</span>.serverManager.shutdownCluster();        &#125;        <span class="hljs-comment">// clusterStatusTracker 是用于在 Zookeeper 中对集群设置进行追踪的，这里通过删除 znode 来达到关闭集群的目的</span>        <span class="hljs-keyword">if</span> (<span class="hljs-keyword">this</span>.clusterStatusTracker != <span class="hljs-keyword">null</span>) &#123;          <span class="hljs-keyword">try</span> &#123;            <span class="hljs-keyword">this</span>.clusterStatusTracker.setClusterDown();          &#125; <span class="hljs-keyword">catch</span> (KeeperException e) &#123;            LOG.error(<span class="hljs-string">&quot;ZooKeeper exception trying to set cluster as down in ZK&quot;</span>, e);          &#125;        &#125;        <span class="hljs-comment">// Stop the procedure executor. Will stop any ongoing assign, unassign, server crash etc.,</span>        <span class="hljs-comment">// processing so we can go down.</span>        <span class="hljs-keyword">if</span> (<span class="hljs-keyword">this</span>.procedureExecutor != <span class="hljs-keyword">null</span>) &#123;          <span class="hljs-keyword">this</span>.procedureExecutor.stop();        &#125;        <span class="hljs-comment">// 关闭集群联机，将杀死可能正在运行的 RPC，如果不关闭连接，将不得不等待 RPC 超时</span>        <span class="hljs-keyword">if</span> (<span class="hljs-keyword">this</span>.clusterConnection != <span class="hljs-keyword">null</span>) &#123;          <span class="hljs-keyword">this</span>.clusterConnection.close();        &#125;    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">stop</span><span class="hljs-params">(String msg)</span> </span>&#123;        <span class="hljs-comment">// isStopped 方法继承自 HRegionServer，在其停止后会设置为 false</span>        <span class="hljs-keyword">if</span> (!isStopped()) &#123;          <span class="hljs-comment">// 调用父类 HRegionServer 的 stop 方法挨个进行停止</span>          <span class="hljs-keyword">super</span>.stop(msg);          <span class="hljs-keyword">if</span> (<span class="hljs-keyword">this</span>.activeMasterManager != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">this</span>.activeMasterManager.stop();          &#125;        &#125;    &#125;&#125;</code></pre><p>接上文，在 ServerManager 中调用 shutdownCluster 方法后又回到 HMaster 中，调用其自身的 stop 方法进行停止。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ServerManager</span> </span>&#123;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> MasterServices master;    ……        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">shutdownCluster</span><span class="hljs-params">()</span> </span>&#123;        String statusStr = <span class="hljs-string">&quot;Cluster shutdown requested of master=&quot;</span> + <span class="hljs-keyword">this</span>.master.getServerName();        LOG.info(statusStr);        <span class="hljs-comment">// 设置集群关闭状态</span>        <span class="hljs-keyword">this</span>.clusterShutdown.set(<span class="hljs-keyword">true</span>);        <span class="hljs-keyword">if</span> (onlineServers.isEmpty()) &#123;          <span class="hljs-comment">// 这里没有使用同步方法可能会导致停止两次，但这没啥问题</span>          master.stop(<span class="hljs-string">&quot;OnlineServer=0 right after cluster shutdown set&quot;</span>);        &#125;      &#125;&#125;</code></pre><p>HRegionServer 在接收到子类 HMaster 的 stop 方法调用后，开始停止服务。其 run 方法在开始运行时一直处于自旋状态，将 stopped 变量改为 true 后，会运行后面部分的代码，即停止相关服务。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">HRegionServer</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">HasThread</span> <span class="hljs-keyword">implements</span></span><span class="hljs-class">    <span class="hljs-title">RegionServerServices</span>, <span class="hljs-title">LastSequenceId</span>, <span class="hljs-title">ConfigurationObserver</span> </span>&#123;        <span class="hljs-keyword">private</span> <span class="hljs-keyword">volatile</span> <span class="hljs-keyword">boolean</span> stopped = <span class="hljs-keyword">false</span>;    ……        <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">stop</span><span class="hljs-params">(<span class="hljs-keyword">final</span> String msg)</span> </span>&#123;        stop(msg, <span class="hljs-keyword">false</span>, RpcServer.getRequestUser().orElse(<span class="hljs-keyword">null</span>));    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">stop</span><span class="hljs-params">(<span class="hljs-keyword">final</span> String msg, <span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> force, <span class="hljs-keyword">final</span> User user)</span> </span>&#123;        <span class="hljs-keyword">if</span> (!<span class="hljs-keyword">this</span>.stopped) &#123;          LOG.info(<span class="hljs-string">&quot;***** STOPPING region server &#x27;&quot;</span> + <span class="hljs-keyword">this</span> + <span class="hljs-string">&quot;&#x27; *****&quot;</span>);          <span class="hljs-keyword">if</span> (<span class="hljs-keyword">this</span>.rsHost != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-comment">// when forced via abort don&#x27;t allow CPs to override</span>            <span class="hljs-keyword">try</span> &#123;              <span class="hljs-keyword">this</span>.rsHost.preStop(msg, user);            &#125; <span class="hljs-keyword">catch</span> (IOException ioe) &#123;              <span class="hljs-keyword">if</span> (!force) &#123;                LOG.warn(<span class="hljs-string">&quot;The region server did not stop&quot;</span>, ioe);                <span class="hljs-keyword">return</span>;              &#125;              LOG.warn(<span class="hljs-string">&quot;Skipping coprocessor exception on preStop() due to forced shutdown&quot;</span>, ioe);            &#125;          &#125;          <span class="hljs-keyword">this</span>.stopped = <span class="hljs-keyword">true</span>;          LOG.info(<span class="hljs-string">&quot;STOPPED: &quot;</span> + msg);          <span class="hljs-comment">// Wakes run() if it is sleeping</span>          sleeper.skipSleepCycle();        &#125;      &#125;&#125;</code></pre><p>省略了后续相关服务停止以及 Zookeeper 清理等部分，至此，整个 HMaster 集群已经完全关闭。</p><hr><h2 id="配置文件"><a href="#配置文件" class="headerlink" title="配置文件"></a>配置文件</h2><h3 id="hbase-env-sh"><a href="#hbase-env-sh" class="headerlink" title="hbase-env.sh"></a>hbase-env.sh</h3><p>前面的一些脚本中有加载 hbase-env.sh 中的环境变量，这些变量都是给用户提供的可配置项。<br>它设置了 HBase 运行中的一些重要 JVM 参数，在对 HBase 进行调优时可能会用到。</p><p>文件格式是以<code>export 环境变量名=变量值</code>这种形式组织的</p><ul><li><p><code>JAVA_HOME</code> - JDK 路径，Java 1.8+</p></li><li><p><code>HBASE_CLASSPATH</code> - 额外的 Java CLASSPATH，可选项</p></li><li><p><code>HBASE_HEAPSIZE</code> - 使用的最大堆数量，默认为 JVM 默认值</p></li><li><p><code>HBASE_OFFHEAPSIZE</code> - 堆外内存</p></li><li><p><code>HBASE_OPTS</code> - 额外的 Java 运行时参数，默认为”-XX:+UseConcMarkSweepGC”，使用 CMS 收集器对年老代进行垃圾收集，CMS 收集器通过多线程并发进行垃圾回收，尽量减少垃圾收集造成的停顿</p></li><li><p><code>SERVER_GC_OPTS</code> - 可以为服务器端进程启用 Java 垃圾回收日志记录</p></li><li><p><code>CLIENT_GC_OPTS</code> - 为客户端进程启用Java垃圾回收日志记录</p></li><li><p>额外的运行时选项配置，包含 JMX 导出、启用主要 HBase 进程的远程 JDWP 调试等</p><ul><li> <code>HBASE_JMX_BASE</code></li><li> <code>HBASE_MASTER_OPTS</code></li><li> <code>HBASE_REGIONSERVER_OPTS</code></li><li> <code>HBASE_THRIFT_OPTS</code></li><li> <code>HBASE_ZOOKEEPER_OPTS</code></li></ul></li><li><p><code>HBASE_REGIONSERVERS</code> - RegionServer 服务运行节点</p></li><li><p><code>HBASE_REGIONSERVER_MLOCK</code> - 是否使所有区域服务器页面都映射为驻留在内存中</p></li><li><p><code>HBASE_REGIONSERVER_UID</code> - RegionServer 的用户 ID</p></li><li><p><code>HBASE_BACKUP_MASTERS</code> - 备用 Master 节点</p></li><li><p><code>HBASE_SSH_OPTS</code> - 额外的 ssh 选项</p></li><li><p><code>HBASE_LOG_DIR</code> - HBase 日志存储路径</p></li><li><p><code>HBASE_IDENT_STRING</code> - 标识 HBase 实例的字符串，默认为当前用户</p></li><li><p><code>HBASE_NICENESS</code> - 守护进程的调度优先级</p></li><li><p><code>HBASE_PID_DIR</code> - PID 文件的存储路径，默认是 /tmp，最好换个稳定的路径</p></li><li><p><code>HBASE_SLAVE_SLEEP</code> - 在从属命令之间休眠的秒数，默认情况下未设置</p></li><li><p><code>HBASE_MANAGES_ZK</code> - 是否启动 HBase 内嵌的 Zookeeper，一般使用集群的 Zookeeper</p></li><li><p><code>HBASE_ROOT_LOGGER</code> - HBase 日志级别</p></li><li><p><code>HBASE_DISABLE_HADOOP_CLASSPATH_LOOKUP</code> - HBase 启动时是否应包含 Hadoop 的库，默认值为 false，表示包含 Hadoop 的库</p></li></ul><h3 id="hbase-site-xml"><a href="#hbase-site-xml" class="headerlink" title="hbase-site.xml"></a>hbase-site.xml</h3><p>该文件配置项较多，此处仅列举一些常见集群配置项，更多参数请移步<a href="https://hbase.apache.org/book.html#config.files">官方文档</a>。<br>文件格式是以下面这种形式组织的：</p><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">property</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">name</span>&gt;</span>参数名称<span class="hljs-tag">&lt;/<span class="hljs-name">name</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">value</span>&gt;</span>参数值<span class="hljs-tag">&lt;/<span class="hljs-name">value</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">property</span>&gt;</span></code></pre><ul><li><p><code>hbase.tmp.dir</code> - 本地文件系统上的临时目录，默认为 /tmp/hbase-${user.name}，最好更改此路径为一个更稳定的，否则数据容易丢失。</p></li><li><p><code>hbase.rootdir</code> - RegionServers 共享目录，也是 HBase 持久化存储的目录，支持 HDFS 的存储。默认情况下写到 ${hbase.tmp.dir}/hbase 目录，所以最好更改此目录，否则机器重新启动后，所有的数据将丢失。</p></li><li><p><code>hbase.cluster.distributed</code> - 集群启动模式，单机模式为 false（默认值），集群模式为 true。如果为 false，将在同一个 JVM 中运行 HBase 以及 Zookeeper 的进程。</p></li><li><p><code>hbase.zookeeper.quorum</code> - Zookeeper 服务器列表，以逗号分隔，默认是 127.0.0.1。如果在 hbase-env.sh 中配置了<code>export HBASE_MANAGES_ZK=true</code>，那么该 Zookeeper服务将由 HBase 进行管理，作为 HBase 启动/停止的一部分，最好是部署独立的 Zookeeper 集群。</p></li><li><p><code>hbase.zookeeper.property.dataDir</code> - Zookeeper 配置文件 zoo.cfg 中的属性，也是快照存储的目录，只有在使用外置的 Zookeeper 集群服务时有效。</p></li><li><p><code>hbase.master.port</code> - Master 的内部端口号，默认是 16000。</p></li><li><p><code>hbase.master.info.port</code> - Master 的 Web UI 端口号，默认是 16010，如果不想运行 UI 实例，设置为 -1 即可。</p></li><li><p><code>hbase.regionserver.port</code> - RegionServer 的内部端口号，默认是 16020。</p></li><li><p><code>hbase.regionserver.info.port</code> - RegionServer 的 Web UI 端口号，默认是 16030，如果不想运行 UI 实例，设置为 -1 即可。</p></li><li><p><code>hbase.regionserver.handler.count</code> - 在 RegionServer 上的 RPC 监听器实例计数，Master 也使用相同的属性，太多的 handlers 可能会适得其反。将其设置为 CPU 的倍数，如果大多数情况下是只读的，那么接近 CPU 数更好，从 CPU 数的两倍开始进行调整，默认为 30。</p></li><li><p><code>hbase.regionserver.global.memstore.size</code> - 在阻止新的更新并强制刷新之前，RegionServer 中所有内存的最大值，默认为堆的 0.4。更新被阻塞并强制刷新，知道一个 RegionServer 中所有内存的大小达到 hbase.regionserver.global.memstore.size.lower.limit，配置中的默认值保留为空。</p></li><li><p><code>hbase.regionserver.global.memstore.size.lower.limit</code> - 默认是 hbase.regionserver.global.memstore.size 的 95%，配置中的默认值保留为空。</p></li><li><p><code>zookeeper.znode.parent</code> - Zookeeper 中 HBase 的根 Znode 节点，默认是 /hbase。</p></li><li><p><code>dfs.client.read.shortcircuit</code> - 设置为 true，则启用本地短路读，默认是 false。</p></li><li><p><code>hbase.column.max.version</code> - 新的列簇将使用此值作为默认的版本数，默认是 1。</p></li><li><p><code>hbase.coprocessor.master.classes</code> - 以逗号分隔的协处理器列表，在 HMaster 上加载的 MasterObserver 协处理器，指定完整的类名。</p></li><li><p><code>hbase.coprocessor.region.classes</code> - 以逗号分隔的协处理器列表，在所有的表上加载，指定完整的类名，也可通过 HTableDescriptor 或 HBase shell 按需加载。</p></li><li><p><code>hbase.coprocessor.user.region.classes</code> - 从配置中加载用户表的系统默认协处理器，用户可以继承 HBase 的 RegionCoprocessor 实现自己需要的逻辑部分，指定完整的类名。</p></li><li><p><code>hbase.coprocessor.user.enabled</code> - 启用/禁用加载用户的协处理器加载，默认为 true。</p></li><li><p><code>hbase.coprocessor.enabled</code> - 启用/禁用加载所有的协处理器加载，默认为 true。</p></li></ul>]]></content>
    
    
    <categories>
      
      <category>分布式系统</category>
      
      <category>分布式存储</category>
      
      <category>HBase</category>
      
    </categories>
    
    
    <tags>
      
      <tag>HBase</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Spark概述</title>
    <link href="/2021/03/17/Spark%E6%A6%82%E8%BF%B0/"/>
    <url>/2021/03/17/Spark%E6%A6%82%E8%BF%B0/</url>
    
    <content type="html"><![CDATA[<h1 id="Spark概述"><a href="#Spark概述" class="headerlink" title="Spark概述"></a>Spark概述</h1><h2 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h2><h3 id="RDD"><a href="#RDD" class="headerlink" title="RDD"></a>RDD</h3><p>弹性分布式数据集 （ Resilient Distrbuted Dataset），本质是一种分布式的内存抽象，表示一个只读的数据分区（Partition）集合。</p><h3 id="DAG"><a href="#DAG" class="headerlink" title="DAG"></a>DAG</h3><p>有向无环图（Directed Acycle graph），Spark 使用 DAG 来反映各 RDD 间的依赖或血缘关系。</p><h3 id="Partition"><a href="#Partition" class="headerlink" title="Partition"></a>Partition</h3><p>数据分区，即一个 RDD 的数据可以划分为多少个分区，Spark 根据 Partition 的数量来确定 Task 的数量。</p><h3 id="NarrowDependency"><a href="#NarrowDependency" class="headerlink" title="NarrowDependency"></a>NarrowDependency</h3><p>窄依赖，即子 RDD 依赖于父 RDD 中固定的 Partition。分为 OneToOneDependency 和 RangeDependency 两种。</p><h3 id="ShuffleDependency"><a href="#ShuffleDependency" class="headerlink" title="ShuffleDependency"></a>ShuffleDependency</h3><p> 宽依赖，即子 RDD 对父 RDD 中的所有 Partition 都可能产生依赖。</p><h3 id="Job"><a href="#Job" class="headerlink" title="Job"></a>Job</h3><p>用户提交的作业。当 RDD 及 DAG 被提交给 DAGScheduler 后，DAGScheduler 会将所有 RDD 中的转换及动作视为一个 Job（由一到多个 Task 组成）。</p><h3 id="Stage"><a href="#Stage" class="headerlink" title="Stage"></a>Stage</h3><p>Job 的执行阶段。DAGScheduler 按照 ShuffleDependency 作为 Stage 的划分节点对 RDD 的 DAG 进行 Stage 划分。一个 Job 可能被分为一到多个 Stage，主要为 ShuffleMapStage 和 ResultStage 两种。</p><h3 id="Task"><a href="#Task" class="headerlink" title="Task"></a>Task</h3><p>具体执行任务。一个 Job 在每个 Stage 内都会按照 RDD 的 Partition 数量，创建多个 Task。Task 分为 ShuffleMapTask（ShuffleMapStage） 和 ResultTask（ResultStage）两种，对应 Hadoop 中的 Map 任务和 Reduce 任务。</p><h3 id="Shuffle"><a href="#Shuffle" class="headerlink" title="Shuffle"></a>Shuffle</h3><p>所有 MapReduce 计算框架的核心执行阶段，用于打通 Map 任务的输出和 Reduce 任务的输入，Map 任务的中间输出结果按照指定的分区策略（例如按照 key 值哈希）分配给处理某一分区的 Reduce 任务。</p><h2 id="基本架构"><a href="#基本架构" class="headerlink" title="基本架构"></a>基本架构</h2><p>从集群部署的角度来看，Spark 由集群管理器（Cluster Manager）、工作节点（Worker）、执行器（Executor）、驱动器（Driver）、应用程序（Application）等部分组成，如下图所示。</p><p><img src="1.png" alt="Spark基本架构"></p><h3 id="Cluster-Manager"><a href="#Cluster-Manager" class="headerlink" title="Cluster Manager"></a>Cluster Manager</h3><p>Spark 的集群管理器，主要负责对整个集群资源的分配与管理。在 YARN 模式下为 ResourceManager；在 Mesos 模式下为 MesosMaster；在 Standlone 模式下为 Master。</p><p>分配的资源属于一级分配，将各个 Worker 上的内存、CPU 等资源分配给 Application，但是并不负责对 Executor 的资源分配。</p><h3 id="Worker"><a href="#Worker" class="headerlink" title="Worker"></a>Worker</h3><p>Spark 的工作节点，YARN 下由 NodeManager 替代。主要负责申请资源并创建 Executor，同时给其分配资源。在 Standalone 模式下，Master 将 Worker 上的内存、CPU 及 Executor 等资源分配给 Application 后，将命令 Worker 启动 CoarseGrainedExecutorBackend 进程（创建 Executor 实例）。</p><h3 id="Executor"><a href="#Executor" class="headerlink" title="Executor"></a>Executor</h3><p>执行计算任务的一线组件，主要负责任务的执行及与 Worker、Driver 的信息同步。</p><h3 id="Driver"><a href="#Driver" class="headerlink" title="Driver"></a>Driver</h3><p>Application 的驱动程序，接受用户的 SQL 请求并进行解析。Driver 可以运行在 Application 中，也可以由 Application 提交给 Cluster Manager 并由其安排 Worker 运行。</p><h3 id="Application"><a href="#Application" class="headerlink" title="Application"></a>Application</h3><p>表示用户的应用程序，通过 Spark API 进行 RDD 的转换和 DAG 的构建，并通过 Driver 将 Application 注册到 Cluster Manager。</p><h2 id="模块划分"><a href="#模块划分" class="headerlink" title="模块划分"></a>模块划分</h2><p>整个 Spark 主要由 Spark Core、Spark SQL、Spark Streaming、Grapx、MLlib 组成，其核心引擎部分是 Spark Core，Spark SQL 部分则支持了 SQL 及 Hive，本文也着重分析的这两部分。</p><hr><h3 id="Spark-Core"><a href="#Spark-Core" class="headerlink" title="Spark Core"></a>Spark Core</h3><h4 id="基础设施"><a href="#基础设施" class="headerlink" title="基础设施"></a>基础设施</h4><p>包括 Spark 的配置（SparkConf）、Spark 内置的 RPC 框架（早期使用的是 Akka）、事件总线（ListenerBus）、度量系统。</p><ol><li>SparkConf 管理 Spark 应用程序的各种配置信息。</li><li>RPC 框架使用 Netty 实现，有同步和异步之分。</li><li>事件总线是 SparkContext 内部各个组件间使用事件 - 监听器模式异步调用的实现。</li><li>度量系统由 Spark 中的多种度量源（Source）和多种度量输出（Sink）构成，完成对整个 Spark 集群中各个组件运行期状态的监控。</li></ol><h4 id="SparkContext"><a href="#SparkContext" class="headerlink" title="SparkContext"></a>SparkContext</h4><p>在正式提交应用程序之前，首先需要初始化 SparkContext。其隐藏了网络通信、分布式部署、消息通信、存储体系、计算引擎、度量系统、文件服务、WebUI 等内容。</p><h4 id="SparkEnv"><a href="#SparkEnv" class="headerlink" title="SparkEnv"></a>SparkEnv</h4><p>Spark 的执行环境，是 Spark 中 Task 运行所必需的组件。内部封装了 RPC 环境（RpcEnv）、序列化管理器、广播管理器（BroadcastManager）、Map 任务输出跟踪器（MapOutputTracker）、存储体系、度量系统（MetricSystem）、输出提交协调器（OutputCommitCoordinator）等 Task 运行所需的各种组件。</p><h4 id="存储体系"><a href="#存储体系" class="headerlink" title="存储体系"></a>存储体系</h4><p>Spark 优先考虑使用各节点的内存作为存储，当内存不足时才会考虑使用磁盘，极大地减少了磁盘 I/O。Spark 的内存空间还提供了 Tungsten 的实现，直接操作操作系统的内存。</p><h4 id="调度系统"><a href="#调度系统" class="headerlink" title="调度系统"></a>调度系统</h4><p>主要由 DAGScheduler 和 TaskScheduler 组成，都内置在 SparkContext 中。DAGSCheduler 负责创建 Job、将 DAG 中的 RDD 划分到不同的 Stage、给 Stage 创建对应的 Task、批量提交 Task 等功能。TaskScheduler 负责按照 FIFO 或者 FAIR 等调度算法对批量 Task 进行调度、给 Task 分配资源；将 Task 发送到 Executor 上由其执行。</p><h4 id="计算引擎"><a href="#计算引擎" class="headerlink" title="计算引擎"></a>计算引擎</h4><p>由内存管理器（MemoryManager）、Tungsten、任务内存管理器（TaskMemoryManager）、Task、外部排序器（ExternalSorter）、Shuffle 管理器（ShuffleManager）等组成。</p><hr><h3 id="Spark-SQL"><a href="#Spark-SQL" class="headerlink" title="Spark SQL"></a>Spark SQL</h3><h4 id="编译器-Parser"><a href="#编译器-Parser" class="headerlink" title="编译器 Parser"></a>编译器 Parser</h4><p>Spark SQL 采用 <strong>ANTLR4</strong> 作为 SQL 语法工具。它有两种遍历模式：监听器模式（Listener）和访问者模式（Visitor），Spark 主要采用的是后者，基于 ANTLR4 文件来生成词法分析器（SqlBaseLexer）、语法分析器（SqlBaseParser）和访问者类（SqlBaseVisitor 接口与 SqlBaseBaseVisitor 类）。</p><p>当面临开发新的语法支持时，首先改动 SqlBase.g4 文件，然后在 AstBuilder 等类中添加相应的访问逻辑，最后添加执行逻辑即可。</p><h4 id="逻辑计划"><a href="#逻辑计划" class="headerlink" title="逻辑计划"></a>逻辑计划</h4><p>在此阶段，SQL 语句转换为树结构形态的逻辑算子树，SQL 中包含的各种处理逻辑（过滤、裁剪等）和数据信息都会被整合在逻辑算子树的不同节点中。在实现层面被定义为 LogicalPlan 类。</p><p>从 SQL 语句经过 SparkSqlParser 解析生成 Unresolved LogicalPlan，到最后优化成为 Optimized LogicalPlan，再传递到下一个阶段用于物理执行计划的生成。</p><h4 id="物理计划"><a href="#物理计划" class="headerlink" title="物理计划"></a>物理计划</h4><p>这是 Spark SQL 整个查询过程处理流程的最后一步，与底层平台紧密相关。Spark SQL 会对生成的逻辑算子树进一步处理得到物理算子树，并将 LogicalPlan 节点及其所包含的各种信息映射成 Spark Core 计算模型的元素，如 RDD、Transformation 和 Action 等，其实现类为 SparkPlan。</p><hr><h2 id="References"><a href="#References" class="headerlink" title="References"></a>References</h2><ul><li><a href="http://spark.apache.org/docs/latest/">Spark官方文档</a></li><li><a href="https://github.com/apache/spark">Spark源码</a></li><li><a href="https://book.douban.com/subject/30157181/">Spark内核设计的艺术</a></li><li><a href="https://book.douban.com/subject/30296615/">Spark SQL内核剖析</a></li></ul>]]></content>
    
    
    <categories>
      
      <category>分布式系统</category>
      
      <category>分布式计算</category>
      
      <category>Spark</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Spark</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>工厂模式</title>
    <link href="/2021/03/16/%E5%B7%A5%E5%8E%82%E6%A8%A1%E5%BC%8F/"/>
    <url>/2021/03/16/%E5%B7%A5%E5%8E%82%E6%A8%A1%E5%BC%8F/</url>
    
    <content type="html"><![CDATA[<h1 id="简单工厂模式"><a href="#简单工厂模式" class="headerlink" title="简单工厂模式"></a>简单工厂模式</h1><h2 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h2><p>为一系列应用程序定义标准化结构模型，但是允许各个应用程序定义自己的对象并提供其实例。即根据不同的条件创建不同的实例对象。</p><h2 id="目的"><a href="#目的" class="headerlink" title="目的"></a>目的</h2><p>定义用于创建对象的接口，但让子类确定要实例化的类，把类的实例化这一过程延迟到子类。</p><h2 id="代码实例"><a href="#代码实例" class="headerlink" title="代码实例"></a>代码实例</h2><pre><code class="hljs java"><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">DecodedImage</span> </span>&#123;    <span class="hljs-keyword">private</span> String image;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">DecodedImage</span><span class="hljs-params">(String image)</span> </span>&#123;        <span class="hljs-keyword">this</span>.image = image;    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> String <span class="hljs-title">toString</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> image + <span class="hljs-string">&quot; is decoded.&quot;</span>;    &#125;&#125;<span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">ImageReader</span> </span>&#123;    <span class="hljs-function">DecodedImage <span class="hljs-title">getDecodedImage</span><span class="hljs-params">()</span></span>;&#125;<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">GifReader</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">ImageReader</span> </span>&#123;    <span class="hljs-keyword">private</span> DecodedImage decodedImage;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">GifReader</span><span class="hljs-params">(String image)</span> </span>&#123;        <span class="hljs-keyword">this</span>.decodedImage = <span class="hljs-keyword">new</span> DecodedImage(image);    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> DecodedImage <span class="hljs-title">getDecodedImage</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> decodedImage;    &#125;&#125;<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">JpegReader</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">ImageReader</span> </span>&#123;    <span class="hljs-keyword">private</span> DecodedImage decodedImage;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">JpegReader</span><span class="hljs-params">(String image)</span> </span>&#123;        <span class="hljs-keyword">this</span>.decodedImage = <span class="hljs-keyword">new</span> DecodedImage(image);    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> DecodedImage <span class="hljs-title">getDecodedImage</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> decodedImage;    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">FactoryMethodDemo</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String[] args)</span> </span>&#123;        DecodedImage decodedImage;        ImageReader reader;        String image = <span class="hljs-string">&quot;test.gif&quot;</span>;        String format = image.substring(image.indexOf(<span class="hljs-string">&#x27;.&#x27;</span>) + <span class="hljs-number">1</span>);        <span class="hljs-keyword">switch</span> (format) &#123;            <span class="hljs-keyword">case</span> <span class="hljs-string">&quot;gif&quot;</span>:                reader = <span class="hljs-keyword">new</span> GifReader(image);                <span class="hljs-keyword">break</span>;            <span class="hljs-keyword">case</span> <span class="hljs-string">&quot;jpeg&quot;</span>:                reader = <span class="hljs-keyword">new</span> JpegReader(image);                <span class="hljs-keyword">break</span>;            <span class="hljs-keyword">default</span>:                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalArgumentException(format);        &#125;        decodedImage = reader.getDecodedImage();        System.out.println(decodedImage);    &#125;&#125;</code></pre><h1 id="抽象工厂模式"><a href="#抽象工厂模式" class="headerlink" title="抽象工厂模式"></a>抽象工厂模式</h1><h2 id="问题-1"><a href="#问题-1" class="headerlink" title="问题"></a>问题</h2><p>简单工厂模式中工厂类职责过重，即每个工厂只创建一类具体的对象，这将使得系统中工厂类过多引起混乱。</p><h2 id="目的-1"><a href="#目的-1" class="headerlink" title="目的"></a>目的</h2><p>提供一个用于创建相关对象的接口，而无需指定具体类，即上面简单工厂中基于条件 new 出的对象。</p><h2 id="代码实例-1"><a href="#代码实例-1" class="headerlink" title="代码实例"></a>代码实例</h2><pre><code class="hljs java"><span class="hljs-keyword">abstract</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">CPU</span> </span>&#123;&#125;<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">EmberCPU</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">CPU</span> </span>&#123;&#125;<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">EnginolaCPU</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">CPU</span> </span>&#123;&#125;<span class="hljs-keyword">abstract</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">MMU</span> </span>&#123;&#125;<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">EmberMMU</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">MMU</span> </span>&#123;&#125;<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">EnginolaMMU</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">MMU</span> </span>&#123;&#125;<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">EmberToolKit</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">AbstractFactory</span> </span>&#123;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> CPU <span class="hljs-title">createCPU</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> EmberCPU();    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> MMU <span class="hljs-title">createMMU</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> EmberMMU();    &#125;&#125;<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">EnginolaToolKit</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">AbstractFactory</span> </span>&#123;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> CPU <span class="hljs-title">createCPU</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> EnginolaCPU();    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> MMU <span class="hljs-title">createMMU</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> EnginolaMMU();    &#125;&#125;<span class="hljs-class"><span class="hljs-keyword">enum</span> <span class="hljs-title">Architecture</span> </span>&#123;    ENGINOLA, EMBER&#125;<span class="hljs-keyword">abstract</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">AbstractFactory</span> </span>&#123;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> EmberToolKit EMBER_TOOL_KIT = <span class="hljs-keyword">new</span> EmberToolKit();    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> EnginolaToolKit ENGINOLA_TOOL_KIT = <span class="hljs-keyword">new</span> EnginolaToolKit();    <span class="hljs-function"><span class="hljs-keyword">static</span> AbstractFactory <span class="hljs-title">getFactory</span><span class="hljs-params">(Architecture architecture)</span> </span>&#123;        AbstractFactory factory = <span class="hljs-keyword">null</span>;        <span class="hljs-keyword">switch</span> (architecture) &#123;            <span class="hljs-keyword">case</span> ENGINOLA:                factory = ENGINOLA_TOOL_KIT;                <span class="hljs-keyword">break</span>;            <span class="hljs-keyword">case</span> EMBER:                factory = EMBER_TOOL_KIT;                <span class="hljs-keyword">break</span>;            <span class="hljs-keyword">default</span>:                <span class="hljs-keyword">break</span>;        &#125;        <span class="hljs-keyword">return</span> factory;    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> CPU <span class="hljs-title">createCPU</span><span class="hljs-params">()</span></span>;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> MMU <span class="hljs-title">createMMU</span><span class="hljs-params">()</span></span>;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Client</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String[] args)</span> </span>&#123;        <span class="hljs-keyword">final</span> CPU cpu = AbstractFactory.getFactory(Architecture.EMBER).createCPU();        System.out.println(cpu.getClass().getSimpleName());        <span class="hljs-keyword">final</span> MMU mmu = AbstractFactory.getFactory(Architecture.ENGINOLA).createMMU();        System.out.println(mmu.getClass().getSimpleName());    &#125;&#125;</code></pre>]]></content>
    
    
    <categories>
      
      <category>设计模式</category>
      
      <category>创建型</category>
      
    </categories>
    
    
    <tags>
      
      <tag>设计模式</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>单例模式</title>
    <link href="/2021/03/16/%E5%8D%95%E4%BE%8B%E6%A8%A1%E5%BC%8F/"/>
    <url>/2021/03/16/%E5%8D%95%E4%BE%8B%E6%A8%A1%E5%BC%8F/</url>
    
    <content type="html"><![CDATA[<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>应用程序仅需要一个对象实例。此外，需要进行懒加载和全局访问。</p><h1 id="目的"><a href="#目的" class="headerlink" title="目的"></a>目的</h1><ol><li>确保这个类只有一个实例，并提供对这个类的全局访问方式。</li><li>首次使用时对该类初始化。</li></ol><h1 id="代码实例"><a href="#代码实例" class="headerlink" title="代码实例"></a>代码实例</h1><h2 id="懒汉"><a href="#懒汉" class="headerlink" title="懒汉"></a>懒汉</h2><h3 id="线程不安全"><a href="#线程不安全" class="headerlink" title="线程不安全"></a>线程不安全</h3><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Singleton</span> </span>&#123;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> Singleton instance;        <span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-title">Singleton</span><span class="hljs-params">()</span> </span>&#123;&#125;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Singleton <span class="hljs-title">getInstance</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">if</span> (instance == <span class="hljs-keyword">null</span>) &#123;            instance = <span class="hljs-keyword">new</span> Singleton();        &#125;        <span class="hljs-keyword">return</span> instance;    &#125;&#125;</code></pre><h3 id="线程安全"><a href="#线程安全" class="headerlink" title="线程安全"></a>线程安全</h3><p>使用 <strong>synchronized</strong> 关键字加锁，能够在多线程中工作，但效率较低。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Singleton</span> </span>&#123;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> Singleton instance;        <span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-title">Singleton</span> <span class="hljs-params">()</span></span>&#123;&#125;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">synchronized</span> Singleton <span class="hljs-title">getInstance</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">if</span> (instance == <span class="hljs-keyword">null</span>) &#123;            instance = <span class="hljs-keyword">new</span> Singleton();        &#125;        <span class="hljs-keyword">return</span> instance;    &#125;  &#125;</code></pre><h2 id="饿汉"><a href="#饿汉" class="headerlink" title="饿汉"></a>饿汉</h2><p>对象在类加载时就实例化，避免了多线程的同步问题，没有加锁，效率较高。但是在类加载时就初始化，浪费内存。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Singleton</span> </span>&#123;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> Singleton instance = <span class="hljs-keyword">new</span> Singleton();        <span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-title">Singleton</span> <span class="hljs-params">()</span></span>&#123;&#125;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Singleton <span class="hljs-title">getInstance</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> instance;    &#125;&#125;</code></pre><h2 id="双重校验锁"><a href="#双重校验锁" class="headerlink" title="双重校验锁"></a>双重校验锁</h2><p>采用双锁机制，线程安全，同时效率也比较高。<strong>volatile</strong> 关键字是 Java 虚拟机提供的轻量级同步机制，被其修饰的变量能够保证每个线程获取到该变量的最新值，从而避免出现脏读的现象。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Singleton</span> </span>&#123;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">volatile</span> <span class="hljs-keyword">static</span> Singleton singleton;        <span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-title">Singleton</span> <span class="hljs-params">()</span></span>&#123;&#125;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Singleton <span class="hljs-title">getSingleton</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">if</span> (singleton == <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">synchronized</span> (Singleton.class) &#123;                <span class="hljs-keyword">if</span> (singleton == <span class="hljs-keyword">null</span>) &#123;                    singleton = <span class="hljs-keyword">new</span> Singleton();                &#125;            &#125;        &#125;        <span class="hljs-keyword">return</span> singleton;    &#125;&#125;</code></pre><h2 id="登记式-静态内部类"><a href="#登记式-静态内部类" class="headerlink" title="登记式/静态内部类"></a>登记式/静态内部类</h2><p>和双重校验锁效果一样，但实现更简单。对静态域使用延迟初始化，应使用这种方式而不是双检锁。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Singleton</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-title">Singleton</span><span class="hljs-params">()</span> </span>&#123;&#125;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">SingletonHolder</span> </span>&#123;        <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> Singleton INSTANCE = <span class="hljs-keyword">new</span> Singleton();    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Singleton <span class="hljs-title">getInstance</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> SingletonHolder.INSTANCE;    &#125;&#125;</code></pre><h2 id="枚举"><a href="#枚举" class="headerlink" title="枚举"></a>枚举</h2><p>这是实现单例的最佳方法，更简洁，同时自动支持序列化机制，绝对防止多次实例化。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">enum</span> <span class="hljs-title">Singleton</span> </span>&#123;    INSTANCE;    <span class="hljs-comment">//...</span>&#125;</code></pre><h1 id="附"><a href="#附" class="headerlink" title="附"></a>附</h1><p>一般情况下，使用饿汉方式，只有在明确需要懒加载时会使用登记式、静态内部类方式，涉及到反序列化创建对象时，可以尝试使用枚举方式，其他特殊需求考虑双重校验锁方式。</p>]]></content>
    
    
    <categories>
      
      <category>设计模式</category>
      
      <category>创建型</category>
      
    </categories>
    
    
    <tags>
      
      <tag>设计模式</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>分布式系统</title>
    <link href="/2020/10/30/%E5%88%86%E5%B8%83%E5%BC%8F%E7%B3%BB%E7%BB%9F/"/>
    <url>/2020/10/30/%E5%88%86%E5%B8%83%E5%BC%8F%E7%B3%BB%E7%BB%9F/</url>
    
    <content type="html"><![CDATA[<h1 id="分布式系统"><a href="#分布式系统" class="headerlink" title="分布式系统"></a>分布式系统</h1><p>在介绍分布式系统之前，先说一说与之对应的集中式系统。</p><p>集中式系统有一个大型的中央处理系统，往往是一台高性能的计算机，所有数据的运算和处理都在中央计算节点上完成，然后由多个终端进行访问连接，终端只用来输入输出，不具备处理能力。像我们日常生活中见到的银行自动提款机 ATM 就是使用的集中式系统。</p><p>这类系统最大的特点就是部署简单，但是由于采用单机部署，系统会很复杂，容易发生单点故障，从而导致整个系统服务崩溃，扩展性比较差。</p><p>当然，分布式系统也有问题，系统中的各个部分彼此分开放置，这本身就带来了极大的困难，远程进程间的通信链路可能既慢又不可靠，分布式领域中的大多数研究都与“没有什么是完全可靠的”这一事实有关。</p><h2 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h2><p>维基百科的定义：分布式系统是一组电脑，透过网络相互连接传递消息与通信后并协调它们的行为而形成的系统。组件之间彼此进行交互以实现一个共同的目标。把需要进行大量计算的工程数据分割成小块，由多台计算机分别计算，再上传运算结果后，将结果统一合并得出数据结论的科学。</p><p>Google 以三驾马车开启了大数据领域的先河，此处简单罗列这三篇论文：</p><ol><li><p>2003 年公布的第一篇论文，这是一个可扩展的分布式文件系统，用于大型的、分布式的、对大量数据进行访问的应用。</p><div class="row">    <embed src="./gfs.pdf" width="100%" height="550" type="application/pdf"></div></li><li><p>2004 年发布的 MapReduce 基本上可以代表大数据处理思想的出现了，其核心是将任务拆解然后在多台廉价的计算机节点上进行运算，最后再将结果合并。</p><div class="row">    <embed src="./mapreduce.pdf" width="100%" height="550" type="application/pdf"></div></li><li><p>2006 年发布的 BigTable 启发了无数的 NoSQL 数据库，最典型的比如：Cassandra、HBase等等。</p><div class="row">    <embed src="./bigtable.pdf" width="100%" height="550" type="application/pdf"></div></li></ol><h2 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h2><p>那么，为什么人们要创建一个分布式系统呢？</p><ul><li>通过并行增加计算能力</li><li>通过复制增加容错</li><li>将计算物理上靠近外部实体，通过某种通信方式克服距离</li><li>通过隔离实现安全，解决通信协议的安全、孤立问题</li></ul><p>分布式系统虽好，但同时其复杂程度也是呈几何倍上升的，在创建和设计一个分布式系统的过程中就不可避免地产生许多问题，比如网络异常、节点故障、负载均衡、资源调度、数据的一致性、分布式事务等等。</p><p>在解决这些问题的过程中，逐渐产生了一些理论，譬如 CAP、BASE 等等，当然还有很多理论，此处暂不讨论。</p><h2 id="CAP-理论"><a href="#CAP-理论" class="headerlink" title="CAP 理论"></a>CAP 理论</h2><p>2000 年 7 月，加州大学伯克利分校的 Eric Brewer 教授在 ACM PODC 会议上提出 CAP 猜想。2 年后，麻省理工学院的 Seth Gilbert 和 Nancy Lynch 从理论上证明了 CAP。之后，CAP 理论正式成为分布式计算领域的公认定理，明确指出任何分布式系统最多可以具有以下三个属性中的两个：</p><ul><li>C：Consistency</li><li>A：Availability</li><li>P：Partition tolerance</li></ul><h3 id="一致性"><a href="#一致性" class="headerlink" title="一致性"></a>一致性</h3><p>即在写操作之后的读操作，必须返回该值。</p><p>举例说明：某条记录是 G1=a，同时这条记录有一个备份 G2=a，用户向 G1 发起写请求，将 a 改为 b，然后用户再向 G1 发起读请求得到 b，这就满足了一致性，但是用户也可能向 G2 发起读请求得到的却还是 a，这就不满足一致性。</p><p>解决方案：为了在 G2 进行读操作的时候与 G1 得到相同的结果，就要在 G1 进行写操作时，让 G1 向 G2 也发送一条信息，要求 G2 将 a 改为 b，这样用户向 G2 发起读请求时也能得到 b。</p><p>此处的一致性要区别于数据库 ACID 中的一致性，这里的一致性是指数据副本的一致性，而事务的一致性则指数据从一个状态变为另一个状态的整体是一致的。比如银行转账，甲（5元）转账给乙（10元）5 元，那么甲就一定要变为 0 元，乙一定就变为 15 元，而不是甲还有 5 元这种整体的一致性。</p><h3 id="可用性"><a href="#可用性" class="headerlink" title="可用性"></a>可用性</h3><p>即系统中非故障节点收到用户的请求后，都必须做出响应。要求系统在一个或多个节点出现故障或不可用的情况任然能够处理请求。</p><h3 id="分区容错性"><a href="#分区容错性" class="headerlink" title="分区容错性"></a>分区容错性</h3><p>从一个节点发送到另一个节点的消息允许丢失。</p><p>大多数分布式系统都存在多个子网络分区，这些分区间的通信有可能会失败，一般来说，分区容错是无法避免的，比如一台服务器在北京，一台服务器在上海，它们之间可能因为很多问题导致通信失败，所以基本可以认为分布式系统中 P 一定是成立的。</p><h3 id="冲突点"><a href="#冲突点" class="headerlink" title="冲突点"></a>冲突点</h3><p>在网络分区的情况下，我们无法实现一个同时保证可用性和一致性的分布式系统。</p><p>举例来说，如果要保证一致性，我们在向 G1 发起写操作时，需要锁定 G2 的读写操作，直到数据同步后再开放，但在锁定期间，G2 是不可用的，违背了可用性；如果要保证 G2 的可用性，那就不能锁定 G2，无法保证一致性。</p><p>因此，我们只能在提供尽力而为的可用性的同时保证强一致性，或者在提供尽力而为一致性的同时保证可用性，于是就有了下面两种系统：</p><ol><li>一致性和分区容忍系统 CP<br>CP 系统更倾向于拒绝请求，而不是提供可能不一致的数据</li><li>可用性和分区容忍系统 AP<br>AP系统则放松了一致性的要求，允许再请求期间提供可能不一致的值</li></ol><h2 id="BASE-理论"><a href="#BASE-理论" class="headerlink" title="BASE 理论"></a>BASE 理论</h2><h3 id="概述-1"><a href="#概述-1" class="headerlink" title="概述"></a>概述</h3><p>eBay 的架构师 Dan Pritchett 源于对大规模分布式系统的实践总结，在 ACM 上发表文章提出 BASE 理论，BASE 理论是对 CAP 理论的延伸，核心思想是即使无法做到强一致性（Strong Consistency，CAP 的一致性就是强一致性），但应用可以采用适合的方式达到最终一致性（Eventual Consitency）。四个字母是 Basically Available（基本可用）、Soft state（软状态）和 Eventually consistent（最终一致性）的简写。</p><h3 id="基本可用"><a href="#基本可用" class="headerlink" title="基本可用"></a>基本可用</h3><p>分布式系统在出现故障的时候，允许损失部分可用性，即保证核心可用。</p><p>举例来说，假设系统出现不可预知的故障：</p><ol><li>响应时间上的损失：正常情况搜索引擎 0.5s 返回结果，而基本可用的搜索引擎在 2s 内返回结果</li><li>功能上的损失：正常情况电商平台用户可以顺利下订单，但是促销时，为了保护购物的稳定性，部分消费者可能会被引导到一个降级页面。</li></ol><h3 id="软状态"><a href="#软状态" class="headerlink" title="软状态"></a>软状态</h3><p>允许系统存在中间状态，而该中间状态不会影响系统整体可用性，即允许系统在多个不同节点的数据副本存在数据延时。数据的三个副本和 MySQL Replication 的异步复制都是一种体现。</p><h3 id="最终一致性"><a href="#最终一致性" class="headerlink" title="最终一致性"></a>最终一致性</h3><p>最终一致性是指系统中的所有数据副本经过一定时间后，最终能够达到一致的状态。在实际工程实践中，有五种情况。</p><h4 id="因果一致性（Causal-consistency）"><a href="#因果一致性（Causal-consistency）" class="headerlink" title="因果一致性（Causal consistency）"></a>因果一致性（Causal consistency）</h4><p>如果进程 A 在更新完某个数据项后通知了进程 B，那么进程 B 之后对该数据项的访问都应该能够获取到进程 A 更新后的最新值，并且如果进程 B 要对该数据项进行更新操作的话，务必基于进程 A 更新后的最新值，即不能发生丢失更新情况。与此同时，与进程 A 无因果关系的进程 C 的数据访问则没有这样的限制。</p><h4 id="读己之所写（Read-your-writes）"><a href="#读己之所写（Read-your-writes）" class="headerlink" title="读己之所写（Read your writes）"></a>读己之所写（Read your writes）</h4><p>读己之所写是指，进程 A 更新一个数据项之后，他自己总是能够访问到更新过的最新值，而不会看到旧值。</p><h4 id="会话一致性（Session-consistency）"><a href="#会话一致性（Session-consistency）" class="headerlink" title="会话一致性（Session consistency）"></a>会话一致性（Session consistency）</h4><p>将对系统数据的访问过程框定在了一个会话当中：系统能保证在同一个有效地会话中实现“读己之所写”的一致性。</p><h4 id="单调读一致性（Monotonic-read-consistency）"><a href="#单调读一致性（Monotonic-read-consistency）" class="headerlink" title="单调读一致性（Monotonic read consistency）"></a>单调读一致性（Monotonic read consistency）</h4><p>如果一个进程从系统中读取出一个数据项的某个值后，那么系统对于该进程后续的任何数据访问都不应该返回更旧的值。</p><h4 id="单调写一致性（Monotonic-write-consistency）"><a href="#单调写一致性（Monotonic-write-consistency）" class="headerlink" title="单调写一致性（Monotonic write consistency）"></a>单调写一致性（Monotonic write consistency）</h4><p>一个系统需要能够保证来自同一个进程的写操作被顺序的执行。</p>]]></content>
    
    
    <categories>
      
      <category>分布式系统</category>
      
      <category>理论</category>
      
    </categories>
    
    
    <tags>
      
      <tag>分布式系统</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>HBase概述</title>
    <link href="/2020/10/30/HBase%E6%A6%82%E8%BF%B0/"/>
    <url>/2020/10/30/HBase%E6%A6%82%E8%BF%B0/</url>
    
    <content type="html"><![CDATA[<h1 id="HBase-概述"><a href="#HBase-概述" class="headerlink" title="HBase 概述"></a>HBase 概述</h1><p>从使用角度来看，HBase 包含了大量的关系型数据库的基本概念——表、行、列，我们通常也说 HBase 是面向列式的存储，包括我在面试一些小伙伴的时候也聊到过这个问题，不少人都认为 HBase 是列存的，但这个理解是有偏差的。</p><p>我们先简单说一下行存和列存。列式存储数据库以列为单位聚合数据，然后将列值顺序地存入磁盘，这种存储方法不同于列式存储的传统数据库，行式存储数据库连续地存储整行，下图展示了列式存储和行式存储的不同物理结构：</p><!--居左格式显示--><!--<div align=left><img src="1.svg" align="center" width = "430" height = "400" alt="RowOrColumn Storage"/>`</div>--><!-- 去除 Viewer does not support full SVG 1.1 水印--><!--sed -i 's/<text text-anchor="middle" font-size="10px" x="50%" y="100%">Viewer does not support full SVG 1.1<\/text>//g' *.svg--><p><img src="1.jpg"></p><p>行存更适合结构化数据，传统的关系型数据库基本都是行式存储，这样无论是在事务的支持还是在多表关联的场景下都能很好地发挥作用。而列存则比较适合非结构化或半结构化数据，只需要进行特定的查询，比如上表中行存有三个字段，而此时我们的业务只需要查出 name 这列的数据，因此只返回一列的查询无疑效率是最高的，在数据量很大的情况下可以减少 I/O，同时由于每列的数据类型都是一样的，我们还可以针对不同的数据类型进行压缩的优化，在查询时降低带宽的消耗。</p><p>HBase 在设计上有一个列簇的概念，那么当一个列簇下有多个列时，可以说此时 HBase 在逻辑存储上是行存的；若是一个列簇一个列，则可以说是列存的，然而其最终在物理存储上依然是 KV 的。</p><h2 id="数据模型"><a href="#数据模型" class="headerlink" title="数据模型"></a>数据模型</h2><p>下图是 HBase 的数据模型。</p><p><img src="2.jpg"></p><p>HBase</p><h2 id="架构设计"><a href="#架构设计" class="headerlink" title="架构设计"></a>架构设计</h2><p>下图是 HBase 的架构图。</p><p><img src="3.jpg"></p>]]></content>
    
    
    <categories>
      
      <category>分布式系统</category>
      
      <category>分布式存储</category>
      
      <category>HBase</category>
      
    </categories>
    
    
    <tags>
      
      <tag>HBase</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>设计模式</title>
    <link href="/2020/10/30/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    <url>/2020/10/30/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/</url>
    
    <content type="html"><![CDATA[<h1 id="设计模式"><a href="#设计模式" class="headerlink" title="设计模式"></a>设计模式</h1><h2 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h2><p>常见的设计模式大致可以分为三类：</p><ol><li>创建型<br> 常用的：单例模式、工厂模式（工厂方法和抽象工厂）、建造者模式。<br> 不常用：原型模式。</li><li>结构型<br> 常用的：代理模式、桥接模式、装饰者模式、适配器模式。<br> 不常用：门面模式、组合模式、享元模式。</li><li>行为型<br> 常用的：观察者模式、模板模式、策略模式、职责链模式、迭代器模式、状态模式。<br> 不常用：访问者模式、备忘录模式、命令模式、解释器模式、中介模式。</li></ol><p>创建型设计模式主要解决“对象的创建”问题。<br>结构型设计模式主要解决“类或对象的组合或组装”问题。<br>行为型设计模式主要解决的就是“类或对象之间的交互”问题。</p><h2 id="设计原则"><a href="#设计原则" class="headerlink" title="设计原则"></a>设计原则</h2><h3 id="SRP-单一职责原则"><a href="#SRP-单一职责原则" class="headerlink" title="SRP 单一职责原则"></a>SRP 单一职责原则</h3><p>一个类只负责完成一个职责或者功能。单一职责原则通过避免设计大而全的类，避免将不相关的功能耦合在一起，来提高类的内聚性。同时，类职责单一，类依赖的和被依赖的其他类也会变少，减少了代码的耦合性，以此来实现代码的高内聚、松耦合。但是，如果拆分得过细，实际上会适得其反，反倒会降低内聚性，也会影响代码的可维护性。</p><p>不同的应用场景、不同阶段的需求背景、不同的业务层面，对同一个类的职责是否单一，可能会有不同的判定结果。实际上，一些侧面的判断指标更具有指导意义和可执行性，比如，出现下面这些情况就有可能说明这类的设计不满足单一职责原则：</p><ul><li>类中的代码行数、函数或者属性过多；</li><li>类依赖的其他类过多或者依赖类的其他类过多；</li><li>私有方法过多；</li><li>比较难给类起一个合适的名字；</li><li>类中大量的方法都是集中操作类中的某几个属性。</li></ul><h3 id="OCP-开闭原则"><a href="#OCP-开闭原则" class="headerlink" title="OCP 开闭原则"></a>OCP 开闭原则</h3><p>对扩展开放、修改关闭</p><h3 id="ISP-接口隔离原则"><a href="#ISP-接口隔离原则" class="headerlink" title="ISP 接口隔离原则"></a>ISP 接口隔离原则</h3><p>接口隔离原则的描述是：客户端不应该强迫依赖它不需要的接口。其中的“客户端”，可以理解为接口的调用者或者使用者。理解“接口隔离原则”的重点是理解其中的“接口”二字。这里有三种不同的理解。</p><p>如果把“接口”理解为一组接口集合，可以是某个微服务的接口，也可以是某个类库的接口等。如果部分接口只被部分调用者使用，我们就需要将这部分接口隔离出来，单独给这部分调用者使用，而不强迫其他调用者也依赖这部分不会被用到的接口。</p><p>如果把“接口”理解为单个 API 接口或函数，部分调用者只需要函数中的部分功能，那我们就需要把函数拆分成粒度更细的多个函数，让调用者只依赖它需要的那个细粒度函数。</p><p>如果把“接口”理解为 OOP 中的接口，也可以理解为面向对象编程语言中的接口语法。那接口的设计要尽量单一，不要让接口的实现类和调用者，依赖不需要的接口函数。</p><p>单一职责原则针对的是模块、类、接口的设计。接口隔离原则相对于单一职责原则，一方面更侧重于接口的设计，另一方面它的思考的角度也是不同的。接口隔离原则提供了一种判断接口的职责是否单一的标准：通过调用者如何使用接口来间接地判定。如果调用者只使用部分接口或接口的部分功能，那接口的设计就不够职责单一。</p><h3 id="DIP-依赖倒置原则"><a href="#DIP-依赖倒置原则" class="headerlink" title="DIP 依赖倒置原则"></a>DIP 依赖倒置原则</h3><p>控制反转：实际上，控制反转是一个比较笼统的设计思想，并不是一种具体的实现方法，一般用来指导框架层面的设计。这里所说的“控制”指的是对程序执行流程的控制，而“反转”指的是在没有使用框架之前，程序员自己控制整个程序的执行。在使用框架之后，整个程序的执行流程通过框架来控制。流程的控制权从程序员“反转”给了框架。</p><p>依赖注入：依赖注入和控制反转恰恰相反，它是一种具体的编码技巧。我们不通过 new 的方式在类内部创建依赖类的对象，而是将依赖的类对象在外部创建好之后，通过构造函数、函数参数等方式传递（或“注入”）给类来使用。</p><p>依赖注入框架：我们通过依赖注入框架提供的扩展点，简单配置一下所有需要的类及其类与类之间的依赖关系，就可以实现由框架来自动创建对象、管理对象的生命周期、依赖注入等原本需要程序员来做的事情。</p><p>依赖反转原则：依赖反转原则也叫作依赖倒置原则。这条原则跟控制反转有点类似，主要用来指导框架层面的设计。高层模块不依赖低层模块，它们共同依赖同一个抽象。抽象不需要依赖具体实现细节，具体实现细节依赖抽象。</p><h3 id="KISS、YAGNI-原则"><a href="#KISS、YAGNI-原则" class="headerlink" title="KISS、YAGNI 原则"></a>KISS、YAGNI 原则</h3><p>KISS 原则的中文描述是：尽量保持简单。KISS 原则是保持代码可读和可维护的重要手段。KISS 原则中的“简单“”并不是以代码行数来考量的。代码行数越少并不代表代码越简单，我们还要考虑逻辑复杂度、实现难度、代码的可读性等。而且，本身就复杂的问题，用复杂的方法解决，也并不违背 KISS 原则。除此之外，同样的代码，在某个业务场景下满足 KISS 原则，换一个应用场景可能就不满足了。</p><p>对于如何写出满足 KISS 原则的代码，我总结了下面几条指导原则：</p><ul><li>不要使用同事可能不懂的技术来实现代码；</li><li>不要重复造轮子，善于使用已经有的工具类库；</li><li>不要过度优化。</li></ul><p>YAGNI 原则的英文全称是：You Ain’t Gonna Need It。直译就是：你不会需要它。这条原则也算是万金油了。当用在软件开发中的时候，它的意思是：不要去设计当前用不到的功能；不要去编写当前用不到的代码。实际上，这条原则的核心思想就是：不要做过度设计。</p><p>YAGNI 原则跟 KISS 原则并非一回事儿。KISS 原则讲的是“如何做”的问题（尽量保持简单），而 YAGNI 原则说的是“要不要做”的问题（当前不需要的就不要做）。</p><h3 id="DRY-原则"><a href="#DRY-原则" class="headerlink" title="DRY 原则"></a>DRY 原则</h3><p>DRY 原则中文描述是：不要重复自己，将它应用在编程中，可以理解为：不要写重复的代码。</p><p>专栏中讲到了三种代码重复的情况：实现逻辑重复、功能语义重复、代码执行重复。实现逻辑重复，但功能语义不重复的代码，并不违反 DRY 原则。实现逻辑不重复，但功能语义重复的代码，也算是违反 DRY 原则。而代码执行重复也算是违反 DRY 原则。</p><hr><h2 id="控制反转（IOC）"><a href="#控制反转（IOC）" class="headerlink" title="控制反转（IOC）"></a>控制反转（IOC）</h2><p>在讲“依赖反转原则”之前，我们先讲一讲“控制反转”。控制反转的英文翻译是 Inversion Of Control，缩写为 IOC。此处我要强调一下，如果你是 Java 工程师的话，暂时别把这个“IOC”跟 Spring 框架的 IOC 联系在一起。关于 Spring 的 IOC，我们待会儿还会讲到。</p><p>我们先通过一个例子来看一下，什么是控制反转。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">UserServiceTest</span> </span>&#123;  <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">doTest</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-comment">// ... </span>  &#125;    <span class="hljs-comment">// 这部分逻辑可以放到框架中</span>  <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String[] args)</span> </span>&#123;    <span class="hljs-keyword">if</span> (doTest()) &#123;      System.out.println(<span class="hljs-string">&quot;Test succeed.&quot;</span>);    &#125; <span class="hljs-keyword">else</span> &#123;      System.out.println(<span class="hljs-string">&quot;Test failed.&quot;</span>);    &#125;  &#125;&#125;</code></pre><p>在上面的代码中，所有的流程都由程序员来控制。如果我们抽象出一个下面这样一个框架，我们再来看，如何利用框架来实现同样的功能。具体的代码实现如下所示：</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">TestCase</span> </span>&#123;  <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">run</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-keyword">if</span> (doTest()) &#123;      System.out.println(<span class="hljs-string">&quot;Test succeed.&quot;</span>);    &#125; <span class="hljs-keyword">else</span> &#123;      System.out.println(<span class="hljs-string">&quot;Test failed.&quot;</span>);    &#125;  &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">doTest</span><span class="hljs-params">()</span></span>;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">JunitApplication</span> </span>&#123;  <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> List&lt;TestCase&gt; testCases = <span class="hljs-keyword">new</span> ArrayList&lt;&gt;();    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">register</span><span class="hljs-params">(TestCase testCase)</span> </span>&#123;    testCases.add(testCase);  &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String[] args)</span> </span>&#123;    <span class="hljs-keyword">for</span> (TestCase <span class="hljs-keyword">case</span>: testCases) &#123;      <span class="hljs-keyword">case</span>.run();    &#125;  &#125;</code></pre><p>把这个简化版本的测试框架引入到工程中之后，我们只需要在框架预留的扩展点，也就是 TestCase 类中的 doTest() 抽象函数中，填充具体的测试代码就可以实现之前的功能了，完全不需要写负责执行流程的 main() 函数了。 具体的代码如下所示：</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">UserServiceTest</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">TestCase</span> </span>&#123;  <span class="hljs-meta">@Override</span>  <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">doTest</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-comment">// ... </span>  &#125;&#125;<span class="hljs-comment">// 注册操作还可以通过配置的方式来实现，不需要程序员显示调用register()</span>JunitApplication.register(<span class="hljs-keyword">new</span> UserServiceTest();</code></pre><p>刚刚举的这个例子，就是典型的通过框架来实现“控制反转”的例子。框架提供了一个可扩展的代码骨架，用来组装对象、管理整个执行流程。程序员利用框架进行开发的时候，只需要往预留的扩展点上，添加跟自己业务相关的代码，就可以利用框架来驱动整个程序流程的执行。</p><p>这里的“控制”指的是对程序执行流程的控制，而“反转”指的是在没有使用框架之前，程序员自己控制整个程序的执行。在使用框架之后，整个程序的执行流程可以通过框架来控制。流程的控制权从程序员“反转”到了框架。</p><p>实际上，实现控制反转的方法有很多，除了刚才例子中所示的类似于模板设计模式的方法之外，还有马上要讲到的依赖注入等方法，所以，控制反转并不是一种具体的实现技巧，而是一个比较笼统的设计思想，一般用来指导框架层面的设计。</p><h2 id="依赖注入（DI）"><a href="#依赖注入（DI）" class="headerlink" title="依赖注入（DI）"></a>依赖注入（DI）</h2><p>接下来，我们再来看依赖注入。依赖注入跟控制反转恰恰相反，它是一种具体的编码技巧。依赖注入的英文翻译是 Dependency Injection，缩写为 DI。对于这个概念，有一个非常形象的说法，那就是：依赖注入是一个标价 25 美元，实际上只值 5 美分的概念。也就是说，这个概念听起来很“高大上”，实际上，理解、应用起来非常简单。</p><p>那到底什么是依赖注入呢？我们用一句话来概括就是：不通过 new() 的方式在类内部创建依赖类对象，而是将依赖的类对象在外部创建好之后，通过构造函数、函数参数等方式传递（或注入）给类使用。</p><p>我们还是通过一个例子来解释一下。在这个例子中，Notification 类负责消息推送，依赖 MessageSender 类实现推送商品促销、验证码等消息给用户。我们分别用依赖注入和非依赖注入两种方式来实现一下。具体的实现代码如下所示：</p><pre><code class="hljs java"><span class="hljs-comment">// 非依赖注入实现方式</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Notification</span> </span>&#123;  <span class="hljs-keyword">private</span> MessageSender messageSender;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">Notification</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-keyword">this</span>.messageSender = <span class="hljs-keyword">new</span> MessageSender(); <span class="hljs-comment">//此处有点像hardcode</span>  &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">sendMessage</span><span class="hljs-params">(String cellphone, String message)</span> </span>&#123;    <span class="hljs-comment">// ...省略校验逻辑等...</span>    <span class="hljs-keyword">this</span>.messageSender.send(cellphone, message);  &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">MessageSender</span> </span>&#123;  <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">send</span><span class="hljs-params">(String cellphone, String message)</span> </span>&#123;    <span class="hljs-comment">//....</span>  &#125;&#125;<span class="hljs-comment">// 使用Notification</span>Notification notification = <span class="hljs-keyword">new</span> Notification();<span class="hljs-comment">// 依赖注入的实现方式</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Notification</span> </span>&#123;  <span class="hljs-keyword">private</span> MessageSender messageSender;    <span class="hljs-comment">// 通过构造函数将messageSender传递进来</span>  <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">Notification</span><span class="hljs-params">(MessageSender messageSender)</span> </span>&#123;    <span class="hljs-keyword">this</span>.messageSender = messageSender;  &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">sendMessage</span><span class="hljs-params">(String cellphone, String message)</span> </span>&#123;    <span class="hljs-comment">// ...省略校验逻辑等...</span>    <span class="hljs-keyword">this</span>.messageSender.send(cellphone, message);  &#125;&#125;<span class="hljs-comment">// 使用Notification</span>MessageSender messageSender = <span class="hljs-keyword">new</span> MessageSender();Notification notification = <span class="hljs-keyword">new</span> Notification(messageSender);</code></pre><p>通过依赖注入的方式来将依赖的类对象传递进来，这样就提高了代码的扩展性，我们可以灵活地替换依赖的类。这一点在我们之前讲“开闭原则”的时候也提到过。当然，上面代码还有继续优化的空间，我们还可以把 MessageSender 定义成接口，基于接口而非实现编程。改造后的代码如下所示：</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Notification</span> </span>&#123;  <span class="hljs-keyword">private</span> MessageSender messageSender;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">Notification</span><span class="hljs-params">(MessageSender messageSender)</span> </span>&#123;    <span class="hljs-keyword">this</span>.messageSender = messageSender;  &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">sendMessage</span><span class="hljs-params">(String cellphone, String message)</span> </span>&#123;    <span class="hljs-keyword">this</span>.messageSender.send(cellphone, message);  &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">MessageSender</span> </span>&#123;  <span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">send</span><span class="hljs-params">(String cellphone, String message)</span></span>;&#125;<span class="hljs-comment">// 短信发送类</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">SmsSender</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">MessageSender</span> </span>&#123;  <span class="hljs-meta">@Override</span>  <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">send</span><span class="hljs-params">(String cellphone, String message)</span> </span>&#123;    <span class="hljs-comment">//....</span>  &#125;&#125;<span class="hljs-comment">// 站内信发送类</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">InboxSender</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">MessageSender</span> </span>&#123;  <span class="hljs-meta">@Override</span>  <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">send</span><span class="hljs-params">(String cellphone, String message)</span> </span>&#123;    <span class="hljs-comment">//....</span>  &#125;&#125;<span class="hljs-comment">//使用Notification</span>MessageSender messageSender = <span class="hljs-keyword">new</span> SmsSender();Notification notification = <span class="hljs-keyword">new</span> Notification(messageSender);</code></pre><p>实际上，你只需要掌握刚刚举的这个例子，就等于完全掌握了依赖注入。尽管依赖注入非常简单，但却非常有用，它是编写可测试性代码最有效的手段。</p><h2 id="依赖注入框架（DI-Framework）"><a href="#依赖注入框架（DI-Framework）" class="headerlink" title="依赖注入框架（DI Framework）"></a>依赖注入框架（DI Framework）</h2><p>弄懂了什么是“依赖注入”，我们再来看一下，什么是“依赖注入框架”。我们还是借用刚刚的例子来解释。</p><p>在采用依赖注入实现的 Notification 类中，虽然我们不需要用类似 hard code 的方式，在类内部通过 new 来创建 MessageSender 对象，但是，这个创建对象、组装（或注入）对象的工作仅仅是被移动到了更上层代码而已，还是需要我们自己来实现。具体代码如下所示：</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Demo</span> </span>&#123;  <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String args[])</span> </span>&#123;    <span class="hljs-comment">// 创建对象</span>    MessageSender sender = <span class="hljs-keyword">new</span> SmsSender();     <span class="hljs-comment">// 依赖注入</span>    Notification notification = <span class="hljs-keyword">new</span> Notification(sender);    notification.sendMessage(<span class="hljs-string">&quot;13918942177&quot;</span>, <span class="hljs-string">&quot;短信验证码：2346&quot;</span>);  &#125;&#125;</code></pre><p>在实际的软件开发中，一些项目可能会涉及几十、上百、甚至几百个类，类对象的创建和依赖注入会变得非常复杂。如果这部分工作都是靠程序员自己写代码来完成，容易出错且开发成本也比较高。而对象创建和依赖注入的工作，本身跟具体的业务无关，我们完全可以抽象成框架来自动完成。</p><p>你可能已经猜到，这个框架就是“依赖注入框架”。我们只需要通过依赖注入框架提供的扩展点，简单配置一下所有需要创建的类对象、类与类之间的依赖关系，就可以实现由框架来自动创建对象、管理对象的生命周期、依赖注入等原本需要程序员来做的事情。</p><p>实际上，现成的依赖注入框架有很多，比如 Google Guice、Java Spring、Pico Container、Butterfly Container 等。不过，如果你熟悉 Java Spring 框架，你可能会说，Spring 框架自己声称是控制反转容器（Inversion Of Control Container）。</p><p>实际上，这两种说法都没错。只是控制反转容器这种表述是一种非常宽泛的描述，DI 依赖注入框架的表述更具体、更有针对性。因为我们前面讲到实现控制反转的方式有很多，除了依赖注入，还有模板模式等，而 Spring 框架的控制反转主要是通过依赖注入来实现的。</p>]]></content>
    
    
    <categories>
      
      <category>设计模式</category>
      
      <category>概述</category>
      
    </categories>
    
    
    <tags>
      
      <tag>设计模式</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Flink源码编译</title>
    <link href="/2020/10/30/Flink%E6%BA%90%E7%A0%81%E7%BC%96%E8%AF%91/"/>
    <url>/2020/10/30/Flink%E6%BA%90%E7%A0%81%E7%BC%96%E8%AF%91/</url>
    
    <content type="html"><![CDATA[<h1 id="Flink-源码编译"><a href="#Flink-源码编译" class="headerlink" title="Flink 源码编译"></a>Flink 源码编译</h1><h2 id="环境"><a href="#环境" class="headerlink" title="环境"></a>环境</h2><p>仅记录编译遇到的两个问题</p><h3 id="版本"><a href="#版本" class="headerlink" title="版本"></a>版本</h3><p>flink-tag-1.11.2<br>jdk-1.8.0_251<br>scala-2.12.11<br>apache-maven-3.5.4</p><h3 id="编译"><a href="#编译" class="headerlink" title="编译"></a>编译</h3><pre><code class="hljs apache"><span class="hljs-attribute">git</span> clone git@github.com:apache/flink.git<span class="hljs-attribute">cd</span> flink<span class="hljs-attribute">git</span> checkout -b xxx release-<span class="hljs-number">1</span>.<span class="hljs-number">11</span>.<span class="hljs-number">2</span>-rc<span class="hljs-number">1</span><span class="hljs-attribute">mvn</span> clean package -DskipTests -e</code></pre><h3 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h3><ol><li>依赖包下载失败，需要重新获取</li></ol><p><img src="1.jpg"></p><pre><code class="hljs powershell">[<span class="hljs-type">ERROR</span>] Failed to execute goal on project flink<span class="hljs-literal">-azure</span><span class="hljs-literal">-fs</span><span class="hljs-literal">-hadoop</span>: Could not resolve dependencies <span class="hljs-keyword">for</span> project org.apache.flink:flink<span class="hljs-literal">-azure</span><span class="hljs-literal">-fs</span><span class="hljs-literal">-hadoop</span>:jar:<span class="hljs-number">1.11</span>.<span class="hljs-number">2</span>: Failure to find io.reactivex:rxjava:jar:<span class="hljs-number">1.3</span>.<span class="hljs-number">8</span> <span class="hljs-keyword">in</span> http://<span class="hljs-number">192.168</span>.<span class="hljs-number">0.139</span>:<span class="hljs-number">8081</span>/repository/maven<span class="hljs-literal">-public</span>/ was cached <span class="hljs-keyword">in</span> the local repository, resolution will not be reattempted <span class="hljs-keyword">until</span> the update interval of nexus_public has elapsed or updates are forced -&gt; [<span class="hljs-type">Help</span> <span class="hljs-number">1</span>]org.apache.maven.lifecycle.LifecycleExecutionException: Failed to execute goal on project flink<span class="hljs-literal">-azure</span><span class="hljs-literal">-fs</span><span class="hljs-literal">-hadoop</span>: Could not resolve dependencies <span class="hljs-keyword">for</span> project org.apache.flink:flink<span class="hljs-literal">-azure</span><span class="hljs-literal">-fs</span><span class="hljs-literal">-hadoop</span>:jar:<span class="hljs-number">1.11</span>.<span class="hljs-number">2</span>: Failure to find io.reactivex:rxjava:jar:<span class="hljs-number">1.3</span>.<span class="hljs-number">8</span> <span class="hljs-keyword">in</span> http://<span class="hljs-number">192.168</span>.<span class="hljs-number">0.139</span>:<span class="hljs-number">8081</span>/repository/maven<span class="hljs-literal">-public</span>/ was cached <span class="hljs-keyword">in</span> the local repository, resolution will not be reattempted <span class="hljs-keyword">until</span> the update interval of nexus_public has elapsed or updates are forced</code></pre><p>flink-azure-fs-hadoop 模块的依赖 jar 包 io.reactivex:rxjava:jar:1.3.8 未下载完全，因为本地有缓存，编译时不会重新拉取，手动删除如下图的目录，重新编译下载即可。</p><p><img src="2.jpg"></p><ol start="2"><li>库中缺少依赖包，需手动下载</li></ol><p><img src="3.jpg"></p><pre><code class="hljs powershell">[<span class="hljs-type">ERROR</span>] Failed to execute goal on project flink<span class="hljs-literal">-avro</span><span class="hljs-literal">-confluent</span><span class="hljs-literal">-registry</span>: Could not resolve dependencies <span class="hljs-keyword">for</span> project org.apache.flink:flink<span class="hljs-literal">-avro</span><span class="hljs-literal">-confluent</span><span class="hljs-literal">-registry</span>:jar:<span class="hljs-number">1.11</span>.<span class="hljs-number">2</span>: Could not find artifact io.confluent:kafka<span class="hljs-literal">-schema</span><span class="hljs-literal">-registry</span><span class="hljs-literal">-client</span>:jar:<span class="hljs-number">4.1</span>.<span class="hljs-number">0</span> <span class="hljs-keyword">in</span> nexus_public (http://<span class="hljs-number">192.168</span>.<span class="hljs-number">0.139</span>:<span class="hljs-number">8081</span>/repository/maven<span class="hljs-literal">-public</span>/) -&gt; [<span class="hljs-type">Help</span> <span class="hljs-number">1</span>]org.apache.maven.lifecycle.LifecycleExecutionException: Failed to execute goal on project flink<span class="hljs-literal">-avro</span><span class="hljs-literal">-confluent</span><span class="hljs-literal">-registry</span>: Could not resolve dependencies <span class="hljs-keyword">for</span> project org.apache.flink:flink<span class="hljs-literal">-avro</span><span class="hljs-literal">-confluent</span><span class="hljs-literal">-registry</span>:jar:<span class="hljs-number">1.11</span>.<span class="hljs-number">2</span>: Could not find artifact io.confluent:kafka<span class="hljs-literal">-schema</span><span class="hljs-literal">-registry</span><span class="hljs-literal">-client</span>:jar:<span class="hljs-number">4.1</span>.<span class="hljs-number">0</span> <span class="hljs-keyword">in</span> nexus_public (http://<span class="hljs-number">192.168</span>.<span class="hljs-number">0.139</span>:<span class="hljs-number">8081</span>/repository/maven<span class="hljs-literal">-public</span>/)</code></pre><p>点击此处下载：<a href="http://packages.confluent.io/maven/io/confluent/kafka-schema-registry-client/4.1.0/">http://packages.confluent.io/maven/io/confluent/kafka-schema-registry-client/4.1.0/</a><br>重新编译即可。</p><p>最终编译完成，直接通过 IDEA 导入</p><p><img src="4.jpg"></p>]]></content>
    
    
    <categories>
      
      <category>编译</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Flink</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
